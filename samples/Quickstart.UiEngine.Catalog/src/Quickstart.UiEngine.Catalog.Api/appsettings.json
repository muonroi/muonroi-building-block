{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  // UiEngineCatalogOptions are configured in code (AddUiEngineCatalog).
  // To persist snapshots, set a connection string there, e.g.:
  //   options.PostgresConnectionString = "Host=localhost;Database=ui_catalog;...";
  // When left empty the in-memory snapshot store is used (no database).
}
