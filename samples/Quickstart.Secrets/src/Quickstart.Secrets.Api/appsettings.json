{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Read by ConfigurationSecretProvider via ISecretProvider.GetSecret("Secrets:ApiKey").
  // In production these would come from environment variables or a secret vault, NOT source control.
  "Secrets": {
    "ApiKey": "demo-api-key-12345",
    "ConnectionString": "Server=localhost;Database=quickstart;"
  }
}
