{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Connection string consumed by AddMConnectorPersistence() to configure
  // ConnectorDbContext (Npgsql). No connection opens until a store operation
  // runs, so the app starts without a live database.
  "ConnectionStrings": {
    "ConnectorDb": "Host=localhost;Port=5432;Database=muonroi_connectors;Username=admin;Password=admin"
  }
}
