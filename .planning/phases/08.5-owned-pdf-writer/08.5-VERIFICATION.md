---
phase: 08.5-owned-pdf-writer
verified: 2026-05-27T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: initial verification
gaps: []
known_gaps:
  - "qpdf not installed on host — external PDF structural validation absent; determinism canary (renders twice, asserts byte-identity, requires parseable+reproducible PDF) substitutes. NOT a hard fail per phase brief."
human_verification: []
---

# Phase 8.5: Owned PDF Writer Verification Report

**Phase Goal:** Own the final PDF-serialization layer with an allocation-controlled, deterministic, AOT-trivial PDF 1.7 writer; close ALLOC-01/SC4 carried over from Phase 8 and remove PdfSharpCore.
**Verified:** 2026-05-27
**Status:** PASSED
**Re-verification:** No — initial verification
**Branch:** `phase/08.5-owned-pdf-writer` (HEAD `3ea4348`)

All checks below were observed directly by the verifier (build run, probe run, full suite run, grep of full src/ + tests/ tree, file reads). The executor's self-report numbers were confirmed to match reality, not trusted on faith.

## Goal Achievement — Observable Truths

| # | Truth (criterion) | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | SC4 / ALLOC-01: total render allocation ≤ 288.96 MB | ✓ VERIFIED | Probe run in **Release** (`--no-build`): `Passed Probe_OwnedPdfWriter_PerStageAllocations [478 ms]`; observed output `TOTAL: 51.62 MB (threshold 288.96 MB) … SC4 MET ✓`. Per-stage parse 1.38 / cascade 0.02 / policy 3.46 / layout 31.14 / write 15.62. Assertion `totalMb.Should().BeLessThanOrEqualTo(288.96)` at `AllocationProbe.cs:103`; threshold constant `Sc4ThresholdMb = 288.96` at line 34. Number is **measured** via `GC.GetTotalAllocatedBytes(precise:true)` deltas (`AllocationProbe.cs:116-120`), not hardcoded. |
| 2 | Vietnamese/Unicode renders correctly (CID Type0, 2-byte GID, no `?`) | ✓ VERIFIED | `OwnedPdfWriter.cs` emits `/Subtype /Type0` (L303), `/Encoding /Identity-H` (L304), `/Subtype /CIDFontType2` (L281), `/FontFile2` (L274), `/FontDescriptor` (L266), ToUnicode CMap (L238-248). Test `WriteAsync_VietnameseText_DoesNotProduceQuestionMarks` renders "Tiếng Việt" (U+1EA0..U+1EF9) and asserts `CIDFontType2` + `ToUnicode` present (`OwnedPdfWriterCidFontTests.cs:102-113`); `WriteAsync_CidFont_NoWinAnsiEncoding` asserts no legacy single-byte encoding (L138-144). 6 CID tests pass within the green suite. |
| 3 | PdfSharpCore fully removed (primary goal) | ✓ VERIFIED | Grep of **entire** `src/` for `PdfSharpCore\|using PdfSharp\|GlobalFontSettings\|XGraphics\|XFont\|PdfReader`: **2 hits, both comments** in `OwnedPdfWriter.cs` (L8, L23). `tests/`: 1 hit, a comment (`PdfWriterTests.cs:119`). `*.csproj` grep for `PdfSharpCore`: **0 hits**. `PdfSharpCoreWriter.cs` → **GONE**; `PdfSharpFontResolverAdapter.cs` → **GONE** (filesystem check). `MPdfService.RenderMultiPageAsync` (L155-258) merges at layout level (concatenate `PositionedPage`s → single `_writer.WriteAsync`, L248) — **no `PdfReader.Open`, no `using PdfSharpCore`**. Only third-party font dep remaining: `SixLabors.Fonts` (managed). |
| 4 | Default writer is OwnedPdfWriter; test parallelism restored | ✓ VERIFIED | `PdfServiceCollectionExtensions.cs:63` → `services.TryAddSingleton<IPdfWriter, OwnedPdfWriter>();`. `PdfRenderCollection.cs:7` → `DisableParallelization = false`. Test harness `PdfServiceTestHarness.BuildProvider()` calls `AddPdf(cfg)` so the entire golden/canary/integration path resolves OwnedPdfWriter. |
| 5 | Suite green + snapshots re-baselined; no excluded categories | ✓ VERIFIED | `dotnet build -c Release`: **0 Error(s)** (80 warnings, all XML-doc/IL trim). `dotnet test -c Release --no-build` (plain, no `-m:1`): **`Failed: 0, Passed: 215, Skipped: 0, Total: 215`**. SlowIntegration ran (probe included). Matches SUMMARY claim of 215. 56 golden snapshots verified through OwnedPdfWriter via DI (`GoldenPdf.RenderAsync` → `IMPdfService` → OwnedPdfWriter). |
| 6 | Determinism: byte-for-byte against OwnedPdfWriter; NormalizeForDeterminism hack gone | ✓ VERIFIED | `DeterminismCanaryTests.CorpusCase_RendersByteDeterministically` renders each corpus case twice via the OwnedPdfWriter path and asserts `a.SequenceEqual(b)` (`DeterminismCanaryTests.cs:22-26`). Grep for `NormalizeForDeterminism` across `*.cs`: **0 hits** (regex hack removed). Writer emits deterministic tokens natively: fixed `/ID [<000…><000…>]` (`OwnedPdfWriter.cs:34`), `D:20000101000000Z` (L36), subset prefix `AAAAAA+` (L230), binary comment byte `0xE2` (L994). All determinism tests pass in the green suite. |
| 7 | SEC-02: never emits /JavaScript, /Launch, /OpenAction, /EmbeddedFile | ✓ VERIFIED | Test `WriteAsync_CidFont_NoForbiddenSecurityEntries` asserts `NotContain` on all four tokens against real emitted bytes (`OwnedPdfWriterCidFontTests.cs:161-164`). Writer catalog comment documents invariant (L116). `OwnedPdfWriter_OutputHeaderIsPdf17` confirms `%PDF-1.7` header (`SecurityTests.cs:103-117`). |

**Score:** 7/7 truths verified.

## Hard Constraints

| Constraint | Status | Evidence |
|-----------|--------|----------|
| No Chromium/browser/native/commercial deps | ✓ HELD | Grep `Chromium\|wkhtmltopdf\|DinkToPdf\|Puppeteer\|Playwright\|SkiaSharp\|libgdiplus\|System.Drawing\|HarfBuzz` in `src/Muonroi.Pdf`: 0 hits. `Muonroi.Pdf.csproj` refs: SixLabors.Fonts + Microsoft.Extensions.* only. Governance csproj: AngleSharp + AngleSharp.Css only. |
| Pure-managed C# | ✓ HELD | Writer uses `System.IO.Compression.ZLibStream` for FlateDecode; no P/Invoke, no native binaries. |
| Open-core licensing intact | ✓ HELD | No commercial package references introduced; PdfSharpCore (the only flagged dep) removed. |

## Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `src/.../Writer/OwnedPdfWriter.cs` | ✓ VERIFIED | Substantive (~990 lines), CID font path, FlateDecode, image XObjects, deterministic tokens, SEC-02 invariant. Wired via DI. No debt markers (TODO/FIXME/XXX/HACK/NotImplemented = 0). |
| `src/.../Writer/PdfSharpCoreWriter.cs` | ✓ DELETED | Filesystem confirms gone. |
| `src/.../Writer/PdfSharpFontResolverAdapter.cs` | ✓ DELETED | Filesystem confirms gone. |
| `tests/.../Service/AllocationProbe.cs` | ✓ VERIFIED | Real GC-delta measurement + SC4 assertion; passes in Release. |
| `tests/.../Writer/OwnedPdfWriterCidFontTests.cs` | ✓ VERIFIED | Vietnamese + CID + SEC-02 coverage. |
| `tests/.../Golden/DeterminismCanaryTests.cs` | ✓ VERIFIED | Twice-render byte-identity over corpus. |

## Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| DI registration | OwnedPdfWriter | `TryAddSingleton<IPdfWriter, OwnedPdfWriter>` | ✓ WIRED |
| MPdfService.RenderMultiPageAsync | OwnedPdfWriter | `_writer.WriteAsync(allPages,…)` (no PdfReader) | ✓ WIRED |
| GoldenPdf/Canary tests | OwnedPdfWriter | `BuildProvider→AddPdf→IMPdfService→IPdfWriter` | ✓ WIRED |
| OwnedPdfWriter | deterministic sentinels | `/ID`, `D:20000101`, `AAAAAA+`, `0xE2` | ✓ WIRED |

## Behavioral Spot-Checks (run by verifier)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Release build clean | `dotnet build -c Release` | 0 errors | ✓ PASS |
| SC4 allocation probe | `dotnet test --filter Probe_OwnedPdfWriter… -c Release --no-build` | TOTAL 51.62 MB ≤ 288.96; Passed: 1 | ✓ PASS |
| Full Pdf suite | `dotnet test -c Release --no-build` | Failed 0 / Passed 215 / Skipped 0 | ✓ PASS |

## Anti-Patterns Found

None. Zero debt markers in `OwnedPdfWriter.cs`. The only `PdfSharpCore`/`PdfSharp*` strings remaining anywhere in src/tests are explanatory comments (3 total), none in emitted output or active code paths.

## Known Gaps (non-blocking)

- **qpdf not installed on host.** External structural PDF validation is unavailable. Per the phase brief this is an accepted substitution: the determinism canary renders each case twice and asserts byte-identity, which requires the PDF to be reproducible and is exercised across the full corpus. Recommend a one-time qpdf/veraPDF structural validation pass before GA, but it does not block Phase 8.5 closure.

## Gaps Summary

No blocking gaps. Every ROADMAP success criterion (SC1 spike/alloc, SC2 deterministic valid PDF 1.7, SC3 golden re-baseline + Vietnamese, SC4 PdfSharpCore removed) and every brief-specified truth is backed by verifier-observed evidence:
- SC4/ALLOC-01 is **independently reproduced**: 51.62 MB measured in Release, 82% under the 288.96 MB threshold — the deferred Phase 8 criterion is closed.
- PdfSharpCore is **fully eliminated** from runtime code, package graph, and the multi-page merge path — the user's primary goal is met.
- Suite is fully green (215/215, 0 skipped) with golden snapshots re-baselined through the owned writer; determinism, CID/Vietnamese, and SEC-02 all have passing tests.

## Verdict

**PASS — Phase 8.5 closes both the Phase 8 SC4 carry-over (ALLOC-01) and the PdfSharpCore-removal goal.** The phase can be called done. Only non-blocking follow-up: an external structural PDF validator (qpdf/veraPDF) should be run once before GA, since the host lacked qpdf during this phase.

---

_Verified: 2026-05-27_
_Verifier: Claude (gsd-verifier)_
