---
phase: 08.6-rendering-fidelity
verified: 2026-05-27T00:00:00Z
status: passed
score: 12/12 FIDELITY requirements verified (4/4 ROADMAP success criteria)
verifier: Claude Opus 4.7 (1M context) — independent goal-backward verification
re_verification: No — initial verification
---

# Phase 8.6: Rendering Fidelity — Verification Report

**Phase Goal:** Eliminate the silent-drop rendering gaps surfaced by the Phase 8.5 capability
audit. Parsed-but-not-rendered CSS and unsupported inputs must either render correctly or fail
loudly under policy — never silently produce wrong output. Requirements FIDELITY-01..12.

**Verified:** 2026-05-27
**Branch:** `phase/08.6-rendering-fidelity` (HEAD `1f1a362`)
**Method:** Independent goal-backward — read source to confirm each SC is actually implemented
(not "tasks done"), ran the full test suite in this verifier's own process, ran SEC-02 grep,
inspected the visual-regression suite, and checked working-tree cleanliness. No source modified.

## Verdict: PASS

All 12 in-scope FIDELITY requirements are implemented in source and exercised by tests.
Full suite is green at **275/275, 0 fail**. SEC-02 holds (0 non-comment occurrences). The
visual gate is rasterization-based (PDFium non-white-pixel + content-stream structural guards),
not byte-compare, and both required guards exist and assert real content. The throwaway
real-template harness is correctly excluded. Working tree has no stray source changes.

---

## Per-SC Verification (FIDELITY-01..12)

| # | Requirement | Status | Evidence (file:line) |
|---|-------------|--------|----------------------|
| FIDELITY-01 | `text-align` center/right/justify changes word x-positions | **PASS** | Read: `BoxTreeBuilder.cs:177-180` (text-align resolved onto box). Apply: `InlineLayoutEngine.cs:63-87` (`wordOffsetX` switch: right/center/justify with `isLastLine` guard; justify gap distributed per inter-word space). Threaded block→inline via `LayoutContext.TextAlign`, propagated in `BlockLayoutEngine.cs:125-126`. |
| FIDELITY-02 | `line-height` increases inter-line spacing | **PASS** | Read + unit conversion (unitless / px→factor / %): `BoxTreeBuilder.cs:205-226`. Applied: `InlineLayoutEngine.cs:44-46` (`lineHeight *= dominantBox.LineHeightFactor`). |
| FIDELITY-03 | `text-decoration:underline/strikethrough` drawn by writer | **PASS** | `<u>`→`underline`, `<s>/<strike>/<del>`→`line-through` in `BoxTreeBuilder.cs:103-107`; CSS cascade read `BoxTreeBuilder.cs:228-233`. Drawn as `re`/`f` filled rect outside BT/ET in `OwnedPdfWriter.cs:741-766`. |
| FIDELITY-04 | `<br>` commits line and advances Y | **PASS** | `<br>`→`LineBreakBox` in `BoxTreeBuilder.cs:84-85`; detected and force-commits the current line in `InlineLayoutEngine.cs:132-133, 183`. |
| FIDELITY-05 | `<hr>` reserves block space and emits filled rect | **PASS** | `<hr>`→`HrBox` (thickness/color read) `BoxTreeBuilder.cs:87-100`. Space reserved + `PositionedElement` emitted `BlockLayoutEngine.cs:108-119`. Filled rect (`rg`/`re`/`f`) drawn `OwnedPdfWriter.cs:650-668`. |
| FIDELITY-06 | `<ul>/<li>` bullet; `<ol>/<li>` ordinal | **PASS** | Marker InlineBox prepended `BoxTreeBuilder.cs:49-52, 313-372`; `DetermineListMarker` returns `"• "` for `<ul>`, per-`<ol>` ordinal counter (`_olOrdinalCounters`) `BoxTreeBuilder.cs:20-21, 374-393`. Marker + inline children wrapped in one `AnonymousBox` so they share the row (`:365-372`). |
| FIDELITY-07 | `<a href>` produces `/Annots /Link /URI` | **PASS** | `<a>` → InlineBox with scheme-filtered `LinkHref` `BoxTreeBuilder.cs:109-124`; link rects collected into `PositionedPage.LinkAnnotations` `InlineLayoutEngine.cs:114-117`; emitted as `/Subtype /Link ... /A << /S /URI /URI (...) >>` `OwnedPdfWriter.cs:201-234`. |
| FIDELITY-08 | OTF-CFF font throws `PdfFormatException("FONT-OTF-CFF")` | **PASS** | sfntVersion `0x4F54544F` ('OTTO') detected, throws fail-loud `TrueTypeFontSubsetter.cs:28, 40-43`. No silent Latin-1 fallback. |
| FIDELITY-09 | WOFF/WOFF2 throws `PdfFormatException("FONT-WOFF")` | **PASS** | sfntVersion `0x774F4646` ('wOFF') and `0x774F4632` ('wOF2') throw `TrueTypeFontSubsetter.cs:29-30, 47-50`. |
| FIDELITY-10 | RGBA PNG throws `PdfFormatException("PNG-RGBA")` | **PASS** | `color_type==6` throws `PureImageDecoder.cs:56-57`. Extra fail-loud gates: GRAYSCALE (0), GRAYSCALE-ALPHA (4), 16-bit (`:50-60`); 8-bit RGB-only boundary enforced `:62-64`. |
| FIDELITY-11 | Palette PNG throws `PdfFormatException("PNG-PALETTE")` | **PASS** | `color_type==3` throws `PureImageDecoder.cs:47-48`. |
| FIDELITY-12 | `javascript:`/`file:` href rejected by policy | **PASS** | `DefaultStrictPolicy.cs:168-195` adds `forbidden.link.scheme` for any non-(http/https/mailto/relative) scheme. Defense-in-depth: `BoxTreeBuilder.cs:115-121` nulls the disallowed href at box-tree level. |

**Score: 12/12 PASS.** No PARTIAL, no FAIL.

### ROADMAP Success Criteria roll-up

| SC | Description | Status |
|----|-------------|--------|
| SC1 | Text layout fidelity (text-align, line-height, text-decoration) | **PASS** — FIDELITY-01/02/03 |
| SC2 | HTML5 semantics (br/hr, ul/ol/li, a) + policy-gated, never silently dropped | **PASS** — FIDELITY-04/05/06/07/12 |
| SC3 | Font + image robustness (fail-loud): OTF-CFF, WOFF, PNG variants rejected; no silent fallback | **PASS** — FIDELITY-08/09/10/11 |
| SC4 | No regression: full suite green; baselines re-baselined for intentional changes | **PASS** — 275/275, 0 fail (see below) |

---

## Test Suite Result

Command run by this verifier (plain `dotnet test`, NO `-m:1 -nodereuse:false`):

```
dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj
→ Passed!  - Failed: 0, Passed: 275, Skipped: 0, Total: 275, Duration: 6 s
```

**275/275 pass, 0 fail — matches expected count exactly.** Build succeeded with only
non-blocking warnings (CS1591 missing XML docs, IL2026/IL3050 AOT-trim advisories on
`BindConfiguration`, CA1416 platform notes on `Conversion.SavePng`). None affect correctness.

Note: SUMMARY.md (08.6-06) recorded 251 tests at plan-06 completion; the post-debug HEAD now
has 275 (the visual-regression suite and additional guards were added during the debug rounds
`60d5a5e..1f1a362`). The live count is authoritative.

---

## SEC-02 Result: PASS

Grep over `src/**/*.cs` for `/JavaScript|/Launch|/OpenAction|/EmbeddedFile` returned 5 hits,
**all in comments** — zero in emitted PDF content:

- `OwnedPdfWriter.cs:4, 123, 201` — SEC-02 documentation comments (writer never emits these).
- `RuleEngine.Runtime/Adapters/RuleGraphEntry.cs:30`, `RuleGraphParser.cs:126` — unrelated
  "FEEL/JavaScript condition" comments, not PDF dictionary entries.

URI link annotations (`/A /S /URI`) are present and expected (FIDELITY-07) — correctly NOT
flagged. **0 non-comment occurrences of the forbidden entries.**

---

## Visual Gate Assessment: MEANINGFUL (not byte-compare)

`tests/Muonroi.Pdf.Tests/Golden/VisualRegressionTests.cs` rasterizes via `PDFtoImage`/PDFium at
150 DPI and asserts real content:

- `PageIsNotBlank_AfterFix` (`:84-104`) — counts non-white pixels via SkiaSharp, asserts
  fraction ≥ 0.05% (blank page = 0%). True rasterization, not byte-compare.
- `ContentStreamUsesGidHex_NotLatin1Literal` (`:106-129`) — asserts `<XXXX> Tj` present and no
  `(text) Tj` Latin-1 literal.
- `ContentStream_NoDuplicateHexTjStringsOnSameLine` (`:138-208`) — Bug A guard.
- `FontWidthArray_NoNearZeroAdvances` (`:215-250`) — Bug B guard (`/W` ≥ 100 per-mille).
- `ListMarker_BulletGlyph_IsEmittedInContentStream` (`:315-342`) — Bug C guard.

Both required guards exist and assert real content:

- **`TextDecorationUnderline_ContentStream_ContainsDecorationRect`** (`:356-381`) — decompresses
  the content stream and asserts the `ET ... re ... f ... BT` decoration-rect pattern is present
  for the `<u>` case. Real structural assertion of the drawn underline.
- **`ListMarker_SharesRowWithItemText`** (`:397-457`) — parses `1 0 0 1 X Y Tm` + `<...> Tj`
  pairs, groups by Y (0.1pt tolerance), and asserts ≥3 rows each have ≥2 Tj calls (bullet +
  item word co-located on one line). Real assertion that the marker is inline-left, not on its
  own row.

PDFtoImage is test-only (documented `:33-34`); no `src/` package references it (confirmed — the
only matches are in the test project). Orchestrator has independently confirmed
`text-decoration-underline.png` (underline drawn) and `list-unordered.png` (• Item inline) by eye.

---

## Throwaway Harness Exclusion: PASS

- No compiled `*RealTemplate*.cs` anywhere under `tests/` (find returned nothing).
- No references to `RealTemplateBaseline` in `tests/`.
- The harness lives only as `.planning/phases/_pending-real-template/RealTemplateBaselineTests.cs.txt`
  (`.txt`, not compiled). Correctly excluded from the build.

---

## Working Tree Cleanliness: PASS (within stated allowances)

`git status --porcelain`:
- ` M .planning/ROADMAP.md` — planning completion edit (allowed).
- `?? .planning/phases/_pending-real-template/` — parked harness `.cs.txt` (allowed).
- `?? samples/Muonroi.Pdf.AotSample/.vs/` — IDE cache, gitignored category (allowed).

**No stray uncommitted source changes.** All phase source landed across commits
`20dca4a..1f1a362` (incl. debug rounds `60d5a5e`, `78bb96c`, `ee5f36c`, `2dde780`, `1f1a362`).

---

## Anti-Patterns

No blocker anti-patterns found in phase source. Fail-loud philosophy is enforced
(`PdfFormatException` thrown for every unsupported font/image variant; `OwnedPdfWriter.cs:734-738`
throws `FONT-GID-MAP-MISSING` rather than emitting silent blank output). No unreferenced
TBD/FIXME/XXX debt markers introduced. The "8-bit RGB only" boundary is enforced loudly
(`PureImageDecoder.cs:62-64`).

---

## Human Verification

None outstanding for sign-off. The two visual PNGs called out in scope
(`text-decoration-underline.png`, `list-unordered.png`) were already visually confirmed correct
by the orchestrator. The full visual-regression suite passes as part of the 275/275 run.

---

_Verified: 2026-05-27_
_Verifier: Claude Opus 4.7 (1M context) — independent goal-backward verification_
