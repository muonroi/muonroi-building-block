---
phase: 08.6-rendering-fidelity
plan: "06"
subsystem: tests
tags: [golden, rejection-tests, fidelity, policy]
completed: 2026-05-27
duration_minutes: 35
---

# Phase 8.6 Plan 06: Test Coverage Gate Summary

## One-Liner

12 FIDELITY requirement tests added: font/image rejection gates, policy href gate, 7 fidelity golden cases, 5 HTML5 semantics golden cases; 251 total tests pass, 56 existing baselines unchanged.

## Tasks

### Task 1 — Rejection and policy unit tests (FIDELITY-08..12)

**Result:** Green. 12 tests pass.

- `FontRejectionTests.cs`: FONT-OTF-CFF thrown for sfntVersion=0x4F54544F; FONT-WOFF thrown for wOFF and wOF2; valid TTF subsets without error.
- `ImageRejectionTests.cs`: PNG-RGBA (color_type=6), PNG-PALETTE (color_type=3), PNG-GRAYSCALE (color_type=0), PNG-16BIT (bit_depth=16/color_type=2) all throw PdfFormatException with correct RuleId; valid 8-bit RGB PNG decodes Width=1 Height=1.
- `PolicyTests.cs`: javascript: and file: hrefs produce `forbidden.link.scheme` violation; https: href produces no violation.

**Commit:** f7c8fb0

**Files:**
- `tests/Muonroi.Pdf.Tests/Font/FontRejectionTests.cs` (new)
- `tests/Muonroi.Pdf.Tests/Image/ImageRejectionTests.cs` (new)
- `tests/Muonroi.Pdf.Tests/Policy/PolicyTests.cs` (new)

---

### Task 2 — Golden corpus cases + baselines (FIDELITY-01..07)

**Result:** Green. 12 new snapshot cases pass; 56 existing baselines byte-identical (zero regressions). 251 total tests pass.

**FidelityLayout group (7 cases):**
- `text-align-center` — text-align:center paragraph
- `text-align-right` — text-align:right paragraph
- `text-align-justify` — text-align:justify multi-sentence paragraph (last line not stretched)
- `line-height-factor` — unitless line-height:2.0 with br break
- `line-height-px` — explicit 32px line-height on 16px font
- `text-decoration-underline` — `<u>` element rendering
- `text-decoration-strikethrough` — `<s>` element rendering

**Html5Semantics group (5 cases):**
- `br-line-break` — three-line paragraph with `<br/>` breaks
- `hr-rule` — gray horizontal rule between paragraphs
- `list-unordered` — `<ul><li>` with bullet markers
- `list-ordered` — `<ol><li>` with ordinal markers (1. 2. 3.)
- `link-annotation` — `<a href="https://example.com">` as PDF /URI link annotation

**Baseline generation:** Run with `MUONROI_UPDATE_SNAPSHOTS=1` filtered to FidelityLayout|Html5Semantics only. Full suite run without flag confirmed 56 existing baselines unchanged.

**Commit:** 7402945

**Files:**
- `tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs` (modified — FidelityLayout + Html5Semantics groups added; AllCases extended)
- `tests/Muonroi.Pdf.Tests/Golden/FidelityGoldenTests.cs` (new)
- `tests/Muonroi.Pdf.Tests/Golden/Html5SemanticsGoldenTests.cs` (new)
- `tests/Muonroi.Pdf.Tests/TestResources/Golden/*.pdf` (12 new baseline PDFs)

---

## Test Count Delta

| Before | After | Added |
|--------|-------|-------|
| 215 | 251 | 36 |

(36 = 12 rejection/policy unit tests from Task 1 + 12 golden theory executions from Task 2 + 12 determinism canary runs over the new corpus cases)

## SEC-02 Verification

grep result from Plan 04 (inherited, unchanged): 0 occurrences of `/JavaScript`, `/Launch`, `/OpenAction`, `/EmbeddedFile` in non-comment source code.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. All 12 new golden cases render actual content through the full pipeline.

## Self-Check: PASSED

- f7c8fb0 exists: confirmed
- 7402945 exists: confirmed
- 12 new PDF baselines in TestResources/Golden/: confirmed (68 total, was 56)
- 251 tests pass, 0 fail: confirmed
- No existing baseline modified (git status shows only new files, GoldenCorpus.cs modified): confirmed
