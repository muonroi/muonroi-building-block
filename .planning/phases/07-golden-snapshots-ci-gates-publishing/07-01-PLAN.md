---
phase: 07-golden-snapshots-ci-gates-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/BlockLayoutGoldenTests.cs
  - tests/Muonroi.Pdf.Tests/Golden/DeterminismCanaryTests.cs
  - tests/Muonroi.Pdf.Tests/TestResources/Golden/.gitkeep
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "GoldenPdf.VerifyAsync renders HTML to bytes and asserts byte-equality against a committed embedded baseline, throwing a clear message when the baseline is absent"
    - "Setting MUONROI_UPDATE_SNAPSHOTS=1 writes/overwrites the baseline in the SOURCE tree (not bin/) and the test passes"
    - "A determinism canary renders each registered corpus case twice and asserts the two byte arrays are identical (SC1)"
    - "The first batch of block-layout golden cases (>=8) renders and matches its committed baselines"
    - ".gitattributes marks *.pdf and *.ttf as binary so committed baselines survive checkout byte-exact"
  artifacts:
    - path: ".gitattributes"
      provides: "Binary attributes preventing CRLF/LF mangling of committed PDF/TTF baselines"
      contains: "*.pdf binary"
    - path: "tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs"
      provides: "Hand-rolled byte-equality golden comparer with opt-in regeneration"
      contains: "MUONROI_UPDATE_SNAPSHOTS"
    - path: "tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs"
      provides: "Central registry of corpus cases (name -> html + options) shared by golden + canary"
      contains: "GoldenCorpus"
    - path: "tests/Muonroi.Pdf.Tests/Golden/DeterminismCanaryTests.cs"
      provides: "SC1 intra-run byte-determinism canary across the corpus"
      contains: "SequenceEqual"
  key_links:
    - from: "GoldenPdf.VerifyAsync"
      to: "PdfServiceTestHarness.BuildProvider"
      via: "render through the real AddPdf container + embedded font resolver"
      pattern: "BuildProvider"
    - from: "DeterminismCanaryTests"
      to: "GoldenCorpus.AllCases"
      via: "MemberData drives every registered case"
      pattern: "MemberData"
---

<objective>
Establish the golden-snapshot infrastructure: a hand-rolled byte-equality comparer (`GoldenPdf`), a shared corpus registry (`GoldenCorpus`), the determinism canary (SC1), the first batch of block-layout golden cases, and `.gitattributes` to keep committed baselines byte-exact. This plan is the foundation every later corpus plan builds on.

Purpose: The engine is already byte-deterministic (DeterminismTests proves it). This plan captures that as a committable, regenerable golden mechanism without adding any new dependency (locked decision 1 — hand-rolled, NOT Verify.Xunit).
Output: `.gitattributes` + harness + corpus registry + first golden batch + canary; `dotnet test` green.
</objective>

<execution_context>
@C:/Users/phila/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/phila/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-golden-snapshots-ci-gates-publishing/07-RESEARCH.md

## Test infrastructure (verified)

- `tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` has `<EmbeddedResource Include="TestResources/**" />` (line 21) — anything under `TestResources/Golden/` is auto-embedded.
- `PdfRenderCollection` (`[CollectionDefinition(Name, DisableParallelization = true)]`, `Name = "PdfRender"`) serializes all renderer tests around PdfSharpCore's process-global FontFactory cache. Every golden/canary test class MUST carry `[Collection(PdfRenderCollection.Name)]`.
- `PdfServiceTestHarness.BuildProvider()` (tests/.../Service/PdfServiceTestHarness.cs) builds a real `AddPdf` container wired with a no-op logger, a fake tenant context, and an `EmbeddedTestFontResolver` (resolves every `@font-face` to the embedded Noto Sans `TestFont.ttf`). Resolve `IMPdfService` from it and call `RenderToBytesAsync(html, options, ct)` → `(byte[] Bytes, PdfRenderResult Metadata)`.
- `IMPdfService.RenderToBytesAsync(string html, PdfRenderOptions options, CancellationToken ct = default)` returns `(byte[] Bytes, PdfRenderResult Metadata)` — use this for golden rendering.
- `PdfRenderOptions` is a `sealed record` with `PageSize` (default A4), `Orientation`, `Margins`, `Header`, `Footer`, `UserStyleSheet`, `TemplateId`. Default ctor is valid.
</context>

<interfaces>
Render path the harness must use (verified from PdfServiceTestHarness + IMPdfService):
```
using var provider = PdfServiceTestHarness.BuildProvider();
var svc = provider.GetRequiredService<IMPdfService>();
(byte[] bytes, _) = await svc.RenderToBytesAsync(html, options, ct);
```

Embedded baseline read pattern (mirror WriterTestFonts):
```
typeof(GoldenPdf).Assembly.GetManifestResourceStream($"Muonroi.Pdf.Tests.TestResources.Golden.{caseName}.pdf")
```
Resource names flatten path separators to dots; a file `TestResources/Golden/block-single.pdf` is the manifest resource `Muonroi.Pdf.Tests.TestResources.Golden.block-single.pdf`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: .gitattributes + corpus dir + GoldenPdf comparer</name>
  <files>
    .gitattributes
    tests/Muonroi.Pdf.Tests/TestResources/Golden/.gitkeep
    tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs
  </files>
  <action>
Create `.gitattributes` at repo root (locked decision 5; RESEARCH Pitfall 2) with lines marking PDF and TTF baselines binary so Git autocrlf cannot mangle committed baselines:
- `*.pdf binary`
- `*.ttf binary`
- `tests/**/TestResources/** -text`

Create empty `tests/Muonroi.Pdf.Tests/TestResources/Golden/.gitkeep` so the baseline directory exists for committers (baselines land here via regen).

Create `GoldenPdf` in namespace `Muonroi.Pdf.Tests.Golden` (internal static class):
- `UpdateMode` property reads env var `MUONROI_UPDATE_SNAPSHOTS` and is true when its value is `"1"` or `"true"` (locked decision 1).
- `public static async Task VerifyAsync(string caseName, string html, PdfRenderOptions options, CancellationToken ct = default)`:
  - Render via the harness path in `<interfaces>` (`PdfServiceTestHarness.BuildProvider()` → `IMPdfService.RenderToBytesAsync`).
  - When `UpdateMode`: resolve the SOURCE-tree path (RESEARCH Pitfall 3 — must be committable, NOT bin/). Use `[CallerFilePath]` of GoldenPdf.cs to derive the `Golden/` directory: `Path.Combine(Path.GetDirectoryName(callerPath)!, "..", "TestResources", "Golden", caseName + ".pdf")`, normalized via `Path.GetFullPath`. `File.WriteAllBytes` the rendered bytes there, then `return` (test passes).
  - When NOT UpdateMode: load the embedded baseline `Muonroi.Pdf.Tests.TestResources.Golden.{caseName}.pdf` via `GetManifestResourceStream`. If null, throw `Xunit.Sdk.XunitException($"No baseline for '{caseName}'. Run with MUONROI_UPDATE_SNAPSHOTS=1 to create it.")`. Assert `actual.SequenceEqual(expected)` via FluentAssertions `.Should().BeTrue(...)` with a message reporting both lengths.
- Add a private helper that reads a manifest stream into a byte[] (mirror WriterTestFonts.LoadTestFontBytes).
Do NOT add any NuGet package.
  </action>
  <verify>
    <automated>dotnet build tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false</automated>
  </verify>
  <done>.gitattributes exists with `*.pdf binary`; GoldenPdf compiles; UpdateMode reads MUONROI_UPDATE_SNAPSHOTS; regen writes to source TestResources/Golden; assert mode does byte SequenceEqual against embedded baseline</done>
</task>

<task type="auto">
  <name>Task 2: GoldenCorpus registry + block-layout first batch (>=8 cases)</name>
  <files>
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
    tests/Muonroi.Pdf.Tests/Golden/BlockLayoutGoldenTests.cs
  </files>
  <action>
Create `GoldenCorpus` (internal static class, namespace `Muonroi.Pdf.Tests.Golden`):
- A `readonly record struct GoldenCase(string Name, string Html, PdfRenderOptions Options)` (or a small class). `Options` defaults to `new PdfRenderOptions()`.
- A static `IReadOnlyList<GoldenCase> AllCases` aggregating per-group lists. In THIS plan register only the block-layout group; later plans (07-02, 07-03) APPEND their groups to this list — structure `AllCases` so each group lives in its own `internal static readonly` field (e.g. `BlockLayout`, and reserve named fields the later plans fill) and `AllCases` is their concatenation. Keep group fields `public`/`internal static` so later plans add entries by extending the file.
- `public static IEnumerable<object[]> AllCasesData()` yields `[case.Name]` for `[MemberData]`; corpus lookup by name is exposed via `public static GoldenCase ByName(string name)`.

Author the BLOCK LAYOUT group with at least 8 cases (RESEARCH corpus table): `block-single`, `block-nested`, `margin-collapse-adjacent`, `margin-collapse-parent-child`, `bfc-root-overflow-hidden`, `box-sizing-padding-border`, plus 2 more (e.g. `block-multi-paragraph`, `block-background-color`). Each is an inline HTML string exercising the declared v0.1 CSS subset only (block/inline; NO flex/grid/float/position). Use the embedded font family in CSS where text is rendered (the EmbeddedTestFontResolver resolves any @font-face; a simple `font-family` referencing it is fine).

Create `BlockLayoutGoldenTests` (`[Collection(PdfRenderCollection.Name)]`, public sealed class):
- `[Theory][MemberData(nameof(GoldenCorpus.AllCasesData), MemberType = typeof(GoldenCorpus))]` filtered to block cases — OR a dedicated `BlockCasesData()` that yields only the block group names. For each name, look up via `GoldenCorpus.ByName(name)` and call `await GoldenPdf.VerifyAsync(c.Name, c.Html, c.Options)`.

After authoring, generate the baselines: the executor MUST run the test once with the env var set so baselines are written to the source tree, then run again WITHOUT it to confirm byte-equality. Use the verify commands below in that order.
  </action>
  <verify>
    <automated>$env:MUONROI_UPDATE_SNAPSHOTS='1'; dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~BlockLayoutGoldenTests"; Remove-Item Env:MUONROI_UPDATE_SNAPSHOTS; dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~BlockLayoutGoldenTests"</automated>
  </verify>
  <done>>=8 block-layout baselines exist under TestResources/Golden/; second run (no env var) passes byte-equality for every case; GoldenCorpus.AllCases includes the block group</done>
</task>

<task type="auto">
  <name>Task 3: Determinism canary across the corpus (SC1)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/DeterminismCanaryTests.cs</files>
  <action>
Create `DeterminismCanaryTests` (`[Collection(PdfRenderCollection.Name)]`, public sealed class) implementing SC1's intra-run determinism canary (RESEARCH "Determinism Canary"):
- `[Theory][MemberData(nameof(GoldenCorpus.AllCasesData), MemberType = typeof(GoldenCorpus))]`.
- Each case: look up via `GoldenCorpus.ByName(name)`, render TWICE through the same harness path GoldenPdf uses (extract the render-to-bytes step into an `internal static` helper on `GoldenPdf`, e.g. `RenderAsync(html, options, ct)`, and reuse it here so canary and baseline share one render path). Assert `a.SequenceEqual(b).Should().BeTrue($"'{name}' must be byte-deterministic within a run (SC1 canary)")`.
This canary is distinct from baseline comparison: it catches intra-run nondeterminism even for cases without a baseline, and automatically covers every case later plans append to `GoldenCorpus.AllCases`.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~DeterminismCanaryTests"</automated>
  </verify>
  <done>Canary renders every registered corpus case twice and asserts byte-identity; passes for the block-layout batch</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed baseline file -> test assembly | A mangled baseline (CRLF) would produce phantom mismatches; .gitattributes locks bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | committed PDF/TTF baselines | mitigate | .gitattributes marks them binary; embedded-resource read-back is cwd/CRLF-immune at runtime |
| T-07-02 | Repudiation | silent determinism regression | mitigate | DeterminismCanaryTests asserts byte-identity per run; baselines lock output drift over time |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs; hand-rolled comparer uses only the existing xunit/FluentAssertions stack |
</threat_model>

<verification>
dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~Golden|FullyQualifiedName~Canary"
</verification>

<success_criteria>
- `.gitattributes` exists marking `*.pdf` / `*.ttf` binary
- `GoldenPdf` byte-comparer with `MUONROI_UPDATE_SNAPSHOTS` regen writing to the source tree
- `GoldenCorpus` registry extensible by later plans; block-layout group has >=8 cases with committed baselines
- DeterminismCanaryTests passes for every registered case (SC1)
- All existing tests plus the new golden/canary tests pass
</success_criteria>

<output>
Create `.planning/phases/07-golden-snapshots-ci-gates-publishing/07-01-SUMMARY.md` when done
</output>
