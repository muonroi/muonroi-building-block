---
phase: 07-golden-snapshots-ci-gates-publishing
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/VietnameseGoldenTests.cs
  - KNOWN-DEVIATIONS.md
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "TestFont.ttf is confirmed to contain Vietnamese glyph coverage before any Vietnamese baseline is captured (no .notdef baselines)"
    - "The corpus includes >=10 Vietnamese golden cases covering diacritic stacking, mixed Latin+Vietnamese, and line-breaking (TEST-02)"
    - "Each Vietnamese case has a committed baseline passing byte-equality on a clean run"
    - "KNOWN-DEVIATIONS.md lists every intentional CSS 2.1 deviation including any introduced in Phases 4-6 (TEST-04)"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Golden/VietnameseGoldenTests.cs"
      provides: "TEST-02 Vietnamese golden coverage + glyph-coverage guard"
      contains: "Vietnamese"
    - path: "KNOWN-DEVIATIONS.md"
      provides: "TEST-03/TEST-04 exhaustive deviation list"
      contains: "KD-"
  key_links:
    - from: "VietnameseGoldenTests"
      to: "GoldenCorpus"
      via: "Vietnamese group appended so the canary covers it"
      pattern: "AllCases"
    - from: "Vietnamese glyph guard"
      to: "TestFont.ttf coverage"
      via: "render asserts non-empty distinct output vs .notdef baseline"
      pattern: "notdef"
---

<objective>
Author the 10+ Vietnamese golden corpus (TEST-02) and finalize `KNOWN-DEVIATIONS.md` (TEST-03/TEST-04). First confirm the embedded font actually covers Vietnamese glyphs (RESEARCH A3 / Pitfall 5) so baselines are not vacuous `.notdef` boxes.

Purpose: TEST-02 requires >=10 Vietnamese goldens proving diacritic rendering is stable; the existing `VietnamesePrecomposed` test already passes, confirming the embedded Noto Sans has coverage. TEST-04 requires KNOWN-DEVIATIONS.md to list every intentional CSS 2.1 deviation; it exists with Phase-3 entries (KD-03-*) and needs any Phase 4-6 additions.
Output: Vietnamese group + guard in GoldenCorpus; committed baselines; updated KNOWN-DEVIATIONS.md; `dotnet test` green.
</objective>

<execution_context>
@C:/Users/phila/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/phila/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-golden-snapshots-ci-gates-publishing/07-RESEARCH.md
@KNOWN-DEVIATIONS.md

## Built by 07-01 (verified contract)

- `GoldenPdf.VerifyAsync(name, html, options, ct)` + `GoldenPdf.RenderAsync(html, options, ct)`; regen via `MUONROI_UPDATE_SNAPSHOTS=1`.
- `GoldenCorpus` — append a `Vietnamese` group field and add it to `AllCases`; `ByName`, `AllCasesData`.
- Render path resolves @font-face to the embedded Noto Sans `TestFont.ttf` (HAS Vietnamese coverage — confirmed by the passing `VietnamesePrecomposed` writer test). 07-03 depends only on 07-01 and may run in parallel with 07-02 (no shared test files except GoldenCorpus.cs — see note).

## File-ownership note

07-02 and 07-03 both append to `GoldenCorpus.cs`. To keep them parallel-safe the executor MUST add ONLY a new `Vietnamese` group field + its entry in the `AllCases` concatenation, never reorder or edit other groups. If a merge conflict arises with 07-02, re-apply only the Vietnamese additions. (Both are wave 2; if the orchestrator serializes them on this shared file, that is acceptable.)
</context>

<interfaces>
Vietnamese sample strings (precomposed + combining) for the cases:
"Tiếng Việt", "ế ộ ữ", "Trang 1 / 3", "Ầ Ữ", plus a long paragraph mixing Latin + Vietnamese to force wrapping/page-break. Use a `font-family` the EmbeddedTestFontResolver serves.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Vietnamese glyph coverage (guard)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/VietnameseGoldenTests.cs</files>
  <action>
Before authoring baselines, add a guard test `VietnameseFont_HasGlyphCoverage` (`[Collection(PdfRenderCollection.Name)]`) proving the embedded font renders Vietnamese as real glyphs, not `.notdef` (RESEARCH A3 / Pitfall 5). Render two short documents via `GoldenPdf.RenderAsync`: one all-ASCII ("Tieng Viet") and one with diacritics ("Tiếng Việt"). Assert the two byte arrays DIFFER (`a.SequenceEqual(b).Should().BeFalse(...)`) — identical output would imply both collapsed to the same fallback/.notdef shaping, signalling missing coverage. If they are equal, the test FAILS and the executor STOPS, recording a BLOCKING finding in the SUMMARY ("TestFont.ttf lacks Vietnamese coverage — TEST-02 baselines would be vacuous; source a covering font") rather than capturing vacuous baselines.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~VietnameseFont_HasGlyphCoverage"</automated>
  </verify>
  <done>Glyph-coverage guard passes (diacritic vs ASCII output differs), proving non-vacuous baselines — OR a BLOCKING finding is recorded</done>
</task>

<task type="auto">
  <name>Task 2: Vietnamese golden group (>=10 cases, TEST-02)</name>
  <files>
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
    tests/Muonroi.Pdf.Tests/Golden/VietnameseGoldenTests.cs
  </files>
  <action>
Append a `Vietnamese` group to `GoldenCorpus` (and to `AllCases`) with >=10 cases (RESEARCH TEST-02 table): `vn-diacritic-word`, `vn-stacked-tone-vowel`, `vn-mixed-latin-vn`, `vn-line-wrap`, `vn-table-cell`, `vn-page-header-counter`, `vn-uppercase-diacritics`, `vn-long-paragraph-pagebreak`, `vn-digits-trang-x-of-y`, `vn-bold-italic-runs`, plus 2 margin cases `vn-counter-footer`, `vn-multi-page-flow` (12 total for headroom).

Add the `[Theory]` to `VietnameseGoldenTests` driving the Vietnamese group via `GoldenPdf.VerifyAsync`. Generate baselines (regen), then verify clean.
  </action>
  <verify>
    <automated>$env:MUONROI_UPDATE_SNAPSHOTS='1'; dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~VietnameseGoldenTests"; Remove-Item Env:MUONROI_UPDATE_SNAPSHOTS; dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~VietnameseGoldenTests"</automated>
  </verify>
  <done>>=10 Vietnamese baselines committed; clean run passes byte-equality; Vietnamese group in AllCases (canary covers it)</done>
</task>

<task type="auto">
  <name>Task 3: Finalize KNOWN-DEVIATIONS.md (TEST-03/TEST-04)</name>
  <files>KNOWN-DEVIATIONS.md</files>
  <action>
Read the existing `KNOWN-DEVIATIONS.md` (Phase-3 entries KD-03-*). Append any intentional CSS 2.1 deviations introduced or confirmed in Phases 4-6 (font subsetting limits, image decoding constraints, writer/security exclusions such as no /JavaScript, no embedded files, no external file:// resources) using the existing KD-NN-NN id convention and table/section format already in the file — mirror it exactly, do not restructure. Add a short note framing TEST-03 per RESEARCH Open Question 2: the corpus exercises every non-rejected property in the declared subset; deviations are exhaustively listed here (not a numeric coverage metric). Ensure every deviation a reader could hit in the v0.1 declared subset is listed (TEST-04 "every intentional deviation").
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path KNOWN-DEVIATIONS.md -Pattern 'KD-' | Measure-Object).Count -ge 5) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>KNOWN-DEVIATIONS.md lists Phase 3-6 deviations with KD- ids in the existing format; TEST-03 framing note present (TEST-04 satisfied)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded font glyph table -> shaping pipeline | Missing Vietnamese glyphs would silently render .notdef, making TEST-02 vacuous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Repudiation | vacuous Vietnamese baselines (.notdef) | mitigate | Glyph-coverage guard asserts diacritic output differs from ASCII before baselines are captured |
| T-07-06 | Repudiation | undocumented CSS deviation | mitigate | KNOWN-DEVIATIONS.md enumerates every intentional deviation (TEST-04) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses the existing embedded font |
</threat_model>

<verification>
dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false --filter "FullyQualifiedName~VietnameseGoldenTests|FullyQualifiedName~Canary"
</verification>

<success_criteria>
- Embedded-font Vietnamese coverage confirmed by a guard test (no vacuous baselines)
- >=10 Vietnamese goldens with committed baselines passing byte-equality (TEST-02)
- Determinism canary covers the Vietnamese group
- KNOWN-DEVIATIONS.md lists every intentional CSS 2.1 deviation (TEST-03/TEST-04)
</success_criteria>

<output>
Create `.planning/phases/07-golden-snapshots-ci-gates-publishing/07-03-SUMMARY.md` when done
</output>
