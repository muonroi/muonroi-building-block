---
phase: 12-owned-css-cascade-b1
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - tests/Muonroi.Pdf.Tests/TestResources/Golden/   # %-table .pdf baselines re-generated (only the changed %-table cases)
# autonomous false: contains a blocking human-verify checkpoint for visual re-baseline approval
autonomous: false

must_haves:
  truths:
    - "The full Muonroi.Pdf.Tests suite is green after wiring the owned cascade."
    - "Simple-doc goldens stay byte-identical (structural-normalized) — no unintended baseline drift."
    - "Only %-table goldens are re-baselined, and only after human visual verification."
    - "The determinism canary is unaffected."
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/TestResources/Golden/"
      provides: "Re-baselined %-table PDF snapshots (changed cases only), visually verified"
      contains: ".pdf"
  key_links:
    - from: "owned cascade (Plan 03)"
      to: "golden snapshot suite"
      via: "full render path through AddPdf container"
      pattern: "GoldenPdf.VerifyAsync"
---

<objective>
Final verification wave: run the full `Muonroi.Pdf.Tests` suite against the owned cascade, confirm
simple-doc goldens are byte-identical (structural-normalized), identify the `%`-table cases whose
output legitimately changed (they now render *better* — the degraded em/rem/% path is gone), visually
verify each, and re-baseline ONLY those. Confirm the determinism canary is unaffected.

Purpose: Phase 12 success criterion 4 — full suite green, only `%`-table goldens re-baselined (visually
verified), determinism canary unaffected. This is the gate that proves the cascade swap did not
silently regress the 17-template corpus.

Output: re-generated `%`-table `.pdf` baselines (changed cases only), and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DESIGN-owned-cascade-B1.md
@.planning/phases/12-owned-css-cascade-b1/12-CONTEXT.md

@tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs
@tests/Muonroi.Pdf.Tests/Golden/TableGoldenTests.cs

<interfaces>
<!-- Golden mechanism (from GoldenPdf.cs, already read): -->
<!-- - Comparison is STRUCTURAL-normalized (decompress FlateDecode, drop xref/startxref). -->
<!-- - Regeneration is opt-in: env MUONROI_UPDATE_SNAPSHOTS=1 writes new .pdf into the SOURCE tree -->
<!--   (tests/Muonroi.Pdf.Tests/TestResources/Golden/) and the test passes. -->
<!-- - With the env UNSET, a changed render FAILS the test → that failure list IS the set of cases -->
<!--   whose output changed. Use that list to scope the re-baseline. -->
<!-- - A determinism canary test exercises the same render path; it must remain green WITHOUT re-baseline. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite; capture the exact set of changed golden cases</name>
  <files>tests/Muonroi.Pdf.Tests/  (read-only run; no source edits)</files>
  <read_first>
    tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs (UpdateMode + structural comparison — already in context)
    tests/Muonroi.Pdf.Tests/Golden/TableGoldenTests.cs (which cases are %-width tables)
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs (the corpus case list + their HTML — find the %-width / table-bodered cases)
  </read_first>
  <action>
    With `MUONROI_UPDATE_SNAPSHOTS` UNSET, run the full test suite:
    `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj`.
    Collect the failing test list. Classify each failure:
    (a) Golden snapshot mismatch on a `%`-table / `table-bodered` / percentage-width case → EXPECTED
        change (candidate for re-baseline).
    (b) Golden snapshot mismatch on a simple-doc / non-%-table case → UNEXPECTED regression — STOP and
        investigate (the cascade must match byte-for-byte here; do not re-baseline, fix the cascade).
    (c) Non-golden unit/integration failure → a real cascade bug — STOP and fix (likely in Plan 02/03
        scope; if so, the bug indicates an incomplete cascade, not a baseline issue).
    Produce a written list of the (a) cases (case names) — this is the re-baseline scope handed to the
    checkpoint. Confirm the determinism canary test is NOT in the failing list (it must already be green).
    Do not regenerate anything in this task.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj --filter "FullyQualifiedName~Determinism"</automated>
  </verify>
  <acceptance_criteria>
    - behavior: the determinism canary test passes WITHOUT any re-baseline.
    - source: a written classified list of changed golden cases exists in the run notes, split into (a) %-table-expected vs (b) simple-doc-unexpected.
    - behavior: zero category (b) simple-doc golden mismatches and zero category (c) non-golden failures remain (if any exist, they are fixed before proceeding — they indicate a cascade defect, not a baseline change).
    - test-command: `dotnet test ... --filter Determinism` passes, 0 failed.
  </acceptance_criteria>
  <done>Full suite run complete; changed-golden cases classified; only %-table cases changed; determinism canary green; no simple-doc drift, no unit failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of %-table re-baseline candidates</name>
  <files>tests/Muonroi.Pdf.Tests/TestResources/Golden/</files>
  <action>PAUSE for human visual verification of the changed %-table render output before any baseline is written. See &lt;how-to-verify&gt; for the exact steps; do not re-baseline until "approved".</action>
  <what-built>
    The owned cascade is wired and the full suite runs. Task 1 produced a list of `%`-table golden
    cases whose rendered PDF changed (the previously-degraded em/rem/% path is replaced by the owned
    cascade, so these should now render MORE correctly). Before re-baselining, these must be visually
    confirmed to be improvements, not regressions.
  </what-built>
  <how-to-verify>
    1. For each changed `%`-table case from Task 1's list, render the current output to a PDF you can open
       (run the specific golden test with `MUONROI_UPDATE_SNAPSHOTS=1` to write the candidate .pdf into
       `tests/Muonroi.Pdf.Tests/TestResources/Golden/`, OR render the case HTML through the harness to a
       temp .pdf first for inspection).
    2. Open each candidate PDF and compare against the Chrome reference for the same template (the
       %-table corpus is the TCIS HBCX family — see PROFILE-V1.md §9 corpus + .planning AUDIT references).
    3. Confirm column widths, cell padding, and borders on the percentage-width tables look correct
       (equal-or-better than the old baseline), and that no text overlaps / overflows.
    4. Confirm NO simple-doc (non-%-table) golden was regenerated (those must stay byte-identical).
    5. Reply "approved" to accept the re-baseline of exactly the listed %-table cases, or describe the
       visual issues found (which would mean a cascade bug to fix in Plan 02/03, not a re-baseline).
  </how-to-verify>
  <resume-signal>Type "approved" to re-baseline the listed %-table cases, or describe the visual regressions found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Re-baseline approved %-table goldens; confirm full suite green</name>
  <files>tests/Muonroi.Pdf.Tests/TestResources/Golden/</files>
  <read_first>
    tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs (UpdateMode regeneration writes to TestResources/Golden in the SOURCE tree)
  </read_first>
  <action>
    After checkpoint approval, regenerate ONLY the approved `%`-table baselines. Prefer a targeted run
    that touches only those cases — run the specific golden tests for the approved case names with
    `MUONROI_UPDATE_SNAPSHOTS=1` so only those `.pdf` files in
    `tests/Muonroi.Pdf.Tests/TestResources/Golden/` are rewritten. Do NOT run the whole suite with the
    env set (that would silently rewrite every baseline incl. simple-docs — forbidden).
    Then run the full suite with the env UNSET to confirm everything is green:
    `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj`.
    Verify via `git status` that only `%`-table `.pdf` files changed under TestResources/Golden (no
    simple-doc baseline touched). If any simple-doc baseline shows as modified, revert it
    (`git checkout -- &lt;file&gt;`) and investigate — it must not have changed.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` — full suite green, 0 failed (Pre-Push Test Gate).
    - source: `git status` shows changed `.pdf` files ONLY for the approved %-table cases under TestResources/Golden; zero simple-doc baselines modified.
    - behavior: determinism canary still green within the full run.
  </acceptance_criteria>
  <done>Approved %-table goldens re-baselined; full suite green; only %-table baselines changed; simple-doc goldens byte-identical; determinism canary unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render output → committed golden baseline | Re-baselining commits rendered bytes; an over-broad regeneration could mask a real regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | over-broad MUONROI_UPDATE_SNAPSHOTS=1 silently rewriting all baselines | mitigate | Regeneration scoped to the approved %-table case names only; whole-suite regeneration is explicitly forbidden; git status diff is checked to prove only %-table .pdf changed. |
| T-12-09 | Repudiation | re-baselining a regression as if it were an improvement | mitigate | Blocking human-verify checkpoint requires visual Chrome-parity confirmation per case before any baseline is written. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` — full suite green, 0 failed.
- `git status` shows ONLY approved %-table `.pdf` baselines modified under TestResources/Golden.
- Determinism canary green without re-baseline.
- No simple-doc golden modified.
</verification>

<success_criteria>
- Full `Muonroi.Pdf.Tests` suite green against the owned cascade.
- Only `%`-table goldens re-baselined, each visually verified at the checkpoint.
- Simple-doc goldens byte-identical (structural-normalized); determinism canary unaffected.
</success_criteria>

<output>
Create `.planning/phases/12-owned-css-cascade-b1/12-04-SUMMARY.md` when done.
</output>
