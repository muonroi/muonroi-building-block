---
phase: 17-monetization-rail-quota-billing-subscription
plan: 01
type: execute
wave: 1
repo: muonroi-building-block
depends_on: []
files_modified:
  - src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj
  - src/Muonroi.Billing.Abstractions/GlobalUsings.cs
  - src/Muonroi.Billing.Abstractions/IBillingProvider.cs
  - src/Muonroi.Billing.Abstractions/BillableEvent.cs
  - src/Muonroi.Billing.Abstractions/UsageLineItem.cs
  - src/Muonroi.Billing.Abstractions/PricingPlan.cs
  - src/Muonroi.Billing.Abstractions/IUsageAggregator.cs
  - src/Muonroi.Billing.Abstractions/RecordOnlyBillingProvider.cs
  - src/Muonroi.Billing.Abstractions/BillingServiceCollectionExtensions.cs
  - tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj
  - tests/Muonroi.Billing.Abstractions.Tests/GlobalUsings.cs
  - tests/Muonroi.Billing.Abstractions.Tests/RecordOnlyBillingProviderTests.cs
  - Muonroi.BuildingBlock.sln
  - OSS-BOUNDARY.md
autonomous: true
requirements: [MON-01, MON-02]

must_haves:
  truths:
    - "A product-agnostic billing seam exists in code (IBillingProvider, IUsageAggregator, UsageLineItem, PricingPlan) with zero payment-SDK dependency"
    - "A record-only default IBillingProvider records billable events without calling any external service"
    - "Provider failures are logged with module/operation/context and never swallowed silently (No Silent Catch)"
  artifacts:
    - path: "src/Muonroi.Billing.Abstractions/IBillingProvider.cs"
      provides: "IBillingProvider contract keyed by metered dimension (QuotaType), not 'pdf'"
      contains: "interface IBillingProvider"
    - path: "src/Muonroi.Billing.Abstractions/UsageLineItem.cs"
      provides: "UsageLineItem + PricingPlan priced-rollup model (MON-01)"
      contains: "UsageLineItem"
    - path: "src/Muonroi.Billing.Abstractions/IUsageAggregator.cs"
      provides: "IUsageAggregator contract (impl lands in 17-02)"
      contains: "interface IUsageAggregator"
    - path: "src/Muonroi.Billing.Abstractions/RecordOnlyBillingProvider.cs"
      provides: "record-only default provider (MON-02)"
      contains: "RecordOnlyBillingProvider"
  key_links:
    - from: "src/Muonroi.Billing.Abstractions/RecordOnlyBillingProvider.cs"
      to: "IMLog logging facade"
      via: "catch (Exception ex) { logger.Error(...) }"
      pattern: "catch.*\\n.*Error"
---

<objective>
Create the product-agnostic billing seam as a new OSS package `Muonroi.Billing.Abstractions`: the contracts `IBillingProvider`, `BillableEvent`, `UsageLineItem`, `PricingPlan`, `IUsageAggregator`, plus a record-only default `IBillingProvider` implementation. No payment SDK is referenced (D-02). The Stripe adapter is deferred and must NOT be a build/test dependency.

Purpose: Establish the seam that every product line (PDF first, rule-engine/storyflow later) bills through — keyed by metered dimension (`QuotaType`), never by "pdf" (D-02, specifics).
Output: New `Muonroi.Billing.Abstractions` project + its test project, wired into the .sln and OSS-BOUNDARY.md allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block. Commit with `git -C D:/sources/Core/muonroi-building-block`. SUMMARY.md goes in this phase dir and is committed in building-block.
</execution_context>

<context>
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing seam this package mirrors. No codebase exploration needed. -->

From src/Muonroi.Quota.Abstractions/QuotaType.cs — the metered dimension key (DO NOT change this enum here):
  public enum QuotaType { ... PdfRendersPerDay }

From src/Muonroi.Quota.Abstractions/ITenantQuotaStore.cs — what 17-02's aggregator will read:
  Task<QuotaUsage> GetUsageAsync(string tenantId, CancellationToken ct = default);
  // QuotaUsage.CurrentUsage : Dictionary<QuotaType,int>; PeriodStart/PeriodEnd : DateTime

From src/Muonroi.Quota.Abstractions/TenantQuotaServiceCollectionExtensions.cs — DI registration shape to mirror (TryAdd):
  services.TryAddSingleton<ITenantQuotaStore, InMemoryTenantQuotaStore>();

From src/Muonroi.Pdf.Enterprise/Metering/EnterprisePdfServiceWrapper.cs — record-only + No Silent Catch pattern to mirror:
  catch (Exception ex) { logger?.Error(ex, "[PDF] Metering record failed (non-blocking): {Message}", ex.Message); }

From src/Muonroi.Quota.Abstractions/GlobalUsings.cs — the GlobalUsings pattern (global using Microsoft.Extensions.DependencyInjection / .Extensions; Muonroi.Core.Abstractions.Interfaces).
The IMLog<T> facade lives in Muonroi.Logging.Abstractions (see EnterprisePdfServiceWrapper using Muonroi.Logging.Abstractions).
</interfaces>

@src/Muonroi.Quota.Abstractions/Muonroi.Quota.Abstractions.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Muonroi.Billing.Abstractions project + contracts (MON-01)</name>
  <files>src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj, src/Muonroi.Billing.Abstractions/GlobalUsings.cs, src/Muonroi.Billing.Abstractions/IBillingProvider.cs, src/Muonroi.Billing.Abstractions/BillableEvent.cs, src/Muonroi.Billing.Abstractions/UsageLineItem.cs, src/Muonroi.Billing.Abstractions/PricingPlan.cs, src/Muonroi.Billing.Abstractions/IUsageAggregator.cs, Muonroi.BuildingBlock.sln, OSS-BOUNDARY.md</files>
  <read_first>
    - src/Muonroi.Quota.Abstractions/Muonroi.Quota.Abstractions.csproj (csproj template: net8.0, GeneratePackageOnBuild, GenerateDocumentationFile, PackageId, ProjectReference to Muonroi.Core.Abstractions)
    - src/Muonroi.Quota.Abstractions/GlobalUsings.cs (GlobalUsings pattern)
    - src/Muonroi.Quota.Abstractions/QuotaType.cs (the QuotaType key dimension)
    - src/Muonroi.Quota.Abstractions/QuotaUsage.cs (shape the aggregator reads)
    - OSS-BOUNDARY.md (allowlist format — "## OSS Packages" list)
  </read_first>
  <action>
Create `src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj` targeting `net8.0`, copying the `Muonroi.Quota.Abstractions.csproj` shape (GeneratePackageOnBuild=true, GenerateDocumentationFile=true, PackageId=Muonroi.Billing.Abstractions, AssemblyVersion/FileVersion 1.0.0.0). Add a `ProjectReference` to `..\Muonroi.Quota.Abstractions\Muonroi.Quota.Abstractions.csproj` (needs `QuotaType` + `ITenantQuotaStore` for the aggregator contract). Do NOT add ANY payment-SDK package reference (no Stripe.net, no Stripe). Add a `ProjectReference` to `..\Muonroi.Logging.Abstractions\Muonroi.Logging.Abstractions.csproj` (for `IMLog<T>` used by the record-only provider in Task 2).
Add `GlobalUsings.cs` mirroring Quota.Abstractions (global using Microsoft.Extensions.DependencyInjection; Microsoft.Extensions.DependencyInjection.Extensions; System.Threading; System.Threading.Tasks; System.Collections.Generic).

Define contracts in namespace `Muonroi.Billing.Abstractions`:
- `BillableEvent` (record/sealed class): `string TenantId`, `QuotaType Dimension`, `long Quantity`, `DateTimeOffset OccurredAt`. Keyed on `QuotaType` — product-agnostic per D-02.
- `UsageLineItem` (record/sealed class): `QuotaType Dimension`, `long Quantity`, `decimal UnitRate`, `decimal Amount` (Amount = Quantity * UnitRate), `string? Description`.
- `PricingPlan` (sealed class): `TenantTier Tier` (from Muonroi.Quota.Abstractions), `decimal FlatBaseAmount` (optional, default 0), and `IReadOnlyDictionary<QuotaType, decimal> UnitRates` (per-dimension per-unit rate). Add a method `decimal GetUnitRate(QuotaType dimension)` returning the configured rate or 0m when absent. D-05: simple per-unit × tier rate + optional flat base; NO proration/tax/multi-currency.
- `IUsageAggregator` (interface): `Task<IReadOnlyList<UsageLineItem>> AggregateAsync(string tenantId, PricingPlan plan, DateTime periodStart, DateTime periodEnd, CancellationToken ct = default)`. Impl lands in 17-02.
- `IBillingProvider` (interface): `Task RecordAsync(BillableEvent billableEvent, CancellationToken ct = default)` and `Task<IReadOnlyList<UsageLineItem>> PreviewInvoiceAsync(string tenantId, IReadOnlyList<UsageLineItem> lineItems, CancellationToken ct = default)`. PreviewInvoice is compute-only (D-03) — never charges. XML-doc every public member explicitly stating "record-only / no external call in the default impl; the payment-processor adapter is a deferred, separate implementation behind this seam."

Add the project to `Muonroi.BuildingBlock.sln` (use `dotnet sln D:/sources/Core/muonroi-building-block/Muonroi.BuildingBlock.sln add src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj`).
Add `Muonroi.Billing.Abstractions` to the `## OSS Packages (Apache 2.0 - public NuGet)` list in OSS-BOUNDARY.md (it has no payment SDK and rides the shared rails exactly like `Muonroi.Quota.Abstractions`; it must NOT appear in the Commercial list).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet build src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Project builds clean on net8.0.
    - `dotnet list src/Muonroi.Billing.Abstractions/Muonroi.Billing.Abstractions.csproj package` shows NO Stripe / payment-SDK package.
    - IBillingProvider, IUsageAggregator, UsageLineItem, PricingPlan, BillableEvent all exist as public types in namespace Muonroi.Billing.Abstractions.
    - OSS-BOUNDARY.md lists Muonroi.Billing.Abstractions under OSS Packages (not Commercial).
  </acceptance_criteria>
  <done>The seam contracts exist, build clean, depend on no payment SDK, and are in the .sln + OSS allowlist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record-only default IBillingProvider + DI + tests (MON-02)</name>
  <files>src/Muonroi.Billing.Abstractions/RecordOnlyBillingProvider.cs, src/Muonroi.Billing.Abstractions/BillingServiceCollectionExtensions.cs, tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj, tests/Muonroi.Billing.Abstractions.Tests/GlobalUsings.cs, tests/Muonroi.Billing.Abstractions.Tests/RecordOnlyBillingProviderTests.cs, Muonroi.BuildingBlock.sln</files>
  <read_first>
    - src/Muonroi.Pdf.Enterprise/Metering/EnterprisePdfServiceWrapper.cs (record-only + No Silent Catch pattern, IMLog usage)
    - src/Muonroi.Quota.Abstractions/TenantQuotaServiceCollectionExtensions.cs (TryAdd DI registration shape)
    - tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj (test csproj reference shape: xunit, FluentAssertions if present, ProjectReference)
    - tests/Muonroi.Pdf.Tests/Enterprise/FeatureGateTests.cs (xunit test style in this repo)
  </read_first>
  <behavior>
    - Test 1: RecordAsync persists the BillableEvent into an in-memory/recorded list and makes zero external/network calls (assert a recorded-events accessor count increments).
    - Test 2: When the recording sink throws, RecordAsync catches, logs via IMLog.Error with the exception, and does NOT rethrow (call returns normally). Use a throwing fake IMLog/sink to assert Error was invoked and no exception propagates.
    - Test 3: PreviewInvoiceAsync returns the supplied line items unchanged (compute-only, no charge side effect, no external call).
  </behavior>
  <action>
Create `RecordOnlyBillingProvider : IBillingProvider` in namespace `Muonroi.Billing.Abstractions`. Constructor takes `IMLog<RecordOnlyBillingProvider>? logger = null` (mirror EnterprisePdfServiceWrapper optional-logger pattern). Maintain a recorded-events store (a thread-safe in-memory `ConcurrentBag<BillableEvent>` or `IReadOnlyList` accessor) so tests and callers can observe what was recorded. `RecordAsync` appends the event; wrap any failure in `try/catch (Exception ex) { logger?.Error(ex, "[Billing] Record failed (non-blocking): {Message}", ex.Message); }` — never rethrow (D-02 No Silent Catch, record-only). `PreviewInvoiceAsync` returns the supplied `lineItems` verbatim (compute-only, D-03) — it performs NO external call and NO charge.
Create `BillingServiceCollectionExtensions.AddRecordOnlyBilling(this IServiceCollection services)` registering `services.TryAddSingleton<IBillingProvider, RecordOnlyBillingProvider>()` (TryAdd so a future Stripe adapter can override).
Create the test project `tests/Muonroi.Billing.Abstractions.Tests` (net8.0, IsTestProject, xunit + xunit.runner.visualstudio + Microsoft.NET.Test.Sdk; FluentAssertions if used elsewhere in repo), with ProjectReference to `..\..\src\Muonroi.Billing.Abstractions\Muonroi.Billing.Abstractions.csproj`. Add it to the .sln. Implement the three tests in `<behavior>` (RED first: write tests, confirm they fail, then implement to GREEN).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - All three behavior tests pass.
    - RecordOnlyBillingProvider contains a `catch (Exception` followed by a `logger?.Error(` call (No Silent Catch) — verify by reading the file.
    - No test or src reference resolves any Stripe/payment SDK assembly.
  </acceptance_criteria>
  <done>Record-only provider records events, swallows-after-logging on failure, previews compute-only, with green tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| billing seam → external processor | Default impl crosses NO boundary (record-only). The deferred Stripe adapter would cross here later. |
| metering source → billing record | BillableEvent carries TenantId + dimension; must not allow cross-tenant aggregation downstream (enforced in 17-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service | RecordOnlyBillingProvider.RecordAsync | mitigate | catch+log+swallow on sink failure so billing record never DoSes the caller (mirrors EnterprisePdfServiceWrapper T-16-05) |
| T-17-02 | Tampering | new Muonroi.Billing.Abstractions package | mitigate | OSS package, no payment SDK; PreviewInvoice is compute-only and cannot execute a charge |
| T-17-SC | Tampering | new csproj/test csproj — NO package-manager installs | accept | No new NuGet packages introduced beyond existing repo-pinned test deps (CPM); RESEARCH Package Legitimacy Gate not triggered (zero new external packages) |
</threat_model>

<verification>
- `dotnet build` of the new project + `dotnet test` of the new test project both green.
- Grep confirms no `Stripe` token anywhere under `src/Muonroi.Billing.Abstractions` or its tests.
- OSS-BOUNDARY.md: Muonroi.Billing.Abstractions under OSS list only.
</verification>

<success_criteria>
- MON-01: IBillingProvider, UsageLineItem, IUsageAggregator, PricingPlan defined with no payment-SDK dependency.
- MON-02: Record-only default IBillingProvider records events, logs-with-context on failure, never swallows silently.
</success_criteria>

<output>
Create `.planning/phases/17-monetization-rail-quota-billing-subscription/17-01-SUMMARY.md` when done (committed in building-block).
</output>
