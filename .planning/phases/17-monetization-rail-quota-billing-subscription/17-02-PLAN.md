---
phase: 17-monetization-rail-quota-billing-subscription
plan: 02
type: execute
wave: 2
repo: muonroi-building-block
depends_on: ["17-01"]
files_modified:
  - src/Muonroi.Billing.Abstractions/UsageAggregator.cs
  - src/Muonroi.Billing.Abstractions/BillingServiceCollectionExtensions.cs
  - src/Muonroi.Quota.Abstractions/TenantQuota.cs
  - tests/Muonroi.Billing.Abstractions.Tests/UsageAggregatorTests.cs
  - tests/Muonroi.Billing.Abstractions.Tests/TierQuotaLimitTests.cs
autonomous: true
requirements: [MON-03, MON-04]

must_haves:
  truths:
    - "IUsageAggregator rolls a tenant's metered usage (from ITenantQuotaStore) into priced UsageLineItems for a billing period via a PricingPlan"
    - "Pricing is deterministic: amount = Σ(quantity per dimension × tier unit-rate) + optional flat base"
    - "MaxPdfRendersPerDay is sourced from the licensed tier (finite per non-Enterprise tier) rather than always int.MaxValue"
  artifacts:
    - path: "src/Muonroi.Billing.Abstractions/UsageAggregator.cs"
      provides: "IUsageAggregator implementation reading ITenantQuotaStore.GetUsageAsync (MON-03)"
      contains: "class UsageAggregator"
    - path: "src/Muonroi.Quota.Abstractions/TenantQuota.cs"
      provides: "tier-sourced MaxPdfRendersPerDay presets (MON-04)"
      contains: "MaxPdfRendersPerDay"
  key_links:
    - from: "src/Muonroi.Billing.Abstractions/UsageAggregator.cs"
      to: "ITenantQuotaStore.GetUsageAsync"
      via: "constructor-injected store read"
      pattern: "GetUsageAsync"
    - from: "src/Muonroi.Billing.Abstractions/UsageAggregator.cs"
      to: "PricingPlan.GetUnitRate"
      via: "per-dimension rate lookup"
      pattern: "GetUnitRate"
---

<objective>
Implement `IUsageAggregator` (`UsageAggregator`) that reads a tenant's metered usage from `ITenantQuotaStore.GetUsageAsync` and prices it into `UsageLineItem`s for a period via a `PricingPlan` (D-03/D-05). Replace the always-`int.MaxValue` `MaxPdfRendersPerDay` in the tier presets with tier-sourced finite limits for non-Enterprise tiers (D-04/MON-04), so the control-plane can source a real per-tier cap.

Purpose: Turn record-only metering into priced, deterministic line items and a real per-tier limit — the data the invoice-preview endpoint (17-03) and the registered 429 enforcement (17-03) consume.
Output: UsageAggregator + DI registration; updated TenantQuotaPresets; unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block. Commit with `git -C D:/sources/Core/muonroi-building-block`.
</execution_context>

<context>
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-CONTEXT.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-01-SUMMARY.md

<interfaces>
From src/Muonroi.Quota.Abstractions/ITenantQuotaStore.cs:
  Task<QuotaUsage> GetUsageAsync(string tenantId, CancellationToken ct = default);

From src/Muonroi.Quota.Abstractions/QuotaUsage.cs:
  Dictionary<QuotaType,int> CurrentUsage; Dictionary<QuotaType,int> Limits;
  DateTime PeriodStart; DateTime PeriodEnd;

From 17-01 (Muonroi.Billing.Abstractions):
  interface IUsageAggregator { Task<IReadOnlyList<UsageLineItem>> AggregateAsync(string tenantId, PricingPlan plan, DateTime periodStart, DateTime periodEnd, CancellationToken ct = default); }
  sealed class PricingPlan { TenantTier Tier; decimal FlatBaseAmount; IReadOnlyDictionary<QuotaType,decimal> UnitRates; decimal GetUnitRate(QuotaType dimension); }
  record UsageLineItem(QuotaType Dimension, long Quantity, decimal UnitRate, decimal Amount, string? Description);

From src/Muonroi.Quota.Abstractions/TenantQuota.cs — presets to edit (all four currently set MaxPdfRendersPerDay = int.MaxValue):
  TenantQuotaPresets.Free / .Starter / .Professional / .Enterprise
</interfaces>

@src/Muonroi.Quota.Abstractions/TenantQuota.cs
@src/Muonroi.Quota.Abstractions/InMemoryTenantQuotaStore.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UsageAggregator implementation + DI + tests (MON-03)</name>
  <files>src/Muonroi.Billing.Abstractions/UsageAggregator.cs, src/Muonroi.Billing.Abstractions/BillingServiceCollectionExtensions.cs, tests/Muonroi.Billing.Abstractions.Tests/UsageAggregatorTests.cs</files>
  <read_first>
    - src/Muonroi.Billing.Abstractions/IUsageAggregator.cs (contract from 17-01)
    - src/Muonroi.Billing.Abstractions/PricingPlan.cs (GetUnitRate, FlatBaseAmount)
    - src/Muonroi.Billing.Abstractions/UsageLineItem.cs (shape)
    - src/Muonroi.Quota.Abstractions/ITenantQuotaStore.cs + QuotaUsage.cs (GetUsageAsync, CurrentUsage)
    - src/Muonroi.Billing.Abstractions/BillingServiceCollectionExtensions.cs (existing AddRecordOnlyBilling from 17-01)
  </read_first>
  <behavior>
    - Test 1: Given a stub ITenantQuotaStore whose GetUsageAsync returns CurrentUsage { PdfRendersPerDay = 100, ApiRequestsPerMinute = 50 } and a PricingPlan with UnitRates { PdfRendersPerDay = 0.02m } and FlatBaseAmount = 5m, AggregateAsync returns a line item for PdfRendersPerDay with Quantity=100, UnitRate=0.02, Amount=2.00, plus a flat-base line item of 5.00; total Amount across line items = 7.00. Dimensions with a 0 rate produce either no line item or a 0-amount line item (assert the documented choice deterministically).
    - Test 2: Determinism — calling AggregateAsync twice with the same inputs returns identical line items (same order, same amounts).
    - Test 3: Empty usage (no CurrentUsage entries) returns only the flat-base line item (or empty list when FlatBaseAmount=0).
  </behavior>
  <action>
Create `UsageAggregator : IUsageAggregator` in namespace `Muonroi.Billing.Abstractions`. Constructor injects `ITenantQuotaStore quotaStore`. `AggregateAsync` calls `quotaStore.GetUsageAsync(tenantId, ct)`, iterates `CurrentUsage` entries, and for each dimension computes `unitRate = plan.GetUnitRate(dimension)`, `amount = quantity * unitRate` (D-05 per-unit × tier rate). Emit `UsageLineItem`s ordered deterministically by `QuotaType` enum value. **[Warning #3 — pin flat-base representation]** `UsageLineItem.Dimension` is a non-nullable `QuotaType` enum with no "flat base" member, and 17-01's contract + `QuotaType.cs` must NOT change. So represent the flat-base line deterministically as: `Dimension = default(QuotaType)` (first enum member, semantically ignored for the flat base), `Quantity = 0`, `UnitRate = 0m`, `Amount = plan.FlatBaseAmount`, `Description = UsageLineItem.FlatBaseDescription` — define a `public const string FlatBaseDescription = "flat base";` on `UsageLineItem` (or `UsageAggregator`) and identify flat-base lines by that Description, never by Dimension. Append it LAST (after the per-dimension lines) so ordering stays deterministic. If `plan.FlatBaseAmount <= 0`, emit no flat-base line. NO proration/tax/multi-currency (D-05). The aggregator reads usage only for the supplied `tenantId` — never enumerates other tenants (T-17-03 cross-tenant mitigation).
Register in `BillingServiceCollectionExtensions`: add to (or alongside) `AddRecordOnlyBilling` a `services.TryAddSingleton<IUsageAggregator, UsageAggregator>()`. Keep `AddRecordOnlyBilling` intact; either extend it or add `AddBillingRail` that calls both — pick the lowest-friction shape and document it so 17-03 calls one method.
Write tests in `<behavior>` against a stub `ITenantQuotaStore` (RED → GREEN).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - UsageAggregatorTests pass, including the deterministic-repeat assertion.
    - UsageAggregator.cs contains a `GetUsageAsync` call and a `GetUnitRate` call.
    - DI extension registers IUsageAggregator via TryAdd.
  </acceptance_criteria>
  <done>Aggregator deterministically prices a tenant's period usage into line items; registered in DI; green tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier-sourced MaxPdfRendersPerDay presets (MON-04)</name>
  <files>src/Muonroi.Quota.Abstractions/TenantQuota.cs, tests/Muonroi.Billing.Abstractions.Tests/TierQuotaLimitTests.cs</files>
  <read_first>
    - src/Muonroi.Quota.Abstractions/TenantQuota.cs (TenantQuotaPresets.Free/.Starter/.Professional/.Enterprise — all four currently MaxPdfRendersPerDay = int.MaxValue)
    - src/Muonroi.Quota.Abstractions/InMemoryTenantQuotaTracker.cs (GetLimit maps QuotaType.PdfRendersPerDay → quota.MaxPdfRendersPerDay; limit==int.MaxValue short-circuits to allowed)
  </read_first>
  <behavior>
    - Test 1: TenantQuotaPresets.Free.MaxPdfRendersPerDay is a finite value (e.g. a small daily cap) — strictly less than int.MaxValue.
    - Test 2: TenantQuotaPresets.Starter and .Professional have finite, monotonically increasing MaxPdfRendersPerDay (Free < Starter < Professional).
    - Test 3: TenantQuotaPresets.Enterprise.MaxPdfRendersPerDay remains int.MaxValue (unlimited tier).
  </behavior>
  <action>
In `TenantQuotaPresets`, replace the `MaxPdfRendersPerDay = int.MaxValue` lines for Free/Starter/Professional with finite per-tier daily caps that are monotonically increasing (per D-04: limits sourced from the licensed tier; choose reasonable values consistent with the existing per-tier scaling of other limits, e.g. Free small, Starter mid, Professional large — Claude's discretion within D-04, document the chosen numbers in XML doc comments). Leave `Enterprise.MaxPdfRendersPerDay = int.MaxValue` (unlimited). Update the XML-doc on `TenantQuota.MaxPdfRendersPerDay` to remove the "(record-only; no hard cap in Phase 16)" wording and state it is now the enforced per-tier daily cap (Phase 17). Do NOT change `QuotaType.cs`, the tracker's `GetLimit` switch (already maps PdfRendersPerDay correctly), or any OSS engine code.
Write TierQuotaLimitTests asserting the `<behavior>` cases.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - TierQuotaLimitTests pass.
    - Free/Starter/Professional MaxPdfRendersPerDay are finite and increasing; Enterprise stays int.MaxValue.
    - No change to QuotaType.cs or any file under src/Muonroi.Pdf (OSS engine untouched).
  </acceptance_criteria>
  <done>Non-Enterprise tiers carry a finite, tier-sourced PDF render cap; Enterprise unlimited; green tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant id → usage read | AggregateAsync must read only the supplied tenant's usage; no cross-tenant enumeration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | UsageAggregator.AggregateAsync | mitigate | read usage strictly via GetUsageAsync(tenantId); never enumerate or sum across tenants — covered by a single-tenant test |
| T-17-04 | Tampering | TenantQuotaPresets edits | mitigate | only preset numbers change; tracker GetLimit + QuotaExceededException(429) path unchanged; OSS engine untouched |
</threat_model>

<verification>
- Both task test classes green via `dotnet test` of the billing test project.
- Grep confirms no edits under `src/Muonroi.Pdf` (OSS) — only Quota.Abstractions + Billing.Abstractions touched.
</verification>

<success_criteria>
- MON-03: IUsageAggregator rolls per-tenant usage into priced UsageLineItems via PricingPlan, deterministically.
- MON-04: Per-tier MaxPdfRendersPerDay sourced from tier (finite for non-Enterprise) instead of always int.MaxValue.
</success_criteria>

<output>
Create `.planning/phases/17-monetization-rail-quota-billing-subscription/17-02-SUMMARY.md` when done.
</output>
