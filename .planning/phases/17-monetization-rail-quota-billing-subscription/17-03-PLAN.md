---
phase: 17-monetization-rail-quota-billing-subscription
plan: 03
type: execute
wave: 3
repo: muonroi-control-plane
depends_on: ["17-01", "17-02", "17-04"]
files_modified:
  - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs
  - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Muonroi.ControlPlane.Host.csproj
  - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/PricingEndpoints.cs
  - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/InvoicePreviewEndpoints.cs
  - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Services/Billing/ControlPlanePricingPlanProvider.cs
  - muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/QuotaEnforcementTests.cs
  - muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/InvoicePreviewEndpointTests.cs
autonomous: true
requirements: [MON-05, MON-06]

must_haves:
  truths:
    - "The control-plane host registers UseQuotaEnforcement(); a tenant over its tier API limit gets HTTP 429 at the API boundary"
    - "The OSS IMPdfService.RenderAsync path is never blocked by this enforcement (SC5 preserved)"
    - "An invoice-preview endpoint returns the computed amount for a tenant + period from aggregated usage and a real PricingPlan (compute-only, no charge)"
    - "PricingEndpoints prices are sourced from the PricingPlan model rather than hard-coded placeholder strings"
  artifacts:
    - path: "muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs"
      provides: "AddTenantQuotaManagement + AddBillingRail DI + app.UseQuotaEnforcement() registration (MON-05)"
      contains: "UseQuotaEnforcement"
    - path: "muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/InvoicePreviewEndpoints.cs"
      provides: "compute-only invoice-preview endpoint (MON-06)"
      contains: "invoice-preview"
    - path: "muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Services/Billing/ControlPlanePricingPlanProvider.cs"
      provides: "real PricingPlan source replacing placeholder prices (MON-06)"
      contains: "PricingPlan"
  key_links:
    - from: "muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs"
      to: "QuotaEnforcementMiddleware"
      via: "app.UseQuotaEnforcement() + AddTenantQuotaManagement()"
      pattern: "UseQuotaEnforcement"
    - from: "muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/InvoicePreviewEndpoints.cs"
      to: "IUsageAggregator.AggregateAsync"
      via: "endpoint handler"
      pattern: "AggregateAsync"
---

<objective>
Wire the billing rail into the control-plane host (D-01/D-03/MON-05/MON-06): register `AddTenantQuotaManagement()` + the billing rail DI + `app.UseQuotaEnforcement()` so a tenant over its tier API limit gets HTTP 429 at the API boundary (the OSS render path stays unblocked — SC5). Add a compute-only invoice-preview endpoint backed by `IUsageAggregator` + a real `PricingPlan`, and replace `PricingEndpoints` placeholder price strings with values sourced from the `PricingPlan` model.

Purpose: Connect the two existing stubs (unregistered middleware + placeholder pricing) to the real seam from 17-01/17-02 and the tier limits from 17-04.
Output: Registered enforcement, invoice-preview endpoint, PricingPlan-sourced pricing, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-control-plane (separate git repo). Code edits + commits via `git -C D:/sources/Core/muonroi-control-plane`. SUMMARY.md goes in the building-block phase dir and is committed in building-block. cwd at execution is muonroi-building-block; paths above are workspace-root-relative.
</execution_context>

<context>
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-CONTEXT.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-01-SUMMARY.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-02-SUMMARY.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-04-SUMMARY.md

<interfaces>
From muonroi-building-block QuotaEnforcementMiddleware (Muonroi.AspNetCore.Middleware) — ALREADY referenced by the host csproj; DO NOT rewrite it:
  app.UseQuotaEnforcement();  // checks QuotaType.ApiRequestsPerMinute via ITenantQuotaTracker; 429 + RetryAfter on exceed
  // resolves tenant from ISystemExecutionContextAccessor.Get().TenantId (already registered + set by the host tenant-scoping middleware)

From Muonroi.Quota.Abstractions (transitively referenced; ADD a direct ProjectReference for clarity):
  services.AddTenantQuotaManagement();  // registers ITenantQuotaStore (InMemory) + ITenantQuotaTracker
  TenantQuotaPresets.Free/.Professional/.Enterprise

From 17-01/17-02 (Muonroi.Billing.Abstractions — ADD a ProjectReference):
  services.AddRecordOnlyBilling();  // + IUsageAggregator registration (see 17-02 SUMMARY for the exact extension name)
  IUsageAggregator.AggregateAsync(tenantId, plan, periodStart, periodEnd, ct) -> IReadOnlyList<UsageLineItem>
  IBillingProvider.PreviewInvoiceAsync(tenantId, lineItems, ct)  // compute-only
  PricingPlan { TenantTier Tier; decimal FlatBaseAmount; IReadOnlyDictionary<QuotaType,decimal> UnitRates }
  UsageLineItem(QuotaType Dimension, long Quantity, decimal UnitRate, decimal Amount, string? Description)

From muonroi-control-plane Program.cs (existing pipeline anchors):
  app.MapPricingEndpoints();              // ~line 480 — replace placeholder prices
  ISystemExecutionContextAccessor + TenantContext.CurrentTenantId set by the tenant-scoping middleware (~line 518-570)
  // PdfAuditControlPlaneStore registered AddSingleton (D-03 adapter pattern at ~line 347) — mirror DI style

From muonroi-control-plane tests — WebApplicationFactory<Program> in ControlPlaneWebApplicationFactory.cs:
  builder.UseEnvironment("Testing"); DisableAuthorization=true; DevBypass auth reads x-tenant-id header.
</interfaces>

@muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/PricingEndpoints.cs
@muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Services/Compliance/PdfAuditControlPlaneStore.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register quota enforcement + billing rail DI; PricingPlan provider (MON-05 prep, MON-06)</name>
  <files>muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Muonroi.ControlPlane.Host.csproj, muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs, muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Services/Billing/ControlPlanePricingPlanProvider.cs</files>
  <read_first>
    - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs (DI section ~lines 64-465; pipeline ~lines 466-516; tenant-scoping middleware ~518-570; MapPricingEndpoints ~480)
    - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Muonroi.ControlPlane.Host.csproj (existing building-block ProjectReferences — Muonroi.AspNetCore already referenced; Muonroi.Pdf.Enterprise pulls Quota transitively)
    - 17-02-SUMMARY.md (exact billing DI extension method name + tier preset numbers)
    - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Services/Compliance/PdfAuditControlPlaneStore.cs (DI/adapter style: ILogger, sealed, namespace under Host.Services)
  </read_first>
  <action>
Add direct `ProjectReference`s in the host csproj to `..\..\..\..\muonroi-building-block\src\Muonroi.Quota.Abstractions\Muonroi.Quota.Abstractions.csproj` and `..\..\..\..\muonroi-building-block\src\Muonroi.Billing.Abstractions\Muonroi.Billing.Abstractions.csproj` (explicit, not transitive — they are consumed directly here).
In `Program.cs` DI section: call `builder.Services.AddTenantQuotaManagement();` (registers ITenantQuotaStore + ITenantQuotaTracker the middleware needs) and the billing-rail DI from 17-02 (`AddRecordOnlyBilling()` + IUsageAggregator — use the exact extension name from 17-02-SUMMARY). Register `ControlPlanePricingPlanProvider`.
**[BLOCKER FIX — DI lifetime]** `AddTenantQuotaManagement()` registers `ITenantQuotaTracker` as **Scoped** (`TenantQuotaServiceCollectionExtensions.cs:16` `TryAddScoped`), but `QuotaEnforcementMiddleware` takes `ITenantQuotaTracker` as a **constructor** parameter, so `UseMiddleware<T>` instantiates it once from the **root** provider → `InvalidOperationException: Cannot resolve scoped service … from root provider` at first request (build-only verify will NOT catch this; it is the Phase-16 "defined-but-unreachable" failure mode). Immediately AFTER `AddTenantQuotaManagement();`, override the tracker to singleton: `builder.Services.AddSingleton<ITenantQuotaTracker, InMemoryTenantQuotaTracker>();` (safe — the tracker is stateless and delegates to the singleton `ITenantQuotaStore`). Do NOT modify the middleware.
Create `Services/Billing/ControlPlanePricingPlanProvider.cs` (namespace `Muonroi.ControlPlane.Host.Services.Billing`, sealed, ILogger ctor mirroring PdfAuditControlPlaneStore): exposes `PricingPlan GetPlan(TenantTier tier)` building a real `PricingPlan` with per-tier `UnitRates` (at minimum a `QuotaType.PdfRendersPerDay` rate and `QuotaType.ApiRequestsPerMinute` rate) + optional `FlatBaseAmount`, aligned with the tier→limit map from 17-04 (D-05 simple per-unit × tier rate). These are the real price values that replace the placeholder strings.
**[Warning #2 — tier mapping]** The building-block `TenantTier` (Free/Starter/Professional/Enterprise) and license-server `LicenseTier` (Free/Licensed/Enterprise) are asymmetric. Add an explicit, documented `LicenseTier → TenantTier` mapping inside the provider (Free→Free, Licensed→Professional, Enterprise→Enterprise) so the tier→limit/price lookup is deterministic across the seam — do not leave it to "document the alignment". Expose `PricingPlan GetPlanForLicenseTier(LicenseTier)` that maps then delegates to `GetPlan(TenantTier)`.
In the request pipeline, register `app.UseQuotaEnforcement();` AFTER the tenant-scoping middleware that sets `ISystemExecutionContextAccessor` (so the middleware can resolve `TenantId`) and AFTER `UseAuthentication/UseAuthorization`, but it must run for the tenant-scoped API endpoints. Place it after the tenant-scoping `app.Use(...)` block (~line 570) and before the API route-group mappings (`controlPlane.Map...`). Do NOT place it ahead of the anonymous-surface guard (the middleware already no-ops when TenantId is empty). Do NOT modify QuotaEnforcementMiddleware itself (D-01: register + configure only).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-control-plane && dotnet build src/Host/Muonroi.ControlPlane.Host/Muonroi.ControlPlane.Host.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - Host builds clean.
    - Program.cs contains `AddTenantQuotaManagement(` and `app.UseQuotaEnforcement(`.
    - Program.cs contains `AddSingleton<ITenantQuotaTracker` (the scoped→singleton override) AFTER AddTenantQuotaManagement; the host resolves the enforcement middleware without a scope-validation exception (proven by the Task 3 429 integration test, which exercises the real DI path).
    - ControlPlanePricingPlanProvider builds a PricingPlan with per-dimension UnitRates and exposes a documented LicenseTier→TenantTier mapping (GetPlanForLicenseTier).
    - Host csproj has direct ProjectReferences to Muonroi.Quota.Abstractions + Muonroi.Billing.Abstractions.
  </acceptance_criteria>
  <done>Quota management + billing rail registered; enforcement middleware wired into the pipeline; PricingPlan provider exists; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Invoice-preview endpoint + PricingPlan-sourced PricingEndpoints (MON-06)</name>
  <files>muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/InvoicePreviewEndpoints.cs, muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/PricingEndpoints.cs, muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs</files>
  <read_first>
    - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Endpoints/PricingEndpoints.cs (MapGet /api/v1/pricing, AllowAnonymous, PricingCatalog shape { name, price, limits, features })
    - 17-01-SUMMARY.md + 17-02-SUMMARY.md (IUsageAggregator + IBillingProvider.PreviewInvoiceAsync + PricingPlan shapes)
    - muonroi-control-plane/src/Host/Muonroi.ControlPlane.Host/Program.cs (where MapPricingEndpoints is called ~line 480; tenant resolution via ISystemExecutionContextAccessor)
  </read_first>
  <action>
Create `Endpoints/InvoicePreviewEndpoints.cs` (namespace `Muonroi.ControlPlane.Host.Endpoints`) with `MapInvoicePreviewEndpoints` mapping `GET /api/v1/billing/invoice-preview` (query params for period start/end, defaulting to the current day if absent). Handler resolves the current tenant from `ISystemExecutionContextAccessor`, resolves the tier→`PricingPlan` via `ControlPlanePricingPlanProvider`, calls `IUsageAggregator.AggregateAsync(tenantId, plan, periodStart, periodEnd)` then `IBillingProvider.PreviewInvoiceAsync(tenantId, lineItems)`, and returns `Results.Ok` with `{ tenantId, periodStart, periodEnd, lineItems, totalAmount }` where totalAmount = Σ lineItem.Amount. This is COMPUTE-ONLY — it MUST NOT execute any charge (D-03). Gate it the same way other tenant-scoped control-plane endpoints are gated (respect `!authOptions.DisableAuthorization`). Map it in `Program.cs` alongside `MapPricingEndpoints` (e.g. `app.MapInvoicePreviewEndpoints();`).
Refactor `PricingEndpoints.PricingCatalog`: replace the hard-coded placeholder `price` strings ("$0"/"$20"/"Contact us") with values derived from the `PricingPlan` per tier (e.g. format FlatBaseAmount + a representative per-unit rate). Keep the emitted SPA shape `{ name, price, description, limits, features }` intact; only the `price` source changes from placeholder literal to PricingPlan-derived. Inject `ControlPlanePricingPlanProvider` (convert the static endpoint to resolve the provider from DI in the handler).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-control-plane && dotnet build src/Host/Muonroi.ControlPlane.Host/Muonroi.ControlPlane.Host.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - InvoicePreviewEndpoints.cs maps GET /api/v1/billing/invoice-preview and calls AggregateAsync + PreviewInvoiceAsync.
    - The handler computes totalAmount and executes NO charge (grep: no IBillingProvider method other than PreviewInvoiceAsync called).
    - PricingEndpoints price values come from ControlPlanePricingPlanProvider, not hard-coded "$20"/"Contact us" literals.
    - Host builds clean.
  </acceptance_criteria>
  <done>Compute-only invoice-preview endpoint live; pricing sourced from PricingPlan; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 429-enforcement + invoice-preview integration tests (MON-05, MON-06)</name>
  <files>muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/QuotaEnforcementTests.cs, muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/InvoicePreviewEndpointTests.cs</files>
  <read_first>
    - muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/ControlPlaneWebApplicationFactory.cs (Testing env, DisableAuthorization=true, DevBypass reads x-tenant-id; how to override services in ConfigureServices)
    - muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/ControlPlaneEndpointTests.cs (HttpClient request + x-tenant-id header pattern, status-code asserts)
    - muonroi-control-plane/tests/Muonroi.ControlPlane.Host.Tests/PortalEndpointTests.cs (another endpoint test pattern for the GET assert shape)
  </read_first>
  <behavior>
    - Test 1 (429 at API boundary): Build a factory that registers a tenant quota with a tiny MaxApiRequestsPerMinute (e.g. seed a TenantQuota with MaxApiRequestsPerMinute=1 via ITenantQuotaStore.SaveQuotaAsync in ConfigureServices, or override the tracker). Send 2 requests with the same x-tenant-id to a tenant-scoped endpoint; assert the second returns HTTP 429 with the rate_limit_exceeded body + Retry-After header.
    - Test 2 (SC5 unblocked): Assert that an anonymous / no-tenant request (no x-tenant-id) is NOT 429'd by the enforcement (the middleware no-ops when TenantId is empty) — i.e. the OSS-equivalent unscoped path is never blocked.
    - Test 3 (invoice-preview compute-only): seed some PdfRendersPerDay usage for a tenant via ITenantQuotaStore.RecordUsageAsync, GET /api/v1/billing/invoice-preview with that x-tenant-id, assert 200 + a totalAmount > 0 + line items reflecting the seeded usage × the tier unit rate; assert no persisted charge/side-effect (a fresh GET returns the same amount — idempotent compute-only).
  </behavior>
  <action>
Add `QuotaEnforcementTests` and `InvoicePreviewEndpointTests` deriving from / using `ControlPlaneWebApplicationFactory` (or a small subclass that seeds quota/usage in `ConfigureServices`). Use the existing x-tenant-id DevBypass mechanism to bind a tenant. RED first: write the tests; they fail until Tasks 1-2 are wired (they should now pass). Keep the seeding deterministic (InMemory store is per-testRunId already). Assert exact status codes + body fields per `<behavior>`.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-control-plane && dotnet test tests/Muonroi.ControlPlane.Host.Tests/Muonroi.ControlPlane.Host.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - QuotaEnforcementTests + InvoicePreviewEndpointTests pass.
    - Full control-plane Host.Tests suite green (0 failures) — per-repo Pre-Push Test Gate.
    - 429 test proves enforcement at the API boundary; no-tenant test proves SC5 (unscoped path not blocked).
  </acceptance_criteria>
  <done>Enforcement + invoice-preview verified by integration tests; full control-plane suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → control-plane API | Untrusted request crosses here; quota enforcement caps abuse at this boundary (429). |
| tenant → invoice-preview | Tenant must only see its own aggregated usage (resolved from execution context, never client-supplied tenant id for the rollup). |
| OSS render path | Must stay outside the enforcement boundary — SC5 inviolable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-20 | Denial of Service | tenant-scoped API endpoints | mitigate | UseQuotaEnforcement() → 429 over tier MaxApiRequestsPerMinute (MON-05); proven by Test 1 |
| T-17-21 | Information Disclosure | invoice-preview endpoint | mitigate | aggregate by tenant resolved from ISystemExecutionContextAccessor, not a client-supplied tenant id; reuses 17-02 single-tenant aggregator (T-17-03) |
| T-17-22 | Elevation of Privilege | SC5 — enforcement must not touch OSS render | mitigate | enforcement is API-boundary middleware only; IMPdfService.RenderAsync unchanged; no-tenant path not blocked (Test 2). Verified byte-identical in 17-05 |
| T-17-23 | Tampering | invoice-preview executes no charge | mitigate | only IBillingProvider.PreviewInvoiceAsync (compute-only) called; idempotent-amount test |
| T-17-SC | Tampering | csproj ProjectReferences only, no new NuGet | accept | only source ProjectReferences added; no package-manager installs (Package Legitimacy Gate not triggered) |
</threat_model>

<verification>
- Full `dotnet test` control-plane Host.Tests green (per-repo Pre-Push Test Gate).
- Program.cs greps: `AddTenantQuotaManagement(` and `app.UseQuotaEnforcement(` both present.
- PricingEndpoints contains no hard-coded "$20"/"Contact us" placeholder literal.
</verification>

<success_criteria>
- MON-05: host registers UseQuotaEnforcement(); over-limit tenant gets 429 at the API boundary; OSS render path never blocked (SC5).
- MON-06: invoice-preview endpoint returns computed amount from aggregated usage + real PricingPlan; placeholder prices replaced.
</success_criteria>

<output>
Create `.planning/phases/17-monetization-rail-quota-billing-subscription/17-03-SUMMARY.md` when done (committed in building-block).
</output>
