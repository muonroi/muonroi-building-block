---
phase: 17-monetization-rail-quota-billing-subscription
plan: 05
type: execute
wave: 4
repo: cross-repo (building-block + control-plane + license-server)
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - tests/Muonroi.Pdf.Tests/Service/OssBoundaryBillingLeakTests.cs
autonomous: false
requirements: [MON-08]

must_haves:
  truths:
    - "Full unit-test suites are green (0 failures) across muonroi-building-block, muonroi-control-plane, and muonroi-license-server"
    - "The Muonroi.Pdf (OSS) engine is byte-identical — no golden snapshot re-baseline was required"
    - "No billing/quota-enforcement reference leaks into the OSS engine (Muonroi.Pdf depends on nothing under billing/enterprise)"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Service/OssBoundaryBillingLeakTests.cs"
      provides: "automated assertion that Muonroi.Pdf has no Billing/Quota/Enterprise dependency (MON-08 / SC5)"
      contains: "Billing"
  key_links:
    - from: "tests/Muonroi.Pdf.Tests/Service/OssBoundaryBillingLeakTests.cs"
      to: "Muonroi.Pdf assembly referenced assemblies"
      via: "reflection over GetReferencedAssemblies()"
      pattern: "GetReferencedAssemblies"
---

<objective>
Close the phase: prove (MON-08 / SC5) that (1) the full suites are green across all three repos, (2) the OSS `Muonroi.Pdf` engine is byte-identical (golden snapshots unchanged — no re-baseline), and (3) no billing/quota-enforcement reference leaked into the OSS engine. Add an automated leak-guard test and run a final cross-repo verification.

Purpose: Lock the inviolable open-core boundary (SC5) with an automated regression guard and a green-everywhere gate before the phase is considered done.
Output: OSS boundary leak-guard test + a verified cross-repo green + byte-identical golden result, recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Cross-repo. The leak-guard test + commit land in muonroi-building-block (`git -C D:/sources/Core/muonroi-building-block`). The verification reads across all three repos (absolute paths). SUMMARY.md goes in the building-block phase dir.
</execution_context>

<context>
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-CONTEXT.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-01-SUMMARY.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-02-SUMMARY.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-03-SUMMARY.md
@.planning/phases/17-monetization-rail-quota-billing-subscription/17-04-SUMMARY.md

<interfaces>
From OSS-BOUNDARY.md — Muonroi.Pdf, Muonroi.Pdf.Abstractions, Muonroi.Pdf.Governance are OSS; billing/quota-enforcement must not be referenced by Muonroi.Pdf.
From tests/Muonroi.Pdf.Tests — golden snapshot tests live under tests/Muonroi.Pdf.Tests/Golden (byte-determinism corpus). The leak-guard test belongs under tests/Muonroi.Pdf.Tests/Service.
The OSS engine assembly is Muonroi.Pdf (typeof a known public type, e.g. from Muonroi.Pdf.Extensions, gives the assembly).
</interfaces>

@tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OSS boundary billing-leak guard test (MON-08 / SC5)</name>
  <files>tests/Muonroi.Pdf.Tests/Service/OssBoundaryBillingLeakTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj (references Muonroi.Pdf + Muonroi.Pdf.Enterprise; the OSS assembly is Muonroi.Pdf)
    - tests/Muonroi.Pdf.Tests/Enterprise/PackagingMetadataTests.cs (existing assembly-metadata/reflection test pattern in this repo)
    - OSS-BOUNDARY.md (OSS vs Commercial lists)
    - src/Muonroi.Pdf.Enterprise/Metering/EnterprisePdfServiceWrapper.cs (the Enterprise wrapper references Quota — confirms billing/quota seam lives in Enterprise/control-plane, NOT in OSS Muonroi.Pdf)
  </read_first>
  <behavior>
    - Test 1: The Muonroi.Pdf (OSS) assembly's GetReferencedAssemblies() contains NO assembly named "Muonroi.Billing.Abstractions".
    - Test 2: The Muonroi.Pdf (OSS) assembly references neither "Muonroi.Quota.Abstractions" nor "Muonroi.AspNetCore" (quota enforcement lives at the API/enterprise layer, never in the OSS engine) — assert the absence list.
    - Test 3: Sanity counter-assert — Muonroi.Pdf.Enterprise IS allowed to reference Muonroi.Quota.Abstractions (proves the seam lives on the Enterprise side, so Test 1/2 are meaningful, not vacuous).
  </behavior>
  <action>
Create `OssBoundaryBillingLeakTests` under `tests/Muonroi.Pdf.Tests/Service/` (xunit, mirror PackagingMetadataTests reflection style). Resolve the OSS assembly via a known public Muonroi.Pdf type (e.g. `typeof(Muonroi.Pdf.Extensions.<known type>).Assembly`). Assert via `assembly.GetReferencedAssemblies()` that none is named `Muonroi.Billing.Abstractions`, `Muonroi.Quota.Abstractions`, or `Muonroi.AspNetCore` (Test 1/2). For Test 3, resolve the Muonroi.Pdf.Enterprise assembly and assert it DOES reference `Muonroi.Quota.Abstractions` (the EnterprisePdfServiceWrapper dependency). This guards SC5 automatically against future leaks.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~OssBoundaryBillingLeakTests"</automated>
  </verify>
  <acceptance_criteria>
    - OssBoundaryBillingLeakTests pass (3 cases).
    - The test reflects over GetReferencedAssemblies() and names Muonroi.Billing.Abstractions in its absence assertion.
  </acceptance_criteria>
  <done>Automated SC5 leak-guard in place and green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-repo full-suite green + byte-identical golden verification (MON-08)</name>
  <files>tests/Muonroi.Pdf.Tests/Service/OssBoundaryBillingLeakTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Golden (the golden snapshot corpus that must remain byte-identical — no re-baseline)
    - .planning/phases/17-monetization-rail-quota-billing-subscription/17-01-SUMMARY.md through 17-04-SUMMARY.md (what changed in each repo)
  </read_first>
  <action>
Run the full unit-test suite in each repo and confirm 0 failures (per-repo Pre-Push Test Gate, CLAUDE.md):
- building-block: `dotnet test` on the relevant projects — at minimum `tests/Muonroi.Pdf.Tests`, `tests/Muonroi.Pdf.Governance.Tests`, `tests/Muonroi.Billing.Abstractions.Tests` (and any project touching Quota). Confirm the Muonroi.Pdf golden snapshot tests pass WITHOUT any snapshot file being modified (byte-identical — DET requirements). If any golden file changed on disk, that is an SC5 failure: investigate and revert the engine change; the OSS engine must not have changed.
- control-plane: `dotnet test tests/Muonroi.ControlPlane.Host.Tests` — 0 failures.
- license-server: `dotnet test tests/Muonroi.LicenseServer.Tests` — 0 failures.
Confirm via `git -C D:/sources/Core/muonroi-building-block status --porcelain tests/Muonroi.Pdf.Tests/Golden src/Muonroi.Pdf` shows NO modifications to the OSS engine source or golden corpus (only the new leak-guard test under tests/.../Service is a permitted addition). Record the three suite results + the byte-identical confirmation in the SUMMARY.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Billing.Abstractions.Tests/Muonroi.Billing.Abstractions.Tests.csproj -c Debug && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug && dotnet test tests/Muonroi.Pdf.Governance.Tests/Muonroi.Pdf.Governance.Tests.csproj -c Debug && git -C D:/sources/Core/muonroi-building-block status --porcelain src/Muonroi.Pdf tests/Muonroi.Pdf.Tests/Golden</automated>
  </verify>
  <acceptance_criteria>
    - building-block AFFECTED suites all green (run per-project, NOT the full ~80-project solution which is known to flake on nested build — see memory test_flakiness_nested_build): `Muonroi.Billing.Abstractions.Tests`, `Muonroi.Pdf.Tests`, `Muonroi.Pdf.Governance.Tests`. (Quota preset changes are covered by Billing.Abstractions.Tests/TierQuotaLimitTests; the middleware is unchanged.)
    - control-plane `Muonroi.ControlPlane.Host.Tests` green; license-server `muonroi-license-server.sln` test suite green (record all three repos' results in SUMMARY).
    - `git status --porcelain src/Muonroi.Pdf tests/Muonroi.Pdf.Tests/Golden` returns empty (no OSS engine or golden changes) — byte-identical confirmed.
    - No file under src/Muonroi.Pdf was modified across the entire phase.
  </acceptance_criteria>
  <done>All three suites green; OSS engine + golden corpus byte-identical; results recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify cross-repo monetization rail + SC5</name>
  <what-built>The full monetization rail across three repos: billing seam + record-only provider + usage aggregator (building-block), registered 429 quota enforcement + compute-only invoice-preview + PricingPlan-sourced pricing (control-plane), subscription/renewal + tier-limit mapping (license-server), plus an automated SC5 leak-guard.</what-built>
  <how-to-verify>
1. Confirm the three suite results recorded in 17-05-SUMMARY.md are all 0-failure.
2. Confirm `git status` in muonroi-building-block shows NO changes under `src/Muonroi.Pdf` and NO changes under `tests/Muonroi.Pdf.Tests/Golden` (SC5 byte-identical).
3. Spot-check: control-plane `Program.cs` contains `app.UseQuotaEnforcement();` and `AddTenantQuotaManagement();`.
4. Spot-check: license-server `KeyEndpoints.cs` exposes a `renew` route.
5. Confirm OSS-BOUNDARY.md lists Muonroi.Billing.Abstractions under OSS Packages only (not Commercial).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OSS engine ↔ billing/enterprise | One-way only (Enterprise/billing → OSS). The leak-guard enforces it automatically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-30 | Tampering | future code adding a billing ref to Muonroi.Pdf | mitigate | OssBoundaryBillingLeakTests fails the build if Muonroi.Pdf ever references Billing/Quota/AspNetCore (SC5 regression guard) |
| T-17-31 | Tampering | OSS golden output drift | mitigate | byte-identical gate — git status must show no golden/engine change; any drift blocks the phase |
| T-17-SC | Tampering | no new packages — test-only addition | accept | only a new test file added; no package-manager installs (Package Legitimacy Gate not triggered) |
</threat_model>

<verification>
- All three repos: `dotnet test` 0 failures.
- `git status --porcelain src/Muonroi.Pdf tests/Muonroi.Pdf.Tests/Golden` empty (byte-identical OSS).
- OssBoundaryBillingLeakTests green.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- MON-08: full suites green across all three repos; Muonroi.Pdf byte-identical (no golden re-baseline); no billing reference leaks into the OSS engine.
</success_criteria>

<output>
Create `.planning/phases/17-monetization-rail-quota-billing-subscription/17-05-SUMMARY.md` when done (committed in building-block). Record the three per-repo suite results + the byte-identical confirmation.
</output>
