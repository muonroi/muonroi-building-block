---
phase: 08-source-generator-aot-designsystem
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj
  - benchmarks/Muonroi.Pdf.Benchmarks/PdfRenderBenchmarks.cs
autonomous: true
requirements:
  - SG-02
  - ALLOC-01

must_haves:
  truths:
    - "BenchmarkDotNet 0.15.8 is added to Directory.Packages.props"
    - "benchmarks/Muonroi.Pdf.Benchmarks project exists targeting net8.0 with IsPackable=false and MemoryDiagnoser attribute"
    - "PdfRenderBenchmarks class has RuntimeFactory benchmark (Baseline=true) and SourceGenerated benchmark"
    - "Benchmark project builds and runs: dotnet run -c Release --project benchmarks/Muonroi.Pdf.Benchmarks exits 0"
    - "ALLOC-01 baseline is captured: BDN output includes Allocated column with a non-zero value for RuntimeFactory"
  artifacts:
    - path: "Directory.Packages.props"
      provides: "BenchmarkDotNet 0.15.8 CPM entry"
      contains: "BenchmarkDotNet"
    - path: "benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj"
      provides: "Benchmark project — net8.0, IsPackable=false, not in test glob"
      contains: "IsPackable"
    - path: "benchmarks/Muonroi.Pdf.Benchmarks/PdfRenderBenchmarks.cs"
      provides: "BDN harness with MemoryDiagnoser and two benchmarks"
      contains: "MemoryDiagnoser"
  key_links:
    - from: "PdfRenderBenchmarks"
      to: "IMPdfService.RenderAsync"
      via: "both benchmarks call RenderAsync on the same 50KB template"
      pattern: "RenderAsync.*50|50.*RenderAsync"
    - from: "PdfRenderBenchmarks"
      to: "MemoryDiagnoser"
      via: "[MemoryDiagnoser] attribute on class"
      pattern: "\\[MemoryDiagnoser\\]"
---

<objective>
Add BenchmarkDotNet 0.15.8 to CPM and create the Muonroi.Pdf.Benchmarks harness with a [MemoryDiagnoser]-annotated class. The RuntimeFactory benchmark captures the ALLOC-01 v0.1 baseline before any allocation optimization. The SourceGenerated benchmark slot is wired but uses the runtime factory until Plan 01's SG is available (Wave 2 will fill the SG renderer slot).

Purpose: ALLOC-01 (capture baseline), SC2 (≥3× throughput measurement infra), SC4 (≥30% alloc reduction baseline). The baseline MUST be measured before any optimization — this plan is the "before" snapshot.
Output: Benchmark project that builds and runs; BDN Allocated column output captured for the v0.1 baseline.
</objective>

<execution_context>
@C:/Users/phila/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/phila/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/d/sources/Core/muonroi-building-block/.planning/PROJECT.md
@/d/sources/Core/muonroi-building-block/.planning/ROADMAP.md

## Package Legitimacy Gate — BenchmarkDotNet

Per RESEARCH.md Package Legitimacy Audit:
- BenchmarkDotNet 0.15.8: [ASSUMED] — .NET Foundation project, >50M downloads, github.com/dotnet/BenchmarkDotNet
- Disposition: Approved — no slopcheck required for .NET Foundation packages; proceed without checkpoint
- Add ONLY to `Directory.Packages.props` as `<PackageVersion Include="BenchmarkDotNet" Version="0.15.8" />`
- Benchmark project references it as `<PackageReference Include="BenchmarkDotNet" />` (no Version)

## ALLOC-01 context

SC4 requires ≥30% allocation reduction vs the v0.1 baseline captured in THIS plan.
The baseline is the `RuntimeFactory()` benchmark Allocated column value from a `dotnet run -c Release` run on the unoptimized codebase.
After optimizations in Plan 05 (Wave 3), the same benchmark must show ≥30% lower Allocated.

## BenchmarkDotNet run command

```bash
dotnet run -c Release --project benchmarks/Muonroi.Pdf.Benchmarks
```
NOT `dotnet test`. BDN benchmarks are NOT test projects.

## Anti-pattern: benchmarks in solution build

Do NOT add `benchmarks/` to `Directory.Build.targets` test detection (the targets file likely globs `tests/**/*.csproj`). The benchmarks/ directory is separate and should not be treated as a test project.

## 50 KB reference template

The benchmark setup must load a ~50 KB HTML template matching PERF-01/PERF-02 spec.
Use one of:
- A `TestResources/` HTML file if already present in tests/Muonroi.Pdf.Tests
- An inline `new string('x', 50_000)` wrapped in minimal HTML (acceptable for allocation measurement — structure matters less than byte count for alloc profiling)
- Prefer the real test resource if accessible, to match the PERF gate baseline exactly

## Existing PERF gate

`tests/Muonroi.Pdf.Tests/Performance/PerfGateTests.cs` uses Stopwatch (not allocation-aware). The BDN harness supplements it — do not modify PerfGateTests.cs.
</context>

<interfaces>
<!-- BDN MemoryDiagnoser pattern (from RESEARCH.md) -->
```csharp
[MemoryDiagnoser]
[SimpleJob]
public class PdfRenderBenchmarks
{
    private IMPdfService _service = null!;
    private string _html50kb = null!;

    [GlobalSetup]
    public void Setup()
    {
        // Build a minimal DI container with AddPdf(config), resolve IMPdfService
        // Load or construct a ~50KB HTML string
    }

    [Benchmark(Baseline = true)]
    public async Task RuntimeFactory()
    {
        using var ms = new System.IO.MemoryStream();
        await _service.RenderAsync(_html50kb, ms, new PdfRenderOptions());
    }

    [Benchmark]
    public async Task SourceGenerated()
    {
        // In Wave 1 (before SG renderer is wired), this calls RuntimeFactory path too.
        // Plan 05 (Wave 3) will swap in the real SG renderer here to measure SC2.
        using var ms = new System.IO.MemoryStream();
        await _service.RenderAsync(_html50kb, ms, new PdfRenderOptions());
    }
}
```

<!-- BenchmarkRunner.Run entry point -->
```csharp
// Program.cs
using BenchmarkDotNet.Running;
BenchmarkRunner.Run<PdfRenderBenchmarks>(args: args);
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>BenchmarkDotNet 0.15.8 is a [ASSUMED]-tagged package per the RESEARCH.md Package Legitimacy Audit. Before adding it to Directory.Packages.props, confirm the package identity is legitimate.</what-built>
  <how-to-verify>
1. Open https://www.nuget.org/packages/BenchmarkDotNet/0.15.8 in a browser
2. Confirm: Owner is "dotnet", Authors includes "BenchmarkDotNet Team", Downloads > 50M on the package page
3. Confirm: Source repository link points to github.com/dotnet/BenchmarkDotNet
4. If all three match: type "approved" to proceed
5. If anything is wrong: describe the discrepancy
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding BenchmarkDotNet to CPM, or describe any concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add BenchmarkDotNet to CPM + create benchmark project</name>
  <files>
    Directory.Packages.props
    benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj
    benchmarks/Muonroi.Pdf.Benchmarks/PdfRenderBenchmarks.cs
  </files>
  <action>
Add to `Directory.Packages.props` inside the existing `<ItemGroup>` (alphabetical order among B entries):
```xml
<PackageVersion Include="BenchmarkDotNet" Version="0.15.8" />
```
No other changes to Directory.Packages.props.

Create directory `benchmarks/Muonroi.Pdf.Benchmarks/`.

Create `benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj`:
- `<TargetFramework>net8.0</TargetFramework>`
- `<Nullable>enable</Nullable>`, `<LangVersion>latest</LangVersion>`, `<ImplicitUsings>enable</ImplicitUsings>`
- `<IsPackable>false</IsPackable>` — must never be published
- `<AllowUnsafeBlocks>false</AllowUnsafeBlocks>`
- `<OutputType>Exe</OutputType>` — BDN requires an executable entry point
- PackageReference: `<PackageReference Include="BenchmarkDotNet" />` (no Version — CPM)
- PackageReference: `<PackageReference Include="Microsoft.Extensions.DependencyInjection" />` (no Version — CPM; for DI setup in GlobalSetup)
- PackageReference: `<PackageReference Include="Microsoft.Extensions.Configuration" />` (no Version — CPM)
- ProjectReference to `src/Muonroi.Pdf/Muonroi.Pdf.csproj`
- ProjectReference to `src/Muonroi.Pdf.Abstractions/Muonroi.Pdf.Abstractions.csproj`
- ProjectReference to `src/Muonroi.Pdf.Governance/Muonroi.Pdf.Governance.csproj`

Create `benchmarks/Muonroi.Pdf.Benchmarks/PdfRenderBenchmarks.cs`:

File contains two classes:
1. `Program.cs` inline top-level statements: `BenchmarkRunner.Run<PdfRenderBenchmarks>(args: args);` — OR a separate `Program.cs` file (prefer separate file for clarity).

2. `PdfRenderBenchmarks` class in namespace `Muonroi.Pdf.Benchmarks`:
   - `[MemoryDiagnoser]` attribute — REQUIRED for SC4 Allocated column
   - `[SimpleJob]` attribute — default job (not short/dry; must run full warmup for accurate alloc measurement)
   - `[RankColumn]` attribute — helps compare RuntimeFactory vs SourceGenerated ranking
   - Fields: `private IMPdfService _service = null!;`, `private string _html50kb = null!;`
   - `[GlobalSetup] public void Setup()`:
     - Build a minimal `ServiceCollection`, add `IConfiguration` from empty `ConfigurationBuilder`
     - Call `services.AddPdf(configuration)` — registers the engine
     - Build service provider, resolve `IMPdfService`
     - Build `_html50kb`: create a realistic 50 KB HTML string. Use a table-heavy HTML with enough content to hit ~50 000 bytes. Example: build a 100-row table with 5 columns using `StringBuilder` to hit the byte target. Wrap in `<!DOCTYPE html><html><head><style>...</style></head><body>...</body></html>` with valid CSS (no forbidden constructs).
   - `[Benchmark(Baseline = true)] public async Task RuntimeFactory()`:
     - `using var ms = new System.IO.MemoryStream();`
     - `await _service.RenderAsync(_html50kb, ms, new PdfRenderOptions());`
   - `[Benchmark] public async Task SourceGenerated()`:
     - Same body as RuntimeFactory for now (both measure the runtime path — SG renderer not wired until Wave 2)
     - Add comment: `// TODO(Wave3): swap _service.RenderAsync for _sgRenderer.RenderAsync after Plan 01 SG is wired`
   - `[GlobalCleanup] public void Cleanup()`: dispose service provider if it is IDisposable

Create separate `benchmarks/Muonroi.Pdf.Benchmarks/Program.cs`:
```csharp
using BenchmarkDotNet.Running;
using Muonroi.Pdf.Benchmarks;

BenchmarkRunner.Run<PdfRenderBenchmarks>(args: args);
```

Build command: `dotnet build benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj -m:1 -nodereuse:false -q`
  </action>
  <verify>
    <automated>dotnet build benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj -m:1 -nodereuse:false -q</automated>
  </verify>
  <done>Benchmark project builds 0 errors; PdfRenderBenchmarks.cs contains [MemoryDiagnoser]; Directory.Packages.props contains BenchmarkDotNet 0.15.8</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Benchmark project is built. Running it captures the ALLOC-01 v0.1 baseline (Allocated bytes per operation for RuntimeFactory before any optimization).</what-built>
  <how-to-verify>
Run the benchmark from the repo root (this will take 2-5 minutes — BDN runs full warmup + measurement):

```
dotnet run -c Release --project benchmarks/Muonroi.Pdf.Benchmarks
```

When complete, BDN prints a results table with columns: Method | Mean | Error | StdDev | Ratio | Gen0 | Gen1 | Allocated

1. Confirm the table appeared with two rows (RuntimeFactory, SourceGenerated)
2. Note the "Allocated" value for RuntimeFactory (e.g. "2.45 MB" or "312.5 KB")
3. Record this number — it is the SC4 baseline. Optimizations in Wave 3 must reduce it by ≥30%
4. Type the Allocated value for RuntimeFactory so it can be recorded in the SUMMARY
  </how-to-verify>
  <resume-signal>Type the RuntimeFactory Allocated value (e.g. "2.45 MB") or "skip" if running the full benchmark is not feasible right now (baseline can be captured later in Wave 3)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark project → engine | Benchmark calls the real engine via DI; no external network or file access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-BM-01 | Tampering | BenchmarkDotNet NuGet | mitigate | Blocking checkpoint:human-verify gates the install; package confirmed as .NET Foundation project before adding to CPM |
| T-08-BM-02 | Denial of Service | Benchmark project in CI | accept | Benchmark project has `IsPackable=false` and is in `benchmarks/` not `tests/`; Directory.Build.targets test glob (`tests/**`) will not discover it; no risk of blocking CI |
| T-08-BM-SC | Tampering | NuGet install via CPM | mitigate | Human checkpoint before install; slopcheck [ASSUMED] resolved by human verification |
</threat_model>

<verification>
dotnet build benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj -m:1 -nodereuse:false -q
# Full run (captures baseline — run manually, not in CI):
# dotnet run -c Release --project benchmarks/Muonroi.Pdf.Benchmarks
</verification>

<success_criteria>
- `Directory.Packages.props` contains `<PackageVersion Include="BenchmarkDotNet" Version="0.15.8" />`
- `benchmarks/Muonroi.Pdf.Benchmarks/Muonroi.Pdf.Benchmarks.csproj` exists with `IsPackable=false`, `OutputType=Exe`
- `PdfRenderBenchmarks` has `[MemoryDiagnoser]`, `RuntimeFactory(Baseline=true)`, and `SourceGenerated` benchmarks
- Project builds 0 errors with `dotnet build -m:1 -nodereuse:false -q`
- Running `dotnet run -c Release` produces BDN results table with Allocated column
- Baseline Allocated value for RuntimeFactory recorded in SUMMARY.md
</success_criteria>

<output>
Create `.planning/phases/08-source-generator-aot-designsystem/08-03-SUMMARY.md` when done.
Record the RuntimeFactory Allocated baseline value in the SUMMARY under a "## ALLOC-01 Baseline" section.
</output>
