---
phase: 08.7-legacy-print-html
plan: 05
subsystem: block-layout
tags: [float, clear, bfc, golden]
one_liner: "CSS 2.1 §9.5 BFC float accumulator in BlockLayoutEngine — left/right side-by-side placement + clear handling"
decisions:
  - "Simplified BFC accumulator (not full line-box shortening) — corpus uses floats only as two-column side-by-side pattern"
  - "Float state propagated from parent context (non-BFC) or reset to 0 (BFC root like table cells)"
  - "Container height encloses floated children (floatBottom > childY → childY = floatBottom)"
metrics:
  duration: ~5 minutes
  completed: 2026-05-28
  tasks_completed: 2
  files_created_or_modified: 8
  baselines_added: 2
key_files:
  created: []
  modified:
    - src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
    - src/Muonroi.Pdf/Internal/Layout/LayoutContext.cs
    - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
    - src/Muonroi.Pdf/Internal/Layout/BlockLayoutEngine.cs
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
    - tests/Muonroi.Pdf.Tests/Golden/BlockLayoutGoldenTests.cs
---

# Phase 08.7 Plan 05: float:left/right + clear:both Summary

## What Was Built

- `BoxNode.FloatValue` ("left"|"right"|null) and `BoxNode.ClearValue` ("left"|"right"|"both"|null)
- `LayoutContext`: 4 float accumulator fields (LeftFloatRight, RightFloatLeft, LeftFloatBottom, RightFloatBottom)
- `BoxTreeBuilder` reads `float` and `clear` CSS properties in general properties block
- `BlockLayoutEngine.Layout`: BFC reset or parent propagation of float state; clear handling advances childY; container height encloses floated children
- `BlockLayoutEngine.DispatchLayout`: float case saves/restores `CurrentY`, places box at left/right edge, returns 0f

## Baselines Added

| Case | Justification |
|------|---------------|
| `float-two-column` | New behavior: float:left + float:right renders side-by-side |
| `float-clear-below` | New behavior: clear:both + block below both floats |

## Test Results

**298 tests green** (was 294 after Plan 04).

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- Both new baseline PDFs exist in TestResources/Golden/
- Commit `d9f7cd4` verified in git log
