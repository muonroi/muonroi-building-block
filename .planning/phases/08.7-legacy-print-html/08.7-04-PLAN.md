---
phase: 08.7-legacy-print-html
plan: 04
type: execute
wave: 2
depends_on: [08.7-01, 08.7-02]
files_modified:
  - src/Muonroi.Pdf/Internal/Layout/Boxes/TableBox.cs
  - src/Muonroi.Pdf/Internal/Layout/Boxes/TableCellBox.cs
  - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
  - src/Muonroi.Pdf/Internal/Layout/TableLayoutEngine.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/TableGoldenTests.cs
autonomous: true
requirements: [SC-3, SC-4, SC-5]

must_haves:
  truths:
    - "border-collapse:collapse sets borderSpacing=0 in TableLayoutEngine so cells have no gap"
    - "vertical-align:top positions cell content at the top of the cell (current behavior confirmed correct)"
    - "vertical-align:middle positions cell content at the vertical center of the cell"
    - "vertical-align:bottom positions cell content at the bottom of the cell"
    - "Golden tests for border-collapse and vertical-align are baselined and passing"
  artifacts:
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/TableBox.cs"
      provides: "BorderCollapse string? property"
      contains: "BorderCollapse"
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/TableCellBox.cs"
      provides: "VerticalAlign string? property"
      contains: "VerticalAlign"
  key_links:
    - from: "BoxTreeBuilder.ResolveCssProperties"
      to: "TableBox.BorderCollapse"
      via: "reads 'border-collapse' CSS property into TableBox"
      pattern: "border-collapse"
    - from: "TableLayoutEngine.Layout"
      to: "borderSpacing = 0f"
      via: "conditional when table.BorderCollapse == 'collapse'"
      pattern: "BorderCollapse.*collapse"
    - from: "TableLayoutEngine final pass"
      to: "cellContentStartY"
      via: "vertical-align offset applied after MeasureCell returns contentHeight"
      pattern: "VerticalAlign"
---

<objective>
Implement border-collapse:collapse and vertical-align in table cells — the two highest-impact
table rendering gaps in the corpus (border-collapse: 10/17 templates; vertical-align: 673 hits
across all templates).

Purpose: Depends on Plan 01 (table engine stable) and Plan 02 (LegacyPrintPolicy allows
border-collapse:collapse). Both features live entirely within TableLayoutEngine and the Box
model — no changes to BlockLayoutEngine or InlineLayoutEngine needed.

Output: Updated TableBox.cs, TableCellBox.cs, BoxTreeBuilder.cs, TableLayoutEngine.cs +
golden tests for both features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.7-legacy-print-html/08.7-RESEARCH.md
@.planning/phases/08.7-legacy-print-html/08.7-01-SUMMARY.md

Key source files (read these, do not explore further):
@src/Muonroi.Pdf/Internal/Layout/Boxes/TableBox.cs
@src/Muonroi.Pdf/Internal/Layout/Boxes/TableCellBox.cs
@src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
@src/Muonroi.Pdf/Internal/Layout/TableLayoutEngine.cs
@tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
</context>

<interfaces>
<!-- Current TableBox (before this plan) -->
internal sealed class TableBox : BoxNode
{
    public string TableLayout { get; set; } = "auto";
    public float BorderSpacing { get; set; }
    // ADD: public string? BorderCollapse { get; set; }
}

<!-- Current TableCellBox (before this plan) -->
internal sealed class TableCellBox : BoxNode
{
    public int Colspan { get; set; } = 1;
    public int Rowspan { get; set; } = 1;
    public int ColumnIndex { get; set; }
    // ADD: public string? VerticalAlign { get; set; }
}

<!-- BoxTreeBuilder.ResolveCssProperties, TableBox branch (existing, add to it) -->
else if (box is TableBox table)
{
    var tableLayout = style.GetValue("table-layout");
    if (!string.IsNullOrWhiteSpace(tableLayout)) table.TableLayout = tableLayout;
    table.BorderSpacing = ParseLength(style.GetValue("border-spacing"), fontSize);
    // ADD: table.BorderCollapse = style.GetValue("border-collapse")?.ToLowerInvariant();
}

<!-- BoxTreeBuilder.ResolveCssProperties, TableCellBox branch (existing, add to it) -->
else if (box is TableCellBox cell)
{
    // colspan/rowspan from attributes (existing)
    // ADD: cell.VerticalAlign = style.GetValue("vertical-align")?.ToLowerInvariant();
}

<!-- TableLayoutEngine.Layout: borderSpacing currently read from table.BorderSpacing -->
// After: float borderSpacing = table.BorderSpacing;
// ADD: if (table.BorderCollapse == "collapse") borderSpacing = 0f;

<!-- TableLayoutEngine final pass: after computing cellHeight, before calling _blockEngine.Layout -->
// cellContentStartY computed from VerticalAlign:
// top:    cellContentStartY = cellY + cell.PaddingTop          (current)
// middle: cellContentStartY = cellY + (cellHeight - contentHeight) / 2
// bottom: cellContentStartY = cellY + cellHeight - contentHeight - cell.PaddingBottom
// contentHeight = MeasureCell(cell, cellWidth, context) — measurement pass already exists
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BorderCollapse + VerticalAlign properties and wire in BoxTreeBuilder</name>
  <files>
    src/Muonroi.Pdf/Internal/Layout/Boxes/TableBox.cs,
    src/Muonroi.Pdf/Internal/Layout/Boxes/TableCellBox.cs,
    src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
  </files>
  <behavior>
    - BoxTreeBuilder.ResolveCssProperties sets TableBox.BorderCollapse = "collapse" when the CSS "border-collapse" computed value is "collapse".
    - BoxTreeBuilder.ResolveCssProperties sets TableCellBox.VerticalAlign from the CSS "vertical-align" computed value when non-empty.
    - When "border-collapse" is absent or "separate", TableBox.BorderCollapse is null (unchanged from default).
    - When "vertical-align" is absent, TableCellBox.VerticalAlign is null (top alignment applied by default in TableLayoutEngine).
  </behavior>
  <action>
  In TableBox.cs, add: public string? BorderCollapse { get; set; }
  In TableCellBox.cs, add: public string? VerticalAlign { get; set; }

  In BoxTreeBuilder.ResolveCssProperties, in the "else if (box is TableBox table)" branch, after the
  existing table.BorderSpacing = ParseLength(...) line, add:
    var borderCollapseVal = style.GetValue("border-collapse");
    if (!string.IsNullOrEmpty(borderCollapseVal))
        table.BorderCollapse = borderCollapseVal.Trim().ToLowerInvariant();

  In BoxTreeBuilder.ResolveCssProperties, in the "else if (box is TableCellBox cell)" branch, after
  the existing colspan/rowspan attribute reading, add:
    var vAlign = style.GetValue("vertical-align");
    if (!string.IsNullOrEmpty(vAlign))
        cell.VerticalAlign = vAlign.Trim().ToLowerInvariant();

  Note: "vertical-align" is already read for InlineBox in the existing code. The TableCellBox branch
  is separate — do not touch the InlineBox branch.
  </action>
  <verify>
    <automated>dotnet build src/Muonroi.Pdf/Muonroi.Pdf.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Project builds; TableBox has BorderCollapse property; TableCellBox has VerticalAlign property.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement border-collapse + vertical-align in TableLayoutEngine + golden tests</name>
  <files>
    src/Muonroi.Pdf/Internal/Layout/TableLayoutEngine.cs,
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs,
    tests/Muonroi.Pdf.Tests/Golden/TableGoldenTests.cs
  </files>
  <behavior>
    - A table with border-collapse:collapse renders with borderSpacing=0 (no gap between cells).
    - A table with border-collapse:separate (or default) retains its existing borderSpacing behavior.
    - A table cell with vertical-align:top has its content placed at cellY + PaddingTop (same as current behavior — this is the default).
    - A table cell with vertical-align:middle has its content placed at cellY + (cellHeight - contentHeight) / 2; for a 50px cell with 10px content, content starts at 20px from cellY.
    - A table cell with vertical-align:bottom has its content placed at cellY + cellHeight - contentHeight - PaddingBottom.
    - Golden test "table-border-collapse" renders without exception and produces a non-empty PDF.
    - Golden test "table-vertical-align-top" renders without exception.
    - Golden test "table-vertical-align-middle" renders without exception.
  </behavior>
  <action>
  In TableLayoutEngine.Layout, immediately after "float borderSpacing = table.BorderSpacing;", add:
    if (string.Equals(table.BorderCollapse, "collapse", StringComparison.OrdinalIgnoreCase))
        borderSpacing = 0f;

  In TableLayoutEngine final pass (the foreach cell loop that calls _blockEngine.Layout):
  After computing cellHeight and before calling CellContext / _blockEngine.Layout, compute the
  vertical alignment offset:
    float contentHeight = MeasureCell(cell, cellWidth, context);
    float vAlignOffset = cell.VerticalAlign switch
    {
        "middle" => MathF.Max(0f, (cellHeight - contentHeight) / 2f),
        "bottom" => MathF.Max(0f, cellHeight - contentHeight - cell.PaddingBottom),
        _ => 0f   // "top" is default; any other value falls to top
    };
    var cellCtx = CellContext(context, cellWidth, cellY + cell.PaddingTop + vAlignOffset);

  Note: MeasureCell is already called in Pass 1 and Pass 2 for height measurement. The additional
  call here for alignment is a third measurement pass per cell. This is acceptable for Phase 8.7
  scope — optimization via caching is a Phase 9 concern. The corpus has 5308 td elements across
  17 templates; the extra pass is measurable but within the render timeout.

  In GoldenCorpus.cs, add 3 new entries to TableCases (or create a new TableExtendedCases list
  following the existing MemberData pattern):
    1. "table-border-collapse": A 2-column table with border-collapse:collapse and border-left:1px
       solid on each cell. Assert: renders without exception.
    2. "table-vertical-align-top": A single cell with height:50px, vertical-align:top, text "A".
    3. "table-vertical-align-middle": A single cell with height:50px, vertical-align:middle, text "B".

  In TableGoldenTests.cs, wire the new cases to the existing [Theory] or add a new [Theory] —
  follow the existing pattern exactly. Baseline on first run (GoldenPdf.VerifyAsync writes the
  .golden file if absent).
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests --filter "FullyQualifiedName~TableGolden" 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All TableGoldenTests pass (including 3 new cases); no IndexOutOfRangeException; no regression in pre-existing table tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS border-collapse → TableLayoutEngine | borderSpacing=0 is a layout parameter; cannot produce active content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.7-07 | DoS | vertical-align MeasureCell (3rd pass) | accept | Bounded by MaxElementCount (100k); 3× cell measurement still within 15s timeout for corpus scale |
| T-08.7-08 | Tampering | border-collapse:collapse | accept | Only reduces borderSpacing to 0; cannot escape containing block or inject content |
| T-08.7-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
dotnet test tests/Muonroi.Pdf.Tests --filter "FullyQualifiedName~Table" 2>&amp;1 | tail -15
Expected: all table tests pass; 3 new golden cases baselined.
</verification>

<success_criteria>
- TableBox.BorderCollapse property exists
- TableCellBox.VerticalAlign property exists
- TableLayoutEngine.Layout applies borderSpacing=0 when BorderCollapse=="collapse"
- TableLayoutEngine final pass applies vAlignOffset to cellContentStartY
- 3 new golden cases baselined and passing
- All pre-existing table tests green
</success_criteria>

<output>
Create .planning/phases/08.7-legacy-print-html/08.7-04-SUMMARY.md when done.
</output>
