---
phase: 08.7-legacy-print-html
plan: 06
subsystem: block-layout
tags: [position, absolute, containing-block, golden]
one_liner: "CSS 2.1 §9.6 position:absolute deferred-pass in BlockLayoutEngine with ContainingBlockRect resolution"
decisions:
  - "deferredAbsPos parameter added to DispatchLayout (option a from plan); avoids thread-safety issues of a field approach"
  - "Percentage top/left/right resolved against ContainingBlockRect dimensions at layout time"
  - "abs-pos fallback when no ContainingBlockRect: use page margin + available width as synthetic containing block"
metrics:
  duration: ~5 minutes
  completed: 2026-05-28
  tasks_completed: 2
  files_created_or_modified: 8
  baselines_added: 2
key_files:
  created:
    - tests/Muonroi.Pdf.Tests/Golden/PositionedLayoutGoldenTests.cs
  modified:
    - src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
    - src/Muonroi.Pdf/Internal/Layout/LayoutContext.cs
    - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
    - src/Muonroi.Pdf/Internal/Layout/BlockLayoutEngine.cs
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
---

# Phase 08.7 Plan 06: position:absolute deferred-pass Summary

## What Was Built

- `BoxNode.Position`, `TopRaw`, `LeftRaw`, `RightRaw` string? properties
- `LayoutContext.ContainingBlockRect` (`Rect?`) — null = no positioned ancestor
- `BoxTreeBuilder` reads `position`, `top`, `left`, `right` CSS properties (raw strings for % deferral)
- `BlockLayoutEngine.Layout`: detects `position:relative` containing block (box.Width>0 && box.Height>0), saves/restores `ContainingBlockRect`
- `BlockLayoutEngine.DispatchLayout`: abs-pos elements collected in `deferredAbsPos` list, return 0f (no childY advance)
- Post-loop abs-pos resolution: `ResolvePositionOffset` handles px/pt/mm/%, NaN = not specified
- `PositionedLayoutGoldenTests.cs` with `GoldenCorpus.PositionedLayout` (2 cases)

## Baselines Added

| Case | Justification |
|------|---------------|
| `abs-pos-image` | New behavior: position:absolute element placed at containing block + offsets |
| `abs-pos-percent-top` | New behavior: top:50% resolved against containing block height |

## Test Results

**302 tests green** (was 298 after Plan 05).

## Deviations from Plan

None — plan executed exactly as written. The `deferredAbsPos` parameter approach (option a) was implemented as specified.

## Self-Check: PASSED

- Both new baseline PDFs exist in TestResources/Golden/
- PositionedLayoutGoldenTests.cs exists
- Commit `04d3540` verified in git log
