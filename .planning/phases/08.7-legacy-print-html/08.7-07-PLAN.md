---
phase: 08.7-legacy-print-html
plan: 07
type: execute
wave: 4
depends_on: [08.7-05, 08.7-06]
files_modified:
  - src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
  - src/Muonroi.Pdf/Internal/Layout/Boxes/InlineBox.cs
  - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
  - src/Muonroi.Pdf/Internal/Layout/InlineLayoutEngine.cs
  - src/Muonroi.Pdf/Internal/Writer/OwnedPdfWriter.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/InlineLayoutGoldenTests.cs
  - tests/Muonroi.Pdf.Tests/Golden/FidelityGoldenTests.cs
autonomous: true
requirements: [SC-1, SC-2, SC-3]

must_haves:
  truths:
    - "background-color on a block or cell is filled as a solid PDF rectangle before text is drawn"
    - "background-image:url(data:image/png;base64,...) on a div renders the PNG at the div's position (barcode divs)"
    - "text-transform:uppercase uppercases text at measurement time (not only at render time) to avoid width mismatch"
    - "white-space:pre-line preserves \n as an explicit line break but collapses horizontal whitespace"
    - "white-space:pre-wrap preserves both \n and space sequences; each \n forces a CommitLine"
    - "<nobr> content is treated as a single unbreakable inline token (no word-splitting at internal spaces)"
    - "rem units parse as 16px * factor (0.5rem = 8px equivalent in pt)"
  artifacts:
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs"
      provides: "BackgroundColor, BackgroundImageSrc string? properties"
      contains: "BackgroundColor"
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/InlineBox.cs"
      provides: "TextTransform, WhiteSpace string? properties"
      contains: "TextTransform"
  key_links:
    - from: "OwnedPdfWriter"
      to: "DrawRect (background-color fill)"
      via: "checks PositionedElement.Source.BackgroundColor before text/image drawing"
      pattern: "BackgroundColor"
    - from: "InlineLayoutEngine"
      to: "word.ToUpperInvariant()"
      via: "text-transform applied before GetCharWidth measurement"
      pattern: "TextTransform.*uppercase"
---

<objective>
Close the remaining visual completeness gaps: background-color fill, background-image data-URI
on block elements, text-transform/white-space/nobr/rem. These are Wave 4 (depends on float +
abs-pos from Wave 3 to be stable before visual concerns).

Two concerns in one plan because both are "read CSS property → Box property → writer/layout
consumer" patterns with no inter-dependency between each other.

Output: Background drawing in OwnedPdfWriter + text/whitespace hardening in InlineLayoutEngine
+ golden tests for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.7-legacy-print-html/08.7-RESEARCH.md
@.planning/phases/08.7-legacy-print-html/08.7-05-SUMMARY.md
@.planning/phases/08.7-legacy-print-html/08.7-06-SUMMARY.md

Key source files (read these first — Plans 05/06 will have modified BoxNode and BoxTreeBuilder):
@src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
@src/Muonroi.Pdf/Internal/Layout/Boxes/InlineBox.cs
@src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
@src/Muonroi.Pdf/Internal/Layout/InlineLayoutEngine.cs
@src/Muonroi.Pdf/Internal/Writer/OwnedPdfWriter.cs
@tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
@tests/Muonroi.Pdf.Tests/Golden/InlineLayoutGoldenTests.cs
</context>

<interfaces>
<!-- BoxNode additions (after Plans 05+06 additions) -->
public string? BackgroundColor { get; set; }        // e.g. "#CCCCCC"
public string? BackgroundImageSrc { get; set; }     // data URI string from background-image

<!-- InlineBox additions -->
public string? TextTransform { get; set; }   // "uppercase" | null
public string? WhiteSpace { get; set; }      // "pre-wrap" | "pre-line" | null

<!-- OwnedPdfWriter: add background rendering BEFORE text/image content for each PositionedElement -->
// Pseudo-code:
//   if (element.Source.BackgroundColor is not null)
//       FillRect(element.Position, ParseHexColor(element.Source.BackgroundColor));
//   if (element.Source.BackgroundImageSrc is not null)
//       DrawBackgroundImage(element.Position, element.Source.BackgroundImageSrc, resolvedImages);

<!-- InlineLayoutEngine: text-transform applied at word measurement time -->
// Before: string word = rawWord;
// After:  string word = (inlineBox.TextTransform == "uppercase") ? rawWord.ToUpperInvariant() : rawWord;
// Then: width = metrics.GetCharWidth(word, ...)  — uses the transformed word

<!-- InlineLayoutEngine: white-space pre-wrap/pre-line newline handling -->
// When whiteSpace is "pre-wrap" or "pre-line":
//   Split text on '\n' to get logical lines.
//   Each '\n' forces CommitLine().
//   For pre-line: horizontal spaces are still collapsed (use StringSplitOptions.RemoveEmptyEntries for spaces).
//   For pre-wrap: preserve spaces (include empty entries in space split).

<!-- BoxTreeBuilder.CreateBox: nobr element -->
// Add before the display-based switch:
// if (localName == "nobr")
//     return new InlineBox { Source = node, Text = node.TextContent };
// Note: TextContent includes all internal spaces — treated as one unbreakable token.

<!-- BoxTreeBuilder.ParseLength: rem units -->
// Add before the "bare number" fallback:
// if (span.EndsWith("rem", StringComparison.Ordinal))
//     return TryParseFloat(span[..^3]) * 16f * (float)Units.PxToPt;
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Background-color + background-image drawing in BoxNode/BoxTreeBuilder/OwnedPdfWriter</name>
  <files>
    src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs,
    src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs,
    src/Muonroi.Pdf/Internal/Writer/OwnedPdfWriter.cs,
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs,
    tests/Muonroi.Pdf.Tests/Golden/FidelityGoldenTests.cs
  </files>
  <behavior>
    - A block div with background-color:#CCCCCC renders a grey filled rectangle at its position before any text content.
    - A div with background-image:url(data:image/png;base64,...) renders the PNG image at the div's position/dimensions.
    - A div with neither background-color nor background-image renders identically to pre-existing behavior (no regression).
    - Golden test "background-color-block" produces a non-empty PDF with the fill.
    - Golden test "background-image-data-uri" produces a non-empty PDF with the image.
  </behavior>
  <action>
  BoxNode.cs: Add after the existing border properties:
    public string? BackgroundColor { get; set; }
    public string? BackgroundImageSrc { get; set; }

  BoxTreeBuilder.ResolveCssProperties: In the general properties block (after float/clear/position from Plans 05+06):
    var bgColor = style.GetValue("background-color");
    if (!string.IsNullOrEmpty(bgColor) && bgColor != "transparent" && bgColor != "initial")
        box.BackgroundColor = bgColor.Trim();

    var bgImage = style.GetValue("background-image");
    if (!string.IsNullOrEmpty(bgImage) && bgImage.Contains("data:") && bgImage.Contains("base64"))
    {
        // Extract the data URI from background-image: url(data:...)
        int start = bgImage.IndexOf("url(", StringComparison.OrdinalIgnoreCase) + 4;
        int end = bgImage.LastIndexOf(')');
        if (start > 4 && end > start)
        {
            string uri = bgImage[start..end].Trim().Trim('\'', '"');
            if (uri.StartsWith("data:", StringComparison.Ordinal))
                box.BackgroundImageSrc = uri;
        }
    }

  OwnedPdfWriter: Before the existing element rendering logic for each PositionedElement,
  add background rendering. Read OwnedPdfWriter to understand where PositionedElement rendering
  happens (the main render loop). Insert background drawing BEFORE text/image content drawing:

    if (element.Source?.BackgroundColor is { } bgColor and { Length: > 0 })
    {
        // Parse hex color (#RGB or #RRGGBB) and emit a PDF filled rectangle (re q ... Q).
        // Use the existing helper for parsing hex colors if one exists (check writer for ParseHexColor
        // or similar). If not, implement inline: split 6 hex digits → r/g/b floats (0-1 range).
        // PDF operator: "r g b rg" (fill color) + "x y w h re f" (fill rect).
    }

    if (element.Source?.BackgroundImageSrc is { } bgSrc and { Length: > 0 })
    {
        // Decode the data URI via DataUriDecoder + PureImageDecoder (already used for <img> elements).
        // Emit as an XObject image at element.Position (same path as ReplacedBox image drawing).
        // Reuse the existing image XObject registration path.
    }

  For FidelityGoldenTests.cs (or add to GoldenCorpus), add 2 entries:
    1. "background-color-block": A div with background-color:#EEEEEE containing text "HELLO".
    2. "background-image-data-uri": A div with background-image:url(data:image/png;base64,{TinyRgbPngBase64})
       at width:50px, height:50px.
  Use the TinyRgbPngBase64 constant already established in RealTemplateBaselineTests.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests --filter "FullyQualifiedName~FidelityGolden|FullyQualifiedName~background" 2>&amp;1 | tail -15</automated>
  </verify>
  <done>2 new fidelity golden cases baselined; no regression in pre-existing fidelity tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: text-transform + white-space + nobr + rem in BoxTreeBuilder and InlineLayoutEngine</name>
  <files>
    src/Muonroi.Pdf/Internal/Layout/Boxes/InlineBox.cs,
    src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs,
    src/Muonroi.Pdf/Internal/Layout/InlineLayoutEngine.cs,
    tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs,
    tests/Muonroi.Pdf.Tests/Golden/InlineLayoutGoldenTests.cs
  </files>
  <behavior>
    - An InlineBox with TextTransform="uppercase" renders its text as uppercase; the word width measured by GetCharWidth uses the uppercased string (no measurement vs rendering mismatch per Pitfall 5).
    - An InlineBox with WhiteSpace="pre-line" treats \n in the text as an explicit line break; horizontal spaces are still collapsed.
    - An InlineBox with WhiteSpace="pre-wrap" treats \n as explicit line break AND preserves space sequences.
    - A <nobr> element renders its full TextContent (including internal spaces) as a single unbreakable token — no word-split at spaces.
    - 0.5rem parses to 8px * PxToPt (= 6.0pt approximately).
    - Golden tests for text-transform, pre-line, and nobr produce non-empty PDFs.
  </behavior>
  <action>
  InlineBox.cs: Add after existing TextDecoration/LinkHref properties:
    public string? TextTransform { get; set; }   // "uppercase" | null
    public string? WhiteSpace { get; set; }       // "pre-wrap" | "pre-line" | null

  BoxTreeBuilder.ResolveCssProperties, in the InlineBox branch:
    var textTransform = style.GetValue("text-transform");
    if (!string.IsNullOrEmpty(textTransform) && textTransform == "uppercase")
        inline.TextTransform = "uppercase";

    var whiteSpace = style.GetValue("white-space");
    if (!string.IsNullOrEmpty(whiteSpace) && whiteSpace is "pre-wrap" or "pre-line")
        inline.WhiteSpace = whiteSpace;

  BoxTreeBuilder.CreateBox: Add before the "display" switch block:
    if (localName == "nobr")
        return new InlineBox { Source = node, Text = node.TextContent };
    // TextContent includes internal spaces — InlineLayoutEngine must not split on them.
    // To signal no-split: we could set a flag, or rely on the word not containing the separator
    // chars. Use a dedicated property on InlineBox if needed.
    // Simpler: set WhiteSpace="nowrap" on the nobr InlineBox (treat as a no-break token in InlineLayoutEngine).

  InlineLayoutEngine: Read the file before modifying. Find the word-splitting logic
  (StringSplitOptions.RemoveEmptyEntries split on WordSeparators). Modify as follows:

  For text-transform: After splitting raw text into words, for each word:
    string renderedWord = (inlineBox.TextTransform == "uppercase")
        ? word.ToUpperInvariant() : word;
    // Use renderedWord for BOTH GetCharWidth measurement and PDF text output.

  For white-space pre-wrap/pre-line: Before the word-split logic, check WhiteSpace:
    if (inlineBox.WhiteSpace is "pre-wrap" or "pre-line")
    {
        // Split on newlines first to find logical lines.
        string[] logicalLines = text.Split('\n');
        foreach (string line in logicalLines)
        {
            // For pre-line: collapse spaces, split on space/tab.
            // For pre-wrap: preserve spaces (include spaces as tokens or keep them in words).
            // Process each logical line's words normally, then call CommitLine() after each.
        }
        return; // exit the normal word-split path
    }

  For nobr (WhiteSpace="nowrap"): Treat the entire InlineBox.Text as a single token — do not
  split on any separator. Measure the full text width; if it exceeds the line width, wrap to
  the next line as a unit (or overflow if it exceeds page width — acceptable for corpus use case).

  BoxTreeBuilder.ParseLength: Add before the bare-number fallback at the end:
    if (span.EndsWith("rem", StringComparison.Ordinal))
        return TryParseFloat(span[..^3]) * 16f * (float)Units.PxToPt;

  In GoldenCorpus.cs, add 3 InlineLayout cases:
    1. "inline-text-transform-uppercase": A div with text-transform:uppercase containing "hello world".
    2. "inline-whitespace-pre-line": A td with white-space:pre-line containing "line1\nline2".
    3. "inline-nobr": A div containing &lt;nobr&gt;no break here&lt;/nobr&gt; with a narrow container.

  In InlineLayoutGoldenTests.cs, wire the new cases to the existing [Theory] (or add a new one).
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests --filter "FullyQualifiedName~InlineLayoutGolden" 2>&amp;1 | tail -15</automated>
  </verify>
  <done>3 new inline layout golden cases baselined; all pre-existing inline tests pass; rem parses correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS background-color hex → OwnedPdfWriter | Hex color parsed to r/g/b floats; no eval/exec |
| background-image data-URI → PureImageDecoder | Same decode path as img elements; existing RGBA/palette guards apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.7-13 | Tampering | background-image data-URI decode | mitigate | PureImageDecoder already rejects RGBA, palette, grayscale PNG (fail-loud); same path as img elements |
| T-08.7-14 | DoS | nobr with very long text | accept | MaxElementCount bounds the number of nobr elements; a single nobr overflowing line width is accepted (page width clamp in writer) |
| T-08.7-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
dotnet test tests/Muonroi.Pdf.Tests --filter "FullyQualifiedName~Inline|FullyQualifiedName~Fidelity" 2>&amp;1 | tail -15
Expected: all inline + fidelity tests pass; 5 new golden cases baselined.
</verification>

<success_criteria>
- BoxNode.BackgroundColor + BackgroundImageSrc properties exist
- InlineBox.TextTransform + WhiteSpace properties exist
- OwnedPdfWriter fills background-color rect before text drawing
- OwnedPdfWriter renders background-image data-URI as image XObject
- text-transform applied at measurement time (not only render time)
- white-space:pre-line/pre-wrap treat \n as CommitLine trigger
- nobr treated as single unbreakable token
- rem units parse as 16px factor
- 5 new golden cases baselined; all pre-existing tests green
</success_criteria>

<output>
Create .planning/phases/08.7-legacy-print-html/08.7-07-SUMMARY.md when done.
</output>
