---
phase: 04-font-image-pipeline
plan: 06
type: execute
wave: 5
depends_on:
  - 04-05
files_modified:
  - tests/Muonroi.Pdf.Tests/Font/FontPipelineTests.cs
  - tests/Muonroi.Pdf.Tests/Font/VietnameseDiacriticTests.cs
  - tests/Muonroi.Pdf.Tests/Image/ImagePipelineTests.cs
  - tests/Muonroi.Pdf.Tests/Font/TrueTypeFontSubsetterTests.cs
  - tests/Muonroi.Pdf.Tests/TestResources/TestFont.ttf
autonomous: true
requirements:
  - FONT-01
  - FONT-02
  - FONT-03
  - FONT-04
  - FONT-05
  - FONT-06
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04
  - IMG-05

must_haves:
  truths:
    - "FontPipelineTests verifies MaxFontFiles enforcement, skipped null resolver, and successful FontCollection build"
    - "VietnameseDiacriticTests verifies SixLaborsTextMetrics returns positive width for Vietnamese precomposed characters"
    - "ImagePipelineTests verifies PNG/JPEG header parsing, data: URI decode, MaxImagePixels rejection, and external src routing"
    - "TrueTypeFontSubsetterTests verifies CFF-OTF passthrough and TTF subset is smaller than the original"
    - "dotnet test exits 0 for all Phase 4 tests"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Font/FontPipelineTests.cs"
      provides: "xUnit tests for FontPipeline and SixLaborsTextMetrics"
      contains: "FontPipelineTests"
    - path: "tests/Muonroi.Pdf.Tests/Font/VietnameseDiacriticTests.cs"
      provides: "xUnit tests verifying Vietnamese precomposed characters measure correctly"
      contains: "VietnameseDiacriticTests"
    - path: "tests/Muonroi.Pdf.Tests/Image/ImagePipelineTests.cs"
      provides: "xUnit tests for DataUriDecoder, PureImageDecoder, and ImagePipeline"
      contains: "ImagePipelineTests"
    - path: "tests/Muonroi.Pdf.Tests/Font/TrueTypeFontSubsetterTests.cs"
      provides: "xUnit tests for TrueTypeFontSubsetter"
      contains: "TrueTypeFontSubsetterTests"
---

<objective>
Write unit tests covering all Phase 4 requirements: FONT-01 through FONT-06 and IMG-01 through IMG-05. Tests use NSubstitute for mocking `IFontResolver` and `IResourceResolver`. A small open-source TTF (NotoSans-Regular subset or synthesized minimal TTF) is embedded as a test resource for subsetter and Vietnamese diacritic tests.

Purpose: Regression-lock the font and image pipeline so Phase 5 changes cannot silently break these subsystems.

Output: 4 test files + test resource font
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- Test project auto-receives xunit + FluentAssertions 7.2.0 + NSubstitute via Directory.Build.props -->
<!-- FluentAssertions is pinned at 7.2.0 — do NOT upgrade -->

<!-- NSubstitute usage pattern -->
// IFontResolver resolver = Substitute.For<IFontResolver>();
// resolver.ResolveAsync(Arg.Any<FontRequest>(), Arg.Any<CancellationToken>())
//     .Returns(new ReadOnlyMemory<byte>(bytes));

<!-- PNG minimal test data (1x1 pixel) -->
// Magic: 89 50 4E 47 0D 0A 1A 0A
// IHDR chunk: 00 00 00 0D "IHDR" {width:4BE} {height:4BE} {bitdepth} {colortype} {compression} {filter} {interlace} {crc:4}
// IDAT chunk: ...
// IEND chunk: 00 00 00 00 "IEND" {crc}
// For width/height tests, a minimal valid PNG is not required — just need bytes with correct magic + IHDR offsets

<!-- JPEG minimal test data -->
// SOI: FF D8
// APP0 (JFIF): FF E0 {len:2} "JFIF\0" ...
// SOF0: FF C0 {len:2} {precision:1} {height:2BE} {width:2BE} {components:1} ...
// The simplest valid JPEG SOF segment for testing purposes

<!-- InlineBox test setup requires IStyledDocument stub -->
// For GlyphCollector/LayoutEngine tests: can build PositionedPageList directly
// and populate with InlineBox elements using NSubstitute IStyledNode stubs

<!-- FontCollection test setup -->
// Needs real TTF bytes. Download NotoSans-Regular.ttf (Apache 2.0) or embed a minimal TTF.
// Minimal TTF can be generated synthetically for basic tests (without full glyph data).

<!-- TrueTypeFontSubsetter test needs a real TTF binary -->
// Use NotoSans-Regular.ttf or similar (Apache 2.0 license).
// For the subsetter test: any font with at least ASCII glyphs will work.

<!-- Test resource embedding in csproj -->
// <ItemGroup>
//   <EmbeddedResource Include="TestResources/**" />
// </ItemGroup>
// Access: Assembly.GetExecutingAssembly().GetManifestResourceStream("Muonroi.Pdf.Tests.TestResources.TestFont.ttf")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FontPipelineTests + VietnameseDiacriticTests + test font resource</name>
  <files>
    tests/Muonroi.Pdf.Tests/Font/FontPipelineTests.cs
    tests/Muonroi.Pdf.Tests/Font/VietnameseDiacriticTests.cs
    tests/Muonroi.Pdf.Tests/TestResources/TestFont.ttf
    tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj
  </files>
  <action>
**Step 0 — Obtain test font:**
Download NotoSans-Regular.ttf (Apache 2.0 license) for use as the test font:
```
curl -L -o "tests/Muonroi.Pdf.Tests/TestResources/TestFont.ttf" \
  "https://github.com/notofonts/noto-fonts/raw/main/hinted/ttf/NotoSans/NotoSans-Regular.ttf"
```
If the download fails (no internet access), create a minimal synthetic TTF with:
- sfntVersion = 0x00010000
- numTables = 0 (minimal, enough to detect as TTF)
- Used only for CFF-OTF detection tests; subsetter tests require a real font with cmap/glyf/loca tables

**Update `tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj`:**
Add `<ItemGroup><EmbeddedResource Include="TestResources/**" /></ItemGroup>`

---

**FontPipelineTests.cs:**
Namespace: `Muonroi.Pdf.Tests.Font`
Test class: `public class FontPipelineTests`

Tests:
1. `MaxFontFiles_ExceededBeforeResolve_ThrowsPdfInputLimitException`:
   - Create `IStyledDocument` stub with `FontFaces` returning 33 `FontFaceDeclaration` records
   - Create `IFontResolver` mock
   - Create `FontPipeline`, call `ResolveAsync(doc, resolver, limits, ct)`
   - Assert throws `PdfInputLimitException` with code "FONT-MAX-FILES"
   - Verify resolver received NO calls: `resolver.DidNotReceive().ResolveAsync(Arg.Any<FontRequest>(), Arg.Any<CancellationToken>())`

2. `NullResolverResult_FontSkipped_NoException`:
   - Doc with 1 FontFaceDeclaration
   - Resolver returns `null` for any request
   - Call `ResolveAsync` — should complete without throwing
   - Returned `SixLaborsTextMetrics` should return non-zero estimate for unknown family (fallback)

3. `ValidTtfBytes_FontCollectionBuilt_MetricsNotEstimated`:
   - Load `TestFont.ttf` from embedded resource
   - Doc with 1 FontFaceDeclaration matching the NotoSans family name (read actual name from font or use "NotoSans")
   - Resolver returns the font bytes for any request
   - Call `ResolveAsync` — should complete
   - Assert returned `SixLaborsTextMetrics` returns width > 0 for `GetCharWidth('A', familyName, 12f, false, false)`

4. `MaxFontFiles_AtLimit_NoException`:
   - Doc with exactly 32 FontFaceDeclaration records (all unique families)
   - Resolver returns `null` for all (avoid font loading)
   - Call `ResolveAsync` — should NOT throw

---

**VietnameseDiacriticTests.cs:**
Namespace: `Muonroi.Pdf.Tests.Font`
Test class: `public class VietnameseDiacriticTests`

Prerequisite: `SixLaborsTextMetrics` built from a FontCollection loaded with `TestFont.ttf`.

Tests:
1. `VietnamesePrecomposed_CharWidth_Positive`:
   - Build `SixLaborsTextMetrics` with FontCollection containing TestFont.ttf (family name detected from font metadata, or hardcode "Noto Sans")
   - Call `GetCharWidth('ế', familyName, 12f, false, false)` where 'ế' is U+1EBF
   - Assert `width > 0` — font has the glyph, no replacement char width expected
   - Call `GetCharWidth('ệ', familyName, 12f, false, false)` where 'ệ' is U+1EB9
   - Assert `width > 0`

2. `MixedLatinVietnamese_LineHeight_Positive`:
   - `GetLineHeight(familyName, 12f)` returns a value > 0
   - `GetAscender(familyName, 12f)` returns a value > 0

3. `SurrogateChar_GlyphCollector_Skipped`:
   - Create a `PositionedPageList` with a `PositionedPage` containing a `PositionedElement` whose Source is an `InlineBox` with `Text = "𐀀"` (a high surrogate pair) and `FontFamily = familyName`
   - Build `FontCollection` with TestFont, call `GlyphCollector.Collect(pageList, collection)`
   - Assert no exception; the family entry should be empty or missing (surrogates skipped by `char.IsSurrogate` guard)

Note: When constructing `PositionedPage` and `PositionedElement` for tests — these are `internal` classes visible via `InternalsVisibleTo` (already set in `Muonroi.Pdf.csproj`). Construct directly.
  </action>
  <verify>
    <automated>dotnet build tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj --no-restore 2>&amp;1 | grep -E "^.*error" | head -20</automated>
  </verify>
  <done>Test project builds without errors; TestFont.ttf is embedded as a resource; FontPipelineTests and VietnameseDiacriticTests files exist with the specified test methods</done>
</task>

<task type="auto">
  <name>Task 2: ImagePipelineTests</name>
  <files>
    tests/Muonroi.Pdf.Tests/Image/ImagePipelineTests.cs
  </files>
  <action>
Create `tests/Muonroi.Pdf.Tests/Image/ImagePipelineTests.cs`:
Namespace: `Muonroi.Pdf.Tests.Image`
Test class: `public class ImagePipelineTests`

**PureImageDecoder tests:**

1. `Png_ValidIhdr_ReturnsCorrectDimensions`:
   - Build minimal PNG bytes with magic + IHDR encoding width=100, height=200 (no need for valid IDAT — decoder only reads header)
   - PNG header bytes (24 bytes minimum):
     ```
     { 0x89,0x50,0x4E,0x47,0x0D,0x0A,0x1A,0x0A,  // magic
       0x00,0x00,0x00,0x0D,0x49,0x48,0x44,0x52,  // length=13, "IHDR"
       0x00,0x00,0x00,0x64,                        // width=100
       0x00,0x00,0x00,0xC8 }                       // height=200
     ```
   - `PureImageDecoder decoder = new(); DecodedImage result = decoder.Decode(bytes, "image/png")`
   - `result.Width.Should().Be(100); result.Height.Should().Be(200)`

2. `Jpeg_ValidSof0_ReturnsCorrectDimensions`:
   - Build minimal JPEG bytes: SOI (FF D8) + APP0 dummy segment + SOF0 segment encoding width=320, height=240
   - SOF0 bytes at known position: FF C0 + length(2) + precision(1) + height BE uint16 + width BE uint16
   - Verify `PureImageDecoder` returns Width=320, Height=240

3. `Jpeg_ProgressiveSof2_FindsMarker`:
   - Build JPEG bytes with SOF2 (FF C2) marker (progressive JPEG)
   - Verify decoder returns correct width/height

4. `Png_InvalidMagic_ThrowsPdfException`:
   - Pass bytes with first byte = 0x00 (wrong magic)
   - Assert throws `PdfException` (or subtype)

5. `Png_TooShort_ThrowsPdfException`:
   - Pass byte array of length 10 (less than 24 required)
   - Assert throws

**DataUriDecoder tests:**

6. `DataUri_PngBase64_DecodesBytes`:
   - Create a small PNG byte array; base64 encode it
   - Call `DataUriDecoder.Decode($"data:image/png;base64,{base64str}")`
   - Assert returned bytes equal original PNG bytes; ContentType == "image/png"

7. `DataUri_WithWhitespace_StripAndDecode`:
   - Insert newlines in the base64 payload (e.g., every 76 chars)
   - Verify decode succeeds (whitespace stripped before FromBase64String)

8. `DataUri_MissingBase64Flag_ImageType_Throws`:
   - `DataUriDecoder.Decode("data:image/png,<non-base64-data>")`
   - Assert throws `PdfException` with code "IMG-FORMAT"

**ImagePipeline tests:**

9. `ExternalSrc_RoutedThroughResolver_NeverDirectNetwork`:
   - Create `IResourceResolver` mock returning a PNG-bytes result
   - Create `IStyledDocument` stub with root having one `<img src="http://example.com/img.png">`
   - Call `ImagePipeline.ResolveAsync(...)`
   - Verify `resolver.ResolveAsync` was called once with `Uri("http://example.com/img.png")`

10. `NullResolverResult_ImageSkipped_EmptyDictionary`:
    - Resolver returns null
    - Verify returned dictionary is empty (no exception)

11. `MaxImagePixels_Exceeded_ThrowsLimitException`:
    - Create `IImageDecoder` mock returning `DecodedImage(5001, 5000, ...)` — 25,005,000 pixels > 25,000,000
    - Verify `PdfInputLimitException` thrown with code "IMG-MAX-PIXELS"

12. `MaxImagePixels_AtBoundary_NoException`:
    - `IImageDecoder` mock returns `DecodedImage(5000, 5000, ...)` — exactly 25,000,000 pixels (at limit, not over)
    - Verify no exception (boundary is `>` not `>=`)

For creating `IStyledDocument` stubs: use `Substitute.For<IStyledDocument>()` and set up `.Root` to return a stubbed `IStyledNode` tree. The tree can be minimal: root node with one child `<img>` node.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/ --filter "FullyQualifiedName~ImagePipelineTests" --no-build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All ImagePipelineTests pass; `dotnet test --filter "ImagePipelineTests"` exits 0</done>
</task>

<task type="auto">
  <name>Task 3: TrueTypeFontSubsetterTests</name>
  <files>
    tests/Muonroi.Pdf.Tests/Font/TrueTypeFontSubsetterTests.cs
  </files>
  <action>
Create `tests/Muonroi.Pdf.Tests/Font/TrueTypeFontSubsetterTests.cs`:
Namespace: `Muonroi.Pdf.Tests.Font`
Test class: `public class TrueTypeFontSubsetterTests`

Helper: Load TestFont.ttf from embedded resource — create private static method `GetTestFontBytes()`:
```csharp
private static ReadOnlyMemory<byte> GetTestFontBytes()
{
    using Stream stream = typeof(TrueTypeFontSubsetterTests).Assembly
        .GetManifestResourceStream("Muonroi.Pdf.Tests.TestResources.TestFont.ttf")
        ?? throw new InvalidOperationException("TestFont.ttf embedded resource not found");
    using MemoryStream ms = new();
    stream.CopyTo(ms);
    return ms.ToArray();
}
```

Tests:

1. `CffOtf_PassthroughUnchanged`:
   - Build minimal CFF-OTF bytes: first 4 bytes = `{ 0x4F, 0x54, 0x54, 0x4F }` (sfntVersion 'OTTO')
   - `TrueTypeFontSubsetter subsetter = new(); ReadOnlyMemory<byte> result = subsetter.Subset(cffBytes, new HashSet<int> { 65, 66 })`
   - Assert `result.Length == cffBytes.Length`; result bytes equal cffBytes — passthrough

2. `UnrecognizedFormat_ThrowsPdfException`:
   - Pass 4 bytes = `{ 0x00, 0x02, 0x00, 0x00 }` (invalid sfntVersion)
   - Assert throws `PdfException` with code "FONT-FORMAT"

3. `TtfSubset_SmallerThanOriginal` (requires real TestFont.ttf):
   - `ReadOnlyMemory<byte> original = GetTestFontBytes()`
   - Subset to ASCII alphabet: `HashSet<int> asciiCodepoints = Enumerable.Range(65, 26).Concat(Enumerable.Range(97, 26)).ToHashSet()`
   - `ReadOnlyMemory<byte> subset = subsetter.Subset(original, asciiCodepoints)`
   - `subset.Length.Should().BeLessThan(original.Length)` — subsetting reduces size
   - Verify subset starts with TTF magic: `subset.Span[0..4]` == `{ 0x00, 0x01, 0x00, 0x00 }`

4. `TtfSubset_ValidTableDirectory` (requires real TestFont.ttf):
   - Subset with a few ASCII codepoints
   - Read `numTables` from output at offset 4
   - `numTables.Should().BeGreaterThan(0).And.BeLessThanOrEqualTo(50)` — sane range
   - Read sfntVersion from output: should be `0x00010000`

5. `MaxpNumGlyphs_UpdatedInSubset` (requires real TestFont.ttf):
   - Subset to just codepoints for 'A', 'B', 'C' (3 chars)
   - Find `maxp` table in output, read `numGlyphs` at offset 4
   - `numGlyphs.Should().BeLessThan(100)` — much smaller than original (NotoSans has ~2400+ glyphs)
   - `numGlyphs.Should().BeGreaterThan(0)` — at least .notdef + 3 used glyphs

Skip tests 3-5 with `Skip` attribute if TestFont.ttf is not available:
```csharp
[Fact(Skip = "Requires TestFont.ttf embedded resource")]  // remove Skip when resource is confirmed present
```
After Task 1 downloads the font, remove the `Skip` attribute.

Also verify all existing Phase 3 tests still pass:
`dotnet test tests/Muonroi.Pdf.Tests/ --no-build` at the end of this plan.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/ --no-build 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All Phase 3 and Phase 4 tests pass; `dotnet test` exits 0 for the entire Muonroi.Pdf.Tests project</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test resource bytes → test code | TestFont.ttf embedded resource — trusted (open-source Apache 2.0 font) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-SC | Tampering | Downloaded TestFont.ttf from network | mitigate | Font is Apache 2.0 (NotoFonts org on GitHub); SHA of downloaded file should match known-good; verify after download |
</threat_model>

<verification>
- `dotnet build tests/Muonroi.Pdf.Tests/` exits 0
- `dotnet test tests/Muonroi.Pdf.Tests/ --no-build` exits 0 (all Phase 3 + Phase 4 tests pass)
- Tests cover all 5 success criteria from ROADMAP Phase 4 (SC1-SC5)
- `FontPipelineTests.MaxFontFiles_ExceededBeforeResolve_ThrowsPdfInputLimitException` verifies resolver received NO calls before limit check
</verification>

<success_criteria>
- `dotnet test tests/Muonroi.Pdf.Tests/` exits 0 with all tests passing
- FONT-01 through FONT-06 covered (FontPipelineTests: resolver integration, MaxFontFiles, null skip; VietnameseDiacriticTests: precomposed char width)
- IMG-01 through IMG-05 covered (ImagePipelineTests: PNG/JPEG header parse, data URI, external resolver, pixel limit)
- FONT-03 subsetter covered: CFF-OTF passthrough, TTF subset smaller than original, maxp numGlyphs updated
- All Phase 3 tests remain green (original Layout() method unchanged)
</success_criteria>

<output>
Create `.planning/phases/04-font-image-pipeline/04-06-SUMMARY.md` when done
</output>
