---
phase: 03-box-tree-layout-engine
plan: 09
type: execute
wave: 1
depends_on: ["03-08"]
files_modified:
  - src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs
  - tests/Muonroi.Pdf.Governance.Tests/GlobalUsings.cs
  - tests/Muonroi.Pdf.Governance.Tests/Policies/DefaultStrictPolicyTests.cs
  - tests/Muonroi.Pdf.Tests/Layout/InlineLayoutTests.cs
  - KNOWN-DEVIATIONS.md
autonomous: true
requirements:
  - LAYOUT-07
  - LAYOUT-02

must_haves:
  truths:
    - "DefaultStrictPolicy.ValidateAsync() emits a PolicyViolation with RuleId 'forbidden.border-collapse.collapse' and SuggestedAlternative 'border-collapse:separate' for a document with border-collapse:collapse"
    - "dotnet test tests/Muonroi.Pdf.Governance.Tests exits 0 with at least 1 test passing"
    - "dotnet test tests/Muonroi.Pdf.Tests exits 0 with all 22 existing tests still passing plus the new Vietnamese test"
    - "KNOWN-DEVIATIONS.md contains KD-03-05 documenting that full UAX#14 line-breaking is deferred and space-based splitting is accepted for Phase 3"
  artifacts:
    - path: "src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs"
      provides: "border-collapse:collapse check in CheckCssFeatures() Pass 2"
      contains: "forbidden.border-collapse.collapse"
    - path: "tests/Muonroi.Pdf.Governance.Tests/Policies/DefaultStrictPolicyTests.cs"
      provides: "LAYOUT-07 regression test"
      exports: ["DefaultStrictPolicyTests"]
    - path: "KNOWN-DEVIATIONS.md"
      provides: "KD-03-05 entry"
      contains: "KD-03-05"
  key_links:
    - from: "DefaultStrictPolicy.CheckCssFeatures()"
      to: "border-collapse:collapse violation"
      via: "style.GetPropertyValue(\"border-collapse\") == \"collapse\" in Pass 2 element loop"
      pattern: "forbidden\\.border-collapse\\.collapse"
    - from: "DefaultStrictPolicyTests"
      to: "DefaultStrictPolicy.ValidateAsync()"
      via: "AngleSharpHtmlParser + AngleSharpCascadeEngine pipeline"
      pattern: "ParseAndCascade"
---

<objective>
Close two Phase 3 verification gaps:
- Gap 1 (BLOCKER): LAYOUT-07 unimplemented — DefaultStrictPolicy.CheckCssFeatures() has no check for border-collapse:collapse; Governance test project has 0 test methods.
- Gap 2 (WARNING): SC2 Vietnamese+Latin line-break test missing; KD-03-05 not documented.

Purpose: SC3 success criterion states "border-collapse:collapse triggers a PolicyViolation naming border-collapse:separate as the alternative." Gap 1 is a code defect — the policy gate never fires for this case. Gap 2 is a documentation+test gap confirming space-based splitting is correct and accepted for Phase 3.
Output: DefaultStrictPolicy fixed, governance test added, Vietnamese inline test added, KD-03-05 documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
From src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs:
The existing CheckCssFeatures() Pass 2 element loop (lines 96-118) checks:
  - display: flex/inline-flex → "forbidden.display.flex"
  - display: grid/inline-grid → "forbidden.display.grid"
  - float: left/right → "forbidden.float"
  - position: absolute → "forbidden.position.absolute"
  - position: fixed → "forbidden.position.fixed"
  - position: sticky → "forbidden.position.sticky"
MISSING: no check for border-collapse:collapse.

The ViolationFor() helper signature (line 121-128):
  private static PolicyViolation ViolationFor(string ruleId, string property, string value, string selector, string alternative)

From src/Muonroi.Pdf.Governance/Cascade/AngleSharpCascadeEngine.cs (public):
  public ValueTask&lt;IStyledDocument&gt; CascadeAsync(IParsedDocument doc, string? userStyleSheet, CancellationToken ct = default)
  Returns AngleSharpStyledDocument (internal), which implements both IStyledDocument AND IPdfDocumentContext.

From src/Muonroi.Pdf.Governance/Parsing/AngleSharpHtmlParser.cs (public):
  public ValueTask&lt;IParsedDocument&gt; ParseAsync(string html, CancellationToken ct = default)

ValidateAsync signature in IPdfCssPolicy:
  ValueTask&lt;PolicyValidationResult&gt; ValidateAsync(IPdfDocumentContext context, CancellationToken ct = default)
The result of CascadeAsync cast to IPdfDocumentContext is the correct argument.

From tests/Muonroi.Pdf.Tests/Layout/InlineLayoutEngine.cs (for reference):
  internal sealed InlineLayoutEngine — Layout(IEnumerable&lt;BoxNode&gt;, LayoutContext, List&lt;PositionedElement&gt;, int) : float
  WordSeparators = { ' ', '\t', '\n', '\r', U+200B }
  Each space-delimited token becomes one PositionedElement even on the same line.
  "Xin chào world" (3 space-separated tokens) → 3 PositionedElements.

From tests/Muonroi.Pdf.Tests/Layout/InlineLayoutTests.cs:
  MakeInlineBox(string text, float fontSize, string verticalAlign = "baseline") : InlineBox
  MakeContext(float availableWidth = 400f, float startY = 0f) : LayoutContext
  These helpers are already defined in the file and can be used by the new test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LAYOUT-07 in DefaultStrictPolicy + add governance test</name>
  <files>
    src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs,
    tests/Muonroi.Pdf.Governance.Tests/GlobalUsings.cs,
    tests/Muonroi.Pdf.Governance.Tests/Policies/DefaultStrictPolicyTests.cs
  </files>
  <action>
    In DefaultStrictPolicy.cs, inside CheckCssFeatures() Pass 2 element loop (after the position:sticky check at line 117), add a check:
    read the "border-collapse" property from the computed style using style.GetPropertyValue("border-collapse").
    If the value is "collapse", add a violation using ViolationFor() with:
      ruleId: "forbidden.border-collapse.collapse"
      property: "border-collapse"
      value: the actual value ("collapse")
      selector: the element selector (already computed as `selector`)
      alternative: "border-collapse:separate"

    In tests/Muonroi.Pdf.Governance.Tests/GlobalUsings.cs, append three global using directives:
      global using Muonroi.Pdf.Governance.Cascade;
      global using Muonroi.Pdf.Governance.Parsing;
      global using Muonroi.Pdf.Governance.Policies;

    Create tests/Muonroi.Pdf.Governance.Tests/Policies/DefaultStrictPolicyTests.cs in namespace Muonroi.Pdf.Governance.Tests.Policies.
    The file must contain a sealed test class DefaultStrictPolicyTests with one async helper ParseAndCascadeAsync(string html) that:
      1. Creates AngleSharpHtmlParser, calls ParseAsync(html)
      2. Creates AngleSharpCascadeEngine, calls CascadeAsync(parsed, null)
      3. Returns the IStyledDocument (which is also IPdfDocumentContext at runtime)
    The class must contain one [Fact] method: DefaultStrictPolicy_BorderCollapseCollapse_EmitsPolicyViolation.
    The test HTML must have a table with inline style "border-collapse:collapse" AND a stylesheet rule setting border-collapse:collapse.
    Use HTML: "&lt;html&gt;&lt;head&gt;&lt;style&gt;table { border-collapse: collapse; }&lt;/style&gt;&lt;/head&gt;&lt;body&gt;&lt;table&gt;&lt;tr&gt;&lt;td&gt;cell&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/body&gt;&lt;/html&gt;"
    Call DefaultStrictPolicy.ValidateAsync((IPdfDocumentContext)doc) after ParseAndCascadeAsync.
    Assert: result.IsValid is false; result.Violations has at least one violation where RuleId == "forbidden.border-collapse.collapse" and SuggestedAlternative contains "border-collapse:separate".
    Do NOT add inline comments explaining what the test does — the names are self-describing.
  </action>
  <verify>
    <automated>cd D:\sources\Core\muonroi-building-block &amp;&amp; dotnet test tests/Muonroi.Pdf.Governance.Tests --no-build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>dotnet test tests/Muonroi.Pdf.Governance.Tests exits 0 with "Passed: 1" (or more); DefaultStrictPolicy.CheckCssFeatures() contains the literal string "forbidden.border-collapse.collapse"</done>
</task>

<task type="auto">
  <name>Task 2: Add SC2 Vietnamese+Latin test + KD-03-05</name>
  <files>
    tests/Muonroi.Pdf.Tests/Layout/InlineLayoutTests.cs,
    KNOWN-DEVIATIONS.md
  </files>
  <action>
    In tests/Muonroi.Pdf.Tests/Layout/InlineLayoutTests.cs, add one [Fact] after the existing four tests:
    Method name: VietnamesePlusLatin_MixedText_ProducesOneElementPerSpaceSeparatedToken
    The test creates a single InlineBox with text "Xin chào world" (3 space-delimited tokens, where "chào" contains the combining grave+circumflex diacritics as precomposed Unicode U+00E0 + U+0301 — or the single precomposed U+1EBB character for "ào").
    availableWidth must be wide enough (500f) that all 3 words fit on one line (do NOT force wrapping — the goal is to verify word tokenisation, not layout wrapping).
    Call engine.Layout() on the single InlineBox.
    Assert output.Count == 3, because: "InlineLayoutEngine splits on spaces, producing one PositionedElement per token regardless of script"
    No additional comments needed.

    In KNOWN-DEVIATIONS.md, append a new section "### KD-03-05: Unicode Line Breaking Algorithm (UAX#14) not implemented" under the Phase 3 Deviations section.
    The entry must document:
    - Specification: Unicode TR#14 (Unicode Line Breaking Algorithm) defines break opportunities by character class (BA, BB, ID, AL, SA, etc.)
    - Actual behavior: InlineLayoutEngine splits text using String.Split on { ' ', '\t', '\n', '\r', U+200B }. No character-class-based break opportunities are computed.
    - Why this is acceptable for Phase 3: Vietnamese text is space-delimited (one syllable per orthographic word); the space-splitting approach produces correct break opportunities for Latin+Vietnamese mixed text in practice.
    - Scope: Phase 3 InlineLayoutEngine (WordSeparators field); full UAX#14 is deferred to Phase 4 when SixLabors.Fonts shaping is integrated.
    - Rationale: UAX#14 requires a Unicode line-breaking table lookup per character. Phase 3 has no font metrics dependency; adding a UAX#14 implementation without font integration introduces complexity with no tested consumer. The space-splitting approach is confirmed correct by VietnamesePlusLatin_MixedText_ProducesOneElementPerSpaceSeparatedToken test.
  </action>
  <verify>
    <automated>cd D:\sources\Core\muonroi-building-block &amp;&amp; dotnet test tests/Muonroi.Pdf.Tests --no-build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>dotnet test tests/Muonroi.Pdf.Tests exits 0 with Passed count = 23 (22 original + 1 new); KNOWN-DEVIATIONS.md contains "KD-03-05"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test HTML → AngleSharpHtmlParser | Attacker-controlled HTML; MaxHtmlBytes limit enforced in parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-09-01 | Tampering | DefaultStrictPolicy border-collapse check | mitigate | The new check reads computed style via GetPropertyValue — same pattern as existing flex/grid/float checks; no new attack surface introduced |
| T-03-09-SC | Tampering | test dependencies | accept | No new NuGet packages added; test uses existing AngleSharp infrastructure already in the project |
</threat_model>

<verification>
dotnet build D:\sources\Core\muonroi-building-block\Muonroi.BuildingBlock.sln -- exits 0
dotnet test tests/Muonroi.Pdf.Governance.Tests -- exits 0, Passed >= 1
dotnet test tests/Muonroi.Pdf.Tests -- exits 0, Passed >= 23
grep -c "forbidden.border-collapse.collapse" src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs -- returns 1
grep -c "KD-03-05" KNOWN-DEVIATIONS.md -- returns 1
</verification>

<success_criteria>
- DefaultStrictPolicy rejects border-collapse:collapse with a structured PolicyViolation (LAYOUT-07 implemented)
- Governance test project has at least 1 passing test method
- Vietnamese+Latin InlineLayoutEngine test passes confirming space-based tokenisation
- KD-03-05 documents the UAX#14 deferral decision
- All 23 Muonroi.Pdf.Tests pass; all Governance.Tests pass
- Phase 3 SC3 is fully satisfied: border-collapse:collapse triggers PolicyViolation naming border-collapse:separate
</success_criteria>

<output>
Create `.planning/phases/03-box-tree-layout-engine/03-09-SUMMARY.md` when done.
</output>
