---
phase: 18-flexbox-layout-engine
plan: 04
type: execute
wave: 4
repo: muonroi-building-block
depends_on: [18-03]
files_modified:
  - tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs
  - tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs
  - tests/Muonroi.Pdf.Tests/Service/PdfServiceTestHarness.cs
  - tests/Muonroi.Pdf.Tests/TestResources/Golden/
autonomous: true
requirements: [FLEX-07, FLEX-08]

must_haves:
  truths:
    - "Unit tests assert PositionedElement.Position (X/Y/W/H) by OPERAND VALUE for row distribution, grow, shrink, justify-content (incl. space-*), align-items (incl. stretch), wrap, gap, column direction, and nested flex (FLEX-07)"
    - "A new FlexLayout golden corpus group + FlexLayoutGoldenTests render each case with AllowModernLayout=true; baselines are committed under TestResources/Golden (FLEX-07)"
    - "The existing 82 golden baselines remain byte-identical — the full structural snapshot suite passes with NO MUONROI_UPDATE_SNAPSHOTS and an explicit regression assertion proves the count/identity (FLEX-08)"
    - "Muonroi.Pdf.Tests + Muonroi.Pdf.Governance.Tests pass per-project; build validated against .NET 8/9 (FLEX-08)"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs"
      provides: "operand-value position assertions for the FLEX-07 scenarios"
      contains: "FlexLayoutTests"
    - path: "tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs"
      provides: "flex golden cases rendered with AllowModernLayout=true (FLEX-07)"
      contains: "FlexLayoutGoldenTests"
    - path: "tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs"
      provides: "FlexLayout corpus group + FlexCasesData + AllCases concat"
      contains: "FlexLayout"
  key_links:
    - from: "tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs"
      to: "tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs"
      via: "VerifyAsync with an AllowModernLayout=true render path"
      pattern: "AllowModernLayout"
    - from: "tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs"
      to: "src/Muonroi.Pdf/Internal/Layout/FlexLayoutEngine.cs"
      via: "BoxTreeBuilder.Build(..., allowModernLayout:true) → FlexLayoutEngine.Layout → assert PositionedElement.Position"
      pattern: "FlexLayoutEngine"
---

<objective>
Prove the flexbox engine works by OPERAND VALUES and prove the opt-in did not perturb the default path. Add unit tests asserting `PositionedElement.Position` X/Y/W/H for the FLEX-07 scenarios; add a new `FlexLayout` golden corpus group + `FlexLayoutGoldenTests` rendered with `AllowModernLayout=true` and commit the new baselines; add an explicit regression assertion that the existing 82 baselines are byte-identical (the decisive opt-in-safety guard); confirm per-project green + .NET 8/9 build validation.

Purpose: Close FLEX-07 (verifiable flex layout) and FLEX-08 (byte-identical default path + green suites). Per memory pdf_phase15_radial_affine, assert item positions — a green non-throwing render once hid a real layout bug.
Output: FlexLayoutTests + FlexLayout golden group + FlexLayoutGoldenTests + committed baselines + regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block. Commit with `git -C D:/sources/Core/muonroi-building-block`.
Run tests PER-PROJECT (memory test_flakiness_nested_build): `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` and `dotnet test tests/Muonroi.Pdf.Governance.Tests/Muonroi.Pdf.Governance.Tests.csproj`. Do NOT run the full ~80-project solution.
.NET 8/9 build validation (memory ci_sdk8_vs_local_sdk10_cs1587): also build the test project with `-p:TargetFramework` against an installed lower TFM, or `dotnet build` under the .NET 8 SDK if available — local .NET 10 green is not sufficient to declare the gate green.
Assert by OPERAND VALUES (memory pdf_phase15_radial_affine).
</execution_context>

<context>
@.planning/phases/18-flexbox-layout-engine/18-CONTEXT.md
@.planning/phases/18-flexbox-layout-engine/18-03-SUMMARY.md

<interfaces>
<!-- Exact existing test infra. -->

tests/Muonroi.Pdf.Tests/Layout/BlockLayoutTests.cs:8-21 — unit test shape: MakeContext() builds a LayoutContext (PageMargins=PdfMargins.Zero); FakeStyledNode("tag", new(){["prop"]="val"}); .ChildList.Add(child); new BoxTreeBuilder().Build(root[, null, allowModernLayout:true]); new BlockLayoutEngine().Layout(box, ctx, elements, 0); elements.First(e => e.Source == childBox).Position.X/Y/Width/Height .Should().BeApproximately(value, 0.1f). NOTE: to exercise FlexLayoutEngine in a unit test you must wire FlexEngine like LayoutEngine does — either use `new LayoutEngine(...)` end-to-end, OR construct `var be = new BlockLayoutEngine(); be.FlexEngine = new FlexLayoutEngine(be); be.TableEngine = new TableLayoutEngine(be, be.InlineEngine);` then call be.Layout on a parent containing the FlexContainerBox. Prefer the explicit-wiring form so positions are asserted directly off PositionedElement.
Units: 1px = Units.PxToPt (≈0.75pt). Gaps/sizes in CSS px convert via ParseLength.

tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs:
  GoldenCase record struct (line 16): GoldenCase(string Name, string Html, PdfRenderOptions Options); convenience ctor GoldenCase(name, html) → Options=new().
  Doc(style, body) helper (line 27): wraps with FontFace under "serif" so the headless host resolves the embedded test font.
  Group fields are `internal static readonly IReadOnlyList<GoldenCase>` (e.g. BlockLayout line 34). ADD `FlexLayout` group the same way.
  AllCases (line 541-558): `.Concat(...)` chain. ADD `.Concat(FlexLayout)`.
  CasesData pattern (line 565): `public static IEnumerable<object[]> XxxCasesData() => Xxx.Select(c => new object[]{ c.Name });`. ADD `FlexCasesData()`.
  ByName (line 642): AllCases.First(c => c.Name == name).
  IMPORTANT: GoldenCase carries only PdfRenderOptions, NOT config. The flex flag must come from the render path (see GoldenPdf + harness below).

tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs:
  RenderAsync(html, options, ct) (line 58-64): uses `PdfServiceTestHarness.BuildProvider()` (DEFAULT config → AllowModernLayout=false). To render flex goldens you MUST build a provider with AllowModernLayout=true.
  VerifyAsync(caseName, html, options, ct, [CallerFilePath] callerPath) (line 70-102): UpdateMode (MUONROI_UPDATE_SNAPSHOTS=1) writes bytes to SOURCE TestResources/Golden; else structural byte-compare via NormalizeStructure. ADD an overload `VerifyAsync(..., bool allowModernLayout)` that calls a flag-aware RenderAsync.

tests/Muonroi.Pdf.Tests/Service/PdfServiceTestHarness.cs:
  ValidConfig(IDictionary<string,string?>? overrides = null) (line 32): in-memory config; pass overrides like ["PdfConfigs:Policy:AllowModernLayout"]="true".
  BuildProvider(IConfiguration? config = null) (line 79): test doubles + AddPdf + config. Call `BuildProvider(ValidConfig(new Dictionary<string,string?>{["PdfConfigs:Policy:AllowModernLayout"]="true"}))` for the flex render path.

tests/Muonroi.Pdf.Tests/Golden/BlockLayoutGoldenTests.cs — the golden test class shape: [Collection(PdfRenderCollection.Name)], [Theory][MemberData(nameof(GoldenCorpus.XxxCasesData), ...)] → GoldenCorpus.ByName(name) → GoldenPdf.VerifyAsync(...). Mirror for FlexLayoutGoldenTests.

Existing 82 baselines live in tests/Muonroi.Pdf.Tests/TestResources/Golden/*.pdf (embedded resources). AllCasesData() drives the all-cases byte-equality theory (the regression guard).
</interfaces>

@tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
@tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs
@tests/Muonroi.Pdf.Tests/Layout/BlockLayoutTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FlexLayoutTests — assert PositionedElement.Position by operand value (FLEX-07)</name>
  <files>tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Layout/BlockLayoutTests.cs:8-95 (MakeContext, FakeStyledNode, Build, engine.Layout, BeApproximately assertions)
    - tests/Muonroi.Pdf.Tests/Layout/TableLayoutTests.cs (multi-box position assertion style; how a non-block engine's output is asserted)
    - src/Muonroi.Pdf/Internal/Layout/FlexLayoutEngine.cs (Plan 03 — the algorithm under test)
  </read_first>
  <behavior>
    - RowDistribution_PositionsItemsLeftToRight: container display:flex width:300px, 3 children width:50px each, no gap → child X positions 0, 50, 100 (in pts via originX); each W≈50px*PxToPt; same Y. Assert all three X and W by value.
    - FlexGrow_DistributesPositiveFreeSpace: width:300px, two children flex:1 and flex:1, basis 0 → each item main size ≈150px; X of second ≈ first.X + 150px. Assert both widths and the second item's X.
    - FlexShrink_ShrinksOnOverflow: width:100px, two children width:80px flex-shrink:1 → total basis 160 > 100, free=-60 split equally → each ≈50px. Assert both widths ≈50px*PxToPt.
    - JustifyContent_SpaceBetween: width:300px, two children width:50px, justify-content:space-between → first X at start, second X at end (start + 250px). Assert second.X.
    - JustifyContent_Center_And_SpaceEvenly: two cases — center (free/2 leading) and space-evenly (free/(n+1) before each). Assert first item X for each.
    - AlignItems_Stretch_SetsCrossSize: row, container height:100px, child with no explicit height, align-items:stretch → child Height ≈100px*PxToPt. And align-items:flex-start → child keeps content height (< container height). Assert the stretch height by value.
    - FlexWrap_BreaksToSecondLine: width:120px, three children width:50px, flex-wrap:wrap, gap:0 → items 1,2 on line 1 (X 0,50), item 3 wraps to line 2 (X 0, Y > line-1 Y). Assert item3.X≈0 and item3.Y > item1.Y.
    - Gap_AddsMainAxisSpacing: width:300px, two children width:50px, gap:20px → second.X ≈ first.X + 50px + 20px*PxToPt. Assert second.X by value.
    - ColumnDirection_StacksVertically: flex-direction:column, two children height:40px → second.Y ≈ first.Y + 40px*PxToPt; both same X. Assert second.Y.
    - Order_ReordersVisually: two children, first has order:2, second order:1 → the order:1 child is placed at main-start (smaller X) despite being second in source. Assert by source identity + X.
    - NestedFlex_Composes: a flex container whose child is itself a FlexContainerBox with two children → assert the inner items get positioned (X advances) AND are offset by the outer item's X (recursion via dispatch). Assert an inner grandchild X relative to the outer item X.
  </behavior>
  <action>
Create `tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs` (namespace `Muonroi.Pdf.Tests.Layout`). Build the box tree with `new BoxTreeBuilder().Build(root, null, allowModernLayout: true)` so flex maps to FlexContainerBox. Wire the engine explicitly: `var be = new BlockLayoutEngine(); be.TableEngine = new TableLayoutEngine(be, be.InlineEngine); be.FlexEngine = new FlexLayoutEngine(be);` then `be.Layout(rootBox, MakeContext(width), elements, 0, isRoot:false)`. Use `PdfMargins.Zero` so originX=0 and positions are clean. Assert `elements.First(e => e.Source == itemBox).Position.{X,Y,Width,Height}.Should().BeApproximately(expected, 0.1f)` for EVERY scenario in `<behavior>` — assert OPERAND VALUES, never just "elements.Count > 0". Convert CSS px expectations through `Units.PxToPt`. RED first: write tests, run, confirm they exercise FlexLayoutEngine, then make them GREEN (fixing the engine in Plan 03's file if a real position bug surfaces — that is the point of operand-value assertions).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~FlexLayoutTests"</automated>
  </verify>
  <done>All FLEX-07 scenarios pass with operand-value position assertions (row, grow, shrink, justify incl. space-*, align-items incl. stretch, wrap, gap, column, order, nested).</done>
</task>

<task type="auto">
  <name>Task 2: FlexLayout golden corpus + FlexLayoutGoldenTests + flag-aware render path + committed baselines (FLEX-07)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs, tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs, tests/Muonroi.Pdf.Tests/Service/PdfServiceTestHarness.cs, tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs, tests/Muonroi.Pdf.Tests/TestResources/Golden/</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs:13-65 (GoldenCase, Doc helper, group field shape), :540-566 (AllCases concat + CasesData)
    - tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs:58-102 (RenderAsync, VerifyAsync, UpdateMode)
    - tests/Muonroi.Pdf.Tests/Service/PdfServiceTestHarness.cs:32-86 (ValidConfig overrides, BuildProvider)
    - tests/Muonroi.Pdf.Tests/Golden/BlockLayoutGoldenTests.cs (golden test class shape)
  </read_first>
  <action>
1) In `GoldenPdf.cs`: add a flag-aware render path. Add `internal static async Task<byte[]> RenderAsync(string html, PdfRenderOptions options, bool allowModernLayout, CancellationToken ct = default)` that builds the provider with `PdfServiceTestHarness.BuildProvider(PdfServiceTestHarness.ValidConfig(new Dictionary<string,string?>{ ["PdfConfigs:Policy:AllowModernLayout"] = allowModernLayout ? "true" : "false" }))` then renders via `IMPdfService.RenderToBytesAsync`. Add a `VerifyAsync` overload taking `bool allowModernLayout` that calls this render path (UpdateMode + NormalizeStructure compare identical to the existing overload). Keep the existing flag-less overloads byte-for-byte unchanged (the 82-baseline regression guard depends on them being untouched).
2) In `GoldenCorpus.cs`: add an `internal static readonly IReadOnlyList<GoldenCase> FlexLayout` group with cases covering the FLEX-07 surface, each authored with `display:flex` + the relevant props (use the Doc(style, body) helper). Minimum cases: `flex-row-basic`, `flex-grow-distribute`, `flex-justify-space-between`, `flex-align-items-stretch`, `flex-wrap-two-line`, `flex-gap`, `flex-column`, `flex-order`, `flex-nested`. Add `.Concat(FlexLayout)` to `AllCases` (line 558). Add `public static IEnumerable<object[]> FlexCasesData() => FlexLayout.Select(c => new object[]{ c.Name });`.
   NOTE: because these flex cases also flow through `AllCases`/`AllCasesData()` (the all-cases byte-equality theory), and that theory uses the FLAG-LESS render path (AllowModernLayout=false → flex degrades to block), DO NOT add the flex cases to the all-cases byte-equality theory's expectation set. Exclude FlexLayout from AllCasesData (keep AllCases for ByName lookups only) OR add the flex cases to AllCases but filter them out of AllCasesData so the default-path byte-equality theory still covers exactly the original 82. Choose the exclusion approach that keeps the 82-count guard exact (document the choice in a comment).
3) Create `tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs` mirroring BlockLayoutGoldenTests: `[Collection(PdfRenderCollection.Name)]`, `[Theory][MemberData(nameof(GoldenCorpus.FlexCasesData), MemberType=typeof(GoldenCorpus))]` → `GoldenCorpus.ByName(name)` → `await GoldenPdf.VerifyAsync(c.Name, c.Html, c.Options, allowModernLayout: true)`.
4) Generate baselines once: run the FlexLayoutGoldenTests with `MUONROI_UPDATE_SNAPSHOTS=1` to write the new `*.pdf` baselines into `tests/Muonroi.Pdf.Tests/TestResources/Golden/`, then re-run WITHOUT the env var to confirm they match. Ensure the new `.pdf` files are added as embedded resources (the test csproj globs TestResources/Golden — confirm the new files are picked up; if the csproj lists them explicitly, add the new names). Commit the new baselines.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~FlexLayoutGoldenTests"</automated>
  </verify>
  <done>FlexLayout golden group + FlexLayoutGoldenTests render with AllowModernLayout=true; new baselines committed and matched structurally; the all-cases byte-equality theory still targets exactly the original 82 (flex cases excluded from the default-path expectation).</done>
</task>

<task type="auto">
  <name>Task 3: Byte-identical regression guard for the existing 82 baselines + per-project + .NET 8/9 (FLEX-08)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs:560-562 (AllCasesData) + the all-cases byte-equality theory consumer (grep for AllCasesData usage)
    - tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs:95-101 (structural byte-equality assertion)
  </read_first>
  <action>
Add an explicit regression assertion proving the existing baselines are byte-identical and not perturbed by the opt-in:
- Add a `[Fact]` (e.g. in a small `FlexRegressionGuardTests` class, or extend the existing all-cases test class) named `DefaultPath_Has_Exactly_82_ByteIdentical_Baselines` that asserts `GoldenCorpus.AllCasesData().Count() == 82` (the count of the default-path cases AFTER excluding the new flex cases per Task 2). Hard-code 82 with a comment citing FLEX-08 / D-04 so any accidental change to the default corpus fails loudly.
- Confirm the all-cases byte-equality theory (the one driven by `AllCasesData()` / the flag-less VerifyAsync) runs green with NO MUONROI_UPDATE_SNAPSHOTS — this IS the byte-identical proof (every default-path case structurally matches its committed baseline). If the exact count is not 82 in the current tree, read the actual committed baseline count and assert THAT number (the locked invariant is "unchanged from before this phase", not literally 82 if the tree already differs) — document the measured number in the comment.
Then run the FULL per-project suites green and validate .NET 8/9 build.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Governance.Tests.csproj -c Debug 2>/dev/null; dotnet test tests/Muonroi.Pdf.Governance.Tests/Muonroi.Pdf.Governance.Tests.csproj -c Debug</automated>
  </verify>
  <done>The default-path byte-equality theory passes against the committed baselines with no re-baseline (existing baselines byte-identical — FLEX-08); the count guard asserts the unchanged default-corpus size; both per-project test suites are green; build validated against .NET 8/9 (not only local .NET 10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flex render path → golden bytes | Flex goldens render with AllowModernLayout=true; the default-path byte-equality theory must stay on AllowModernLayout=false so the 82 baselines prove the opt-in didn't perturb the default. |
| new baselines → committed bytes | New flex .pdf baselines are committed; they must be deterministic (same render twice = same bytes) — relies on the existing OwnedPdfWriter determinism + structural normalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | flex cases leaking into the 82-case default-path theory | mitigate | Exclude FlexLayout from AllCasesData; the count guard (Task 3) asserts the default-corpus size is unchanged — fails loudly on accidental inclusion |
| T-18-09 | Repudiation | silent re-baseline masking a regression | mitigate | The default-path theory runs with NO MUONROI_UPDATE_SNAPSHOTS; structural compare fails on any byte drift in the 82 baselines |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; reuses existing repo-pinned xunit/FluentAssertions test deps (CPM) — Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `dotnet test` Muonroi.Pdf.Tests + Muonroi.Pdf.Governance.Tests both green (per-project).
- FlexLayoutTests assert operand-value positions; FlexLayoutGoldenTests match committed baselines; the default-path all-cases theory matches with no re-baseline.
- Count guard asserts the unchanged default-corpus baseline count (82 or the measured committed number).
- Build validated under .NET 8/9 (not only local .NET 10).
</verification>

<success_criteria>
- FLEX-07: unit tests assert PositionedElement.Position by value for all representative flex scenarios; FlexLayout golden corpus + baselines added.
- FLEX-08: existing baselines byte-identical (structural suite green, no re-baseline); both suites green per-project; .NET 8/9 build validated.
</success_criteria>

<output>
Create `.planning/phases/18-flexbox-layout-engine/18-04-SUMMARY.md` when done.
</output>
