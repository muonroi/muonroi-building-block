---
phase: 16-pdf-enterprise-governance-controlplane-integration
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified:
  - muonroi-ui-engine/packages/m-ui-engine-pdf-designer/tests/license/RequireCapability.test.tsx
autonomous: false
requirements: [SC1, SC2, SC4]
must_haves:
  truths:
    - "WS-C: RequireCapability gates pdf.registry and pdf.canary (not only pdf.designer) — an ungranted registry/canary capability renders the locked stub, a granted one renders children"
    - "WS-D: license-server KnownPdfCapabilities recognizes pdf.designer/pdf.registry/pdf.canary (confirm-only; already shipped Phase 9.4) — IsKnown returns true for all three and All contains all three"
    - "SC2: a template publish propagates per-tenant with no cross-tenant cache invalidation (confirm-only; shipped Phase 9 hot-reload) — SignalRPdfTemplateChangeNotifier uses a tenant-scoped channel, not a global broadcast"
    - "SC4: full Muonroi.Pdf.Tests + governance suites are green (0 failed) and the OSS engine is byte-identical (no golden snapshot re-baseline)"
  artifacts:
    - path: "muonroi-ui-engine/packages/m-ui-engine-pdf-designer/tests/license/RequireCapability.test.tsx"
      provides: "WS-C registry/canary gating coverage (extends existing test)"
  key_links:
    - from: "RequireCapability.test.tsx"
      to: "RequireCapability component"
      via: "render with capability='pdf.registry' / 'pdf.canary'"
      pattern: "pdf\\.(registry|canary)"
---

<objective>
Close the two light cross-repo workstreams and the phase-level SC2/SC4 confirm + green gate:
- WS-C (ui-engine): confirm the existing `RequireCapability` component gates `pdf.registry` and
  `pdf.canary`, not just the Phase 9 `pdf.designer` — by extending its test suite (the component is already
  capability-generic; this proves the registry/canary keys gate correctly).
- WS-D (license-server): confirm `KnownPdfCapabilities` recognizes all three pdf.* keys (shipped 9.4 — assertion only).
- SC2 (cross-tenant cache isolation): confirm the Phase 9 hot-reload change-notification is tenant-scoped
  (per-tenant channel, not a broadcast) so a publish does not invalidate another tenant's cache — read-only confirmation.
- SC4: verify full `Muonroi.Pdf.Tests` + governance suites green and the OSS engine byte-identical (no golden re-baseline).

Purpose: satisfy SC1 (UI gate) + SC4 (green gate, byte-identical engine), confirm SC2 (per-tenant
propagation) shipped, and record WS-D confirmation.
Output: an extended ui-engine test + a human-verified full-suite + golden-stability + SC2/WS-D confirmation checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE-09-CLOSEOUT.md
@.planning/phases/16-pdf-enterprise-governance-controlplane-integration/16-CONTEXT.md
@.planning/phases/16-pdf-enterprise-governance-controlplane-integration/16-RESEARCH.md

<interfaces>
RequireCapability (muonroi-ui-engine/packages/m-ui-engine-pdf-designer/src/license/RequireCapability.tsx):
  props { capability: string; children: ReactNode; fallback?: ReactNode }
  Renders children when MLicenseVerifier.hasAnyFeature(candidates) is true, else MLockedFeatureStub.
  The component is generic over `capability`; pdf.registry / pdf.canary pass through (no designer-only alias).

KnownPdfCapabilities (muonroi-license-server/src/Muonroi.LicenseServer/KnownPdfCapabilities.cs):
  consts PdfDesigner="pdf.designer", PdfRegistry="pdf.registry", PdfCanary="pdf.canary";
  IReadOnlyList<string> All; bool IsKnown(string) — ALL THREE already present (Phase 9.4).

SignalRPdfTemplateChangeNotifier (control-plane; Phase 9 hot-reload):
  Publishes pdf.template.* change notifications over a per-tenant channel/group (SC2 isolation). Confirm
  the channel/group name is tenant-scoped (carries tenantId), NOT a single global broadcast group, by
  reading the notifier source and/or PHASE-09-CLOSEOUT.md — read-only, no code change.

ui-engine test command: from packages/m-ui-engine-pdf-designer — `vitest run` (package script "test").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RequireCapability tests to cover pdf.registry and pdf.canary (WS-C)</name>
  <files>muonroi-ui-engine/packages/m-ui-engine-pdf-designer/tests/license/RequireCapability.test.tsx</files>
  <read_first>
    - muonroi-ui-engine/packages/m-ui-engine-pdf-designer/src/license/RequireCapability.tsx (the generic component; designer keys always in candidates, registry/canary pass through; MLicenseVerifier.hasAnyFeature is the seam to stub)
    - muonroi-ui-engine/packages/m-ui-engine-pdf-designer/tests/license/RequireCapability.test.tsx (existing pdf.designer tests — match the MLicenseVerifier mock/stub style and the locked-stub assertion via role="alert")
  </read_first>
  <action>
    Append test cases to the existing `RequireCapability.test.tsx` that exercise `capability="pdf.registry"`
    and `capability="pdf.canary"`. Using the same `MLicenseVerifier.hasAnyFeature` stub approach already in the
    file: (a) when the verifier reports the key granted, assert children render; (b) when not granted, assert the
    locked stub renders (query the `role="alert"` element and the capability-key text). Do NOT modify
    `RequireCapability.tsx` — it is already capability-generic; this plan only proves registry/canary gate.
    If a genuine behavioral gap surfaces (e.g. registry/canary incorrectly inherit the designer alias and pass
    when they should not), STOP and surface it rather than editing the component silently.
  </action>
  <verify>
    <automated>cd muonroi-ui-engine/packages/m-ui-engine-pdf-designer && npx vitest run tests/license/RequireCapability.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `RequireCapability.test.tsx` contains test cases referencing `pdf.registry` and `pdf.canary`.
    - `vitest run tests/license/RequireCapability.test.tsx` exits 0 with the new cases passing.
    - `RequireCapability.tsx` is unchanged (git diff shows no source edit).
  </acceptance_criteria>
  <done>RequireCapability is proven to gate registry and canary capabilities, not only designer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC4 green gate + SC2 + WS-D confirmation (human-verify)</name>
  <what-built>
    SC4 green gate + SC2 + WS-D confirmation. All Phase 16 code changes (Plans 01-04) are in place: real
    LicenseFeatureGate + pdf.* registrations (building-block), Quota metering, control-plane compliance
    adapter, and canary auto-rollback. This checkpoint confirms the full suites are green, the OSS engine
    is byte-identical, per-tenant publish propagation is tenant-scoped (SC2, shipped Phase 9), and
    license-server entitlements are confirmed shipped (WS-D).
  </what-built>
  <how-to-verify>
    1. Building-block full suite (Pre-Push Test Gate): from `D:/sources/Core/muonroi-building-block`, run
       `dotnet test` — expect 0 failed. Specifically confirm `tests/Muonroi.Pdf.Tests/` and
       `tests/Muonroi.Pdf.Governance.Tests/` are green.
    2. OSS engine byte-identical (SC4 no golden re-baseline): confirm the golden-snapshot tests in
       `tests/Muonroi.Pdf.Tests/` pass WITHOUT regenerating baselines (no `--update`/`UPDATE_SNAPSHOTS`
       flag was used); `git status src/Muonroi.Pdf/` shows no source change from this phase.
    3. Control-plane: from `D:/sources/Core/muonroi-control-plane`, run `dotnet test` — confirm no NEW
       failures vs the Phase 9 baseline (33 pass / 44 pre-existing fail per PHASE-09-CLOSEOUT.md); the
       PdfAuditControlPlane and PdfCanary tests are green.
    4. ui-engine: from `muonroi-ui-engine/packages/m-ui-engine-pdf-designer`, run `npx vitest run` — green.
    5. WS-D confirm (license-server, no code change): confirm `KnownPdfCapabilities.All` contains
       pdf.designer/pdf.registry/pdf.canary and `IsKnown` returns true for each (read
       `muonroi-license-server/src/Muonroi.LicenseServer/KnownPdfCapabilities.cs`). Record as confirmed-shipped.
    6. SC2 confirm (cross-tenant cache isolation, no code change — shipped Phase 9 hot-reload): confirm via
       `.planning/PHASE-09-CLOSEOUT.md` and/or the notifier source that the template-change notification is
       tenant-scoped — i.e. `SignalRPdfTemplateChangeNotifier` (or the change-notification channel) publishes
       to a per-tenant channel/group whose name carries the tenant id, NOT a single global broadcast group, so
       a publish in tenant A does not invalidate tenant B's cache. Record as confirmed-shipped, read-only
       (mirror the WS-D confirmation style). If the notifier turns out to broadcast globally (no tenant scoping),
       STOP and surface it — SC2 would then be an unclosed gap rather than a confirmation.
  </how-to-verify>
  <resume-signal>Type "approved" if all suites are green, the OSS engine is byte-identical (no golden re-baseline), SC2 per-tenant propagation is confirmed tenant-scoped, and WS-D is confirmed; otherwise describe the failing suite/test or the SC2 scoping gap.</resume-signal>
  <action>Run the full building-block, control-plane, and ui-engine suites, plus the WS-D and SC2 confirmation reads as described in how-to-verify; do not regenerate golden snapshots. Present results for human approval.</action>
  <verify><human-check>All suites green (0 failed building-block, no new control-plane failures), OSS golden snapshots pass without re-baseline, KnownPdfCapabilities confirmed, SignalRPdfTemplateChangeNotifier confirmed tenant-scoped (SC2).</human-check></verify>
  <done>Human confirms SC4 green gate + byte-identical OSS engine + SC2 per-tenant propagation + WS-D entitlements shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client license claims → UI capability gate | A client without the entitlement must not see/operate gated UI |
| tenant A publish → tenant B cache | A template publish must not invalidate or leak into another tenant's cache (SC2 isolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Elevation of privilege | UI capability bypass for registry/canary | mitigate | `RequireCapability` delegates to `MLicenseVerifier.hasAnyFeature`; the new tests prove ungranted registry/canary render the locked stub. UI gating is defense-in-depth — the authoritative gate is the server-side `LicenseFeatureGate` (Plan 01). |
| T-16-15 | Spoofing | client-forged feature flags | accept | Client-side gate is advisory; server enforces via signed ActivationProof + `LicenseFeatureGate`. A forged client flag cannot grant a server-gated capability. |
| T-16-16 | Information disclosure | cross-tenant cache invalidation via template-change broadcast | mitigate | SC2 confirmation step verifies `SignalRPdfTemplateChangeNotifier` publishes on a per-tenant (tenant-scoped) channel, not a global broadcast; a tenant A publish cannot invalidate tenant B's cache. Shipped Phase 9; confirmed read-only here. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (existing `vitest`/`dotnet` only). |
</threat_model>

<verification>
- ui-engine: `npx vitest run tests/license/RequireCapability.test.tsx` → green.
- Building-block: `dotnet test` (solution) → 0 failed; golden snapshots pass without re-baseline.
- Control-plane: `dotnet test` → no NEW failures vs Phase 9 baseline.
- license-server: KnownPdfCapabilities confirmed (read-only).
- SC2: SignalRPdfTemplateChangeNotifier confirmed tenant-scoped per-tenant channel (read-only).
</verification>

<success_criteria>
SC1 (UI gate) + SC2 (per-tenant publish propagation, no cross-tenant cache invalidation — confirmed shipped) + SC4 (full suites green, OSS engine byte-identical, no golden re-baseline) satisfied; WS-D entitlements confirmed shipped.
</success_criteria>

<output>
Create `.planning/phases/16-pdf-enterprise-governance-controlplane-integration/16-05-SUMMARY.md` when done.
</output>
</content>
