---
phase: 19-grid-layout-engine
plan: 04
type: execute
wave: 4
repo: muonroi-building-block
depends_on: [19-03]
files_modified:
  - tests/Muonroi.Pdf.Tests/Layout/GridLayoutTests.cs
  - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
  - tests/Muonroi.Pdf.Tests/Golden/GridLayoutGoldenTests.cs
  - tests/Muonroi.Pdf.Tests/Golden/FlexRegressionGuardTests.cs
  - tests/Muonroi.Pdf.Tests/TestResources/Golden/
autonomous: true
requirements: [GRID-06, GRID-07, GRID-08]

must_haves:
  truths:
    - "Unit tests assert PositionedElement.Position (X/Y/W/H) and/or resolved track sizes by OPERAND VALUE for fixed tracks, fr distribution, minmax, repeat, gap, explicit line placement, span, auto-placement (row + column), named areas, and nested grid (GRID-06)"
    - "A new GridLayout golden corpus group (standalone, OUTSIDE AllCases) + GridLayoutGoldenTests render each case with AllowModernLayout=true via the existing flag-aware VerifyAsync overload; baselines committed under TestResources/Golden (GRID-07)"
    - "GridLayout is sourced via GridCasesData() directly from the standalone group; ByName is extended to AllCases.Concat(FlexLayout).Concat(GridLayout); GridLayout is NOT added to AllCases (the flag-less canary/byte-equality theory would throw PdfPolicyException forbidden.display.grid on a grid case)"
    - "The existing baselines (default-path + the 9 Phase-18 flex baselines) remain byte-identical — the default-path suite passes with NO MUONROI_UPDATE_SNAPSHOTS; the regression-guard count [Fact] stays 84 (grid adds 0 to AllCases); flex still renders with the flag on (GRID-08, SC4)"
    - "Muonroi.Pdf.Tests + Muonroi.Pdf.Governance.Tests pass per-project; build validated against net8.0 (GRID-08)"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Layout/GridLayoutTests.cs"
      provides: "operand-value position/track-size assertions for the GRID-06 scenarios"
      contains: "GridLayoutTests"
    - path: "tests/Muonroi.Pdf.Tests/Golden/GridLayoutGoldenTests.cs"
      provides: "grid golden cases rendered with AllowModernLayout=true (GRID-07)"
      contains: "GridLayoutGoldenTests"
    - path: "tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs"
      provides: "GridLayout standalone corpus group + GridCasesData + ByName concat"
      contains: "GridLayout"
  key_links:
    - from: "tests/Muonroi.Pdf.Tests/Golden/GridLayoutGoldenTests.cs"
      to: "tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs"
      via: "VerifyAsync(c.Name, c.Html, c.Options, allowModernLayout: true) — the existing flag-aware overload"
      pattern: "allowModernLayout: true"
    - from: "tests/Muonroi.Pdf.Tests/Layout/GridLayoutTests.cs"
      to: "src/Muonroi.Pdf/Internal/Layout/GridLayoutEngine.cs"
      via: "BoxTreeBuilder.Build(..., allowModernLayout:true) → GridLayoutEngine.Layout → assert PositionedElement.Position"
      pattern: "GridLayoutEngine"
---

<objective>
Prove the grid engine works by OPERAND VALUES and prove the opt-in did not perturb the default path or the Phase-18 flex path. Add unit tests asserting `PositionedElement.Position` X/Y/W/H (and resolved track sizes) for the GRID-06 scenarios; add a new standalone `GridLayout` golden corpus group + `GridLayoutGoldenTests` rendered with `AllowModernLayout=true` (reusing the EXISTING flag-aware `VerifyAsync` overload from Phase 18) and commit the new baselines; confirm the existing baselines (default-path + 9 flex) are byte-identical and the regression-guard count stays 84; confirm per-project green + net8.0 build validation.

Purpose: Close GRID-06 (verifiable grid layout), GRID-07 (grid golden corpus), GRID-08 (byte-identical existing path + green suites + flex unaffected). Per memory pdf_phase15_radial_affine, assert positions/track sizes — a green non-throwing render once hid a real layout bug.
Output: GridLayoutTests + GridLayout golden group + GridLayoutGoldenTests + committed baselines + confirmed regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block. Commit with `git -C D:/sources/Core/muonroi-building-block`.
Run tests PER-PROJECT (memory test_flakiness_nested_build): `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` and `dotnet test tests/Muonroi.Pdf.Governance.Tests/Muonroi.Pdf.Governance.Tests.csproj`. Do NOT run the full ~80-project solution.
net8.0 validation (memory ci_sdk8_vs_local_sdk10_cs1587): the test project targets net8.0 (Muonroi.Pdf.Tests.csproj), so a local net10 SDK compiles the net8.0 TFM — sufficient to surface CS1574/CS1587. Local net10 green alone is NOT sufficient to declare the gate green.
Assert by OPERAND VALUES (memory pdf_phase15_radial_affine).
</execution_context>

<context>
@.planning/phases/19-grid-layout-engine/19-CONTEXT.md
@.planning/phases/19-grid-layout-engine/19-03-SUMMARY.md

<interfaces>
<!-- Exact existing test infra (verified 2026-06-21, post-Phase-18). -->

tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs — the operand-value unit-test shape to MIRROR: build the box tree with `new BoxTreeBuilder().Build(root, null, allowModernLayout:true)`; wire the engine explicitly `var be = new BlockLayoutEngine(); be.TableEngine = new TableLayoutEngine(be, be.InlineEngine); be.FlexEngine = new FlexLayoutEngine(be); be.GridEngine = new GridLayoutEngine(be);` then drive the engine — EITHER call it directly as FlexLayoutTests does (`be.GridEngine.Layout(gridContainer, MakeContext(width), elements, 0)`, finding the GridContainerBox in the built tree first, mirror `FlexLayoutTests.FindFlex`) OR via `be.Layout(rootBox, MakeContext(width), elements, 0, isRoot:false)` — both emit the same PositionedElements; use `PdfMargins.Zero` so originX=0; assert `elements.First(e => e.Source == itemBox).Position.{X,Y,Width,Height}.Should().BeApproximately(expected, 0.1f)`. Convert CSS px through `Units.PxToPt` (≈0.75pt).
tests/Muonroi.Pdf.Tests/Layout/GridLayoutEngineSmokeTests.cs (Plan 03) — the column-placement smoke gate; GridLayoutTests is the full GRID-06 suite (do not duplicate the smoke case).

tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs (Phase 18 left the flex pattern to MIRROR):
  GoldenCase record struct (line 16): GoldenCase(string Name, string Html, PdfRenderOptions Options); convenience ctor GoldenCase(name, html) → Options=new(). Doc(style, body) helper (line 27).
  FlexLayout standalone group (line 76-115): `internal static readonly IReadOnlyList<GoldenCase> FlexLayout = new[] { ... }` — NOT in AllCases. ADD `GridLayout` the SAME way (standalone field).
  AllCases (line 591-609): `.Concat(...)` chain. Do NOT add GridLayout (or FlexLayout) here.
  FlexCasesData() (line 623): `FlexLayout.Select(c => new object[]{ c.Name })`. ADD `GridCasesData()` sourced from the GridLayout group.
  ByName (line 704): `AllCases.Concat(FlexLayout).First(c => c.Name == name)`. EXTEND to `AllCases.Concat(FlexLayout).Concat(GridLayout).First(...)`.

tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs (Phase 18 — REUSE, no change):
  RenderAsync(html, options, bool allowModernLayout, ct) (line 72) — flag-aware render path ALREADY exists.
  VerifyAsync(caseName, html, options, bool allowModernLayout, ct, [CallerFilePath]) (line 104) — flag-aware overload ALREADY exists; UpdateMode (MUONROI_UPDATE_SNAPSHOTS=1) writes bytes to SOURCE TestResources/Golden, else structural byte-compare. USE THIS for grid goldens. Do NOT add a new overload; do NOT touch the flag-less overloads (the byte-identity guard depends on them).

tests/Muonroi.Pdf.Tests/Service/PdfServiceTestHarness.cs — ValidConfig + BuildProvider (used internally by the flag-aware RenderAsync; no change).

tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs (the golden test class shape to MIRROR):
  [Collection(PdfRenderCollection.Name)]; [Theory][MemberData(nameof(GoldenCorpus.FlexCasesData), MemberType=typeof(GoldenCorpus))] → GoldenCorpus.ByName(name) → await GoldenPdf.VerifyAsync(c.Name, c.Html, c.Options, allowModernLayout: true).

tests/Muonroi.Pdf.Tests/Golden/FlexRegressionGuardTests.cs — the FLEX-08 guard:
  const int DefaultPathCorpusCount = 84; DefaultPath_Baseline_Count_Unchanged asserts AllCasesData().Count() == 84; FlexCases_AreExcludedFromDefaultPath asserts flex names ∉ AllCases. Grid adds 0 to AllCases → the 84 count is UNCHANGED. ADD a `GridCases_AreExcludedFromDefaultPath` [Fact] mirroring the flex one; do NOT change the count constant.

Existing baselines live in tests/Muonroi.Pdf.Tests/TestResources/Golden/*.pdf (embedded resources; 90 .pdf files on disk including the 9 flex). AllCasesData() drives the flag-less DeterminismCanaryTests + default-path byte-equality theory — both throw PdfPolicyException on a grid case, so grid MUST stay out of AllCases.
</interfaces>

@tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs
@tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs
@tests/Muonroi.Pdf.Tests/Golden/FlexRegressionGuardTests.cs
@tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GridLayoutTests — assert PositionedElement.Position / track sizes by operand value (GRID-06)</name>
  <files>tests/Muonroi.Pdf.Tests/Layout/GridLayoutTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Layout/FlexLayoutTests.cs (MakeContext, explicit engine wiring, BeApproximately position assertions)
    - tests/Muonroi.Pdf.Tests/Layout/TableLayoutTests.cs (multi-box position assertion style)
    - src/Muonroi.Pdf/Internal/Layout/GridLayoutEngine.cs (Plan 03 — the algorithm under test)
  </read_first>
  <behavior>
    - FixedTracks_PositionItemsIntoColumnCells: container display:grid width:300px grid-template-columns:100px 100px 100px, 3 children → child X positions 0, 75pt, 150pt (100px*PxToPt each); each cell width≈75pt; same Y. Assert all three X by value.
    - FrDistribution_SplitsFreeSpace: width:300px grid-template-columns:1fr 2fr, two children → first cell width≈100px*PxToPt, second≈200px*PxToPt; second.X≈first.X+first.Width. Assert both cell widths.
    - Minmax_ClampsTrackSize: grid-template-columns:minmax(50px,1fr) minmax(50px,1fr) width:80px (so 2×50px floor=100px>80px) → each track clamped to its 50px min (≈37.5pt). Assert both cell widths ≈37.5pt. Also a width:300px case where 1fr max wins → each ≈150px*PxToPt.
    - Repeat_ExpandsTracks: grid-template-columns:repeat(3,1fr) width:300px, 3 children → three equal cells (≈100px*PxToPt); third.X≈first.X+200px*PxToPt. Assert third.X + a cell width.
    - Gap_AddsBetweenTracks: width:300px grid-template-columns:100px 100px column-gap:20px (or gap:20px), two children → second.X≈first.X+100px*PxToPt+20px*PxToPt. Assert second.X.
    - ExplicitLinePlacement: grid-template-columns:repeat(3,100px), a child grid-column:"2 / 3" → that child placed in column 2 (X≈100px*PxToPt). Assert X by source identity.
    - SpanPlacement: grid-template-columns:repeat(3,100px), a child grid-column:"span 2" → cell width≈200px*PxToPt (spans two tracks). Assert cell width.
    - AutoPlacementRow: grid-template-columns:repeat(2,100px) grid-auto-flow:row, 3 children (no explicit placement) → child1 (0,0), child2 (col2, same Y), child3 wraps to row 2 (X≈0, Y>child1.Y). Assert child3.X≈0 and child3.Y>child1.Y.
    - AutoPlacementColumn: grid-template-rows:repeat(2,40px) grid-auto-flow:column, 3 children → child1 (row1), child2 (row2 same X), child3 (col2 row1, X>child1.X). Assert child2.Y>child1.Y (same X) and child3.X>child1.X.
    - NamedAreas: grid-template-areas:"\"head head\" \"nav main\"" grid-template-columns:100px 100px grid-template-rows:40px 40px; a child grid-area:"head" → spans both columns of row 1 (cell width≈200px*PxToPt, Y≈0); a child grid-area:"main" → column 2 row 2 (X≈100px*PxToPt, Y≈40px*PxToPt). Assert head cell width + main X/Y.
    - JustifyAlignSelf: a cell larger than its item (item width:40px in a 100px track) justify-self:center → item X ≈ cellX + (100-40)/2 px*PxToPt; align-self:end pins to cell bottom. Assert the centered X.
    - NestedGrid: a grid whose child is itself a GridContainerBox with two cells → assert the inner grandchildren get positioned (X advances) and are offset by the outer cell's X (recursion via dispatch). Assert an inner grandchild X relative to the outer cell X.
  </behavior>
  <action>
Create `tests/Muonroi.Pdf.Tests/Layout/GridLayoutTests.cs` (namespace `Muonroi.Pdf.Tests.Layout`). Build the tree with `new BoxTreeBuilder().Build(root, null, allowModernLayout:true)`. Wire the engine explicitly including `be.GridEngine = new GridLayoutEngine(be);` (plus FlexEngine + TableEngine as in FlexLayoutTests). Use `PdfMargins.Zero` so originX=0. Assert `elements.First(e => e.Source == itemBox).Position.{X,Y,Width,Height}.Should().BeApproximately(expected, 0.1f)` for EVERY scenario in `<behavior>` — assert OPERAND VALUES (cell rects / resolved track sizes), never just "elements.Count > 0". Convert CSS px through `Units.PxToPt`. RED first: write tests, run, confirm they exercise GridLayoutEngine, then make them GREEN (fixing the engine in Plan 03's file if a real position/track-size bug surfaces — that is the point of operand-value assertions; record any engine fix in the SUMMARY).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~GridLayoutTests"</automated>
  </verify>
  <done>All GRID-06 scenarios pass with operand-value assertions (fixed tracks, fr, minmax, repeat, gap, explicit line placement, span, auto-placement row + column, named areas, justify/align-self, nested grid).</done>
</task>

<task type="auto">
  <name>Task 2: GridLayout golden corpus + GridLayoutGoldenTests + committed baselines (GRID-07)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs, tests/Muonroi.Pdf.Tests/Golden/GridLayoutGoldenTests.cs, tests/Muonroi.Pdf.Tests/TestResources/Golden/</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs:76-115 (FlexLayout standalone group — the exact pattern), :591-624 (AllCases concat + FlexCasesData), :700-705 (ByName concat)
    - tests/Muonroi.Pdf.Tests/Golden/FlexLayoutGoldenTests.cs (golden test class shape)
    - tests/Muonroi.Pdf.Tests/Golden/GoldenPdf.cs:104-113 (flag-aware VerifyAsync overload — REUSE)
  </read_first>
  <action>
1) In `GoldenCorpus.cs`: add an `internal static readonly IReadOnlyList<GoldenCase> GridLayout` group (mirroring the FlexLayout group field at line 76, with the SAME XML-doc rationale — standalone, NOT in AllCases, because a grid case in AllCases throws PdfPolicyException `forbidden.display.grid` on the flag-less canary/byte-equality theory). Cases covering the GRID-07 surface, authored with `display:grid` + the relevant props via the Doc(style, body) helper. Minimum cases: `grid-fixed-tracks`, `grid-fr-distribute`, `grid-minmax`, `grid-repeat`, `grid-gap`, `grid-explicit-placement`, `grid-span`, `grid-auto-flow-row`, `grid-named-areas`, `grid-nested`.
   Add `public static IEnumerable<object[]> GridCasesData() => GridLayout.Select(c => new object[]{ c.Name });` next to FlexCasesData (line 623).
   CRITICAL (do NOT deviate): Do NOT add `GridLayout` to `AllCases`. Extend ONLY `ByName` (line 704) to `AllCases.Concat(FlexLayout).Concat(GridLayout).First(c => c.Name == name)`. Rationale (evidence, identical to flex): `AllCasesData()` drives `DeterminismCanaryTests` AND the default-path byte-equality theory, both rendering via the FLAG-LESS path (AllowModernLayout=false); under the default LegacyPrintPolicy a `display:grid` document hits `forbidden.display.grid` (Error) and `MPdfService` THROWS `PdfPolicyException` — any grid case in AllCases reddens the canary, it does not merely mismatch. Keeping grid out of AllCases guarantees the flag-less consumers only ever see the 84 default-path cases.
2) Create `tests/Muonroi.Pdf.Tests/Golden/GridLayoutGoldenTests.cs` mirroring FlexLayoutGoldenTests: `[Collection(PdfRenderCollection.Name)]`, `[Theory][MemberData(nameof(GoldenCorpus.GridCasesData), MemberType=typeof(GoldenCorpus))]` → `GoldenCorpus.ByName(name)` → `await GoldenPdf.VerifyAsync(c.Name, c.Html, c.Options, allowModernLayout: true)`. Reuse the EXISTING flag-aware VerifyAsync overload (do NOT add a new one).
3) Generate baselines once: run GridLayoutGoldenTests with `MUONROI_UPDATE_SNAPSHOTS=1` to write the new `*.pdf` baselines into `tests/Muonroi.Pdf.Tests/TestResources/Golden/`, then re-run WITHOUT the env var to confirm they match. Ensure the new `.pdf` files are picked up as embedded resources (the test csproj globs TestResources/Golden — confirm; if the csproj lists baselines explicitly, add the new names). Commit the new baselines.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~GridLayoutGoldenTests"</automated>
  </verify>
  <done>GridLayout standalone golden group + GridLayoutGoldenTests render with AllowModernLayout=true; new baselines committed and matched structurally; GridLayout excluded from AllCases (ByName-resolvable only); the flag-aware VerifyAsync overload is reused unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm existing baselines byte-identical + regression guard + per-project + net8.0 (GRID-08, SC4)</name>
  <files>tests/Muonroi.Pdf.Tests/Golden/FlexRegressionGuardTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Golden/FlexRegressionGuardTests.cs (full — DefaultPathCorpusCount=84, the two flex guard [Fact]s)
    - tests/Muonroi.Pdf.Tests/Golden/GoldenCorpus.cs:611-624 (AllCasesData + GridCasesData)
  </read_first>
  <action>
- ADD a `GridCases_AreExcludedFromDefaultPath` `[Fact]` to `FlexRegressionGuardTests` mirroring `FlexCases_AreExcludedFromDefaultPath`: assert `GoldenCorpus.GridCasesData()` is non-empty (the grid golden group exists — GRID-07) AND every grid case name is NOT in `AllCasesData()` (no grid case may appear in the flag-less default-path corpus — it would throw PdfPolicyException on the canary). Add a comment `// GRID-08 / T-19-08: grid cases live in the standalone GridLayout group, never in AllCases.`
- Do NOT change `DefaultPathCorpusCount` — it stays `84`. Grid adds 0 entries to `AllCases` (GridLayout is standalone), so `DefaultPath_Baseline_Count_Unchanged` still asserts 84 and proves the opt-in did not perturb the default corpus size. If, contrary to expectation, the on-disk default corpus count changed since Phase 18, re-measure (`ls tests/Muonroi.Pdf.Tests/TestResources/Golden/*.pdf | wc -l` minus the flex+grid groups) and justify the new constant in the comment — the invariant is "unchanged from before this phase".
- Confirm the default-path all-cases byte-equality theory (driven by `AllCasesData()` / flag-less VerifyAsync) runs green with NO MUONROI_UPDATE_SNAPSHOTS — this IS the byte-identical proof for both the default path AND the 9 Phase-18 flex baselines (the flex theory also runs green unchanged → SC4 flex unaffected).
- Run the FULL per-project suites green (Muonroi.Pdf.Tests + Muonroi.Pdf.Governance.Tests) and validate the net8.0 build (the test project targets net8.0; CS1574/CS1587 surface under the net8.0 TFM compile).
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug && dotnet test tests/Muonroi.Pdf.Governance.Tests/Muonroi.Pdf.Governance.Tests.csproj -c Debug</automated>
    <note>net8.0 validation: both test projects target net8.0, so the local net10 SDK compiles the net8.0 TFM — sufficient to surface CS1574/CS1587. If a net8 SDK is installed, also run `dotnet build -p:TargetFramework=net8.0`.</note>
  </verify>
  <done>The default-path byte-equality theory passes against the committed baselines with no re-baseline (existing default + 9 flex baselines byte-identical — GRID-08, SC4); the count guard stays 84 (grid adds 0 to AllCases); GridCases_AreExcludedFromDefaultPath passes; both per-project suites green; net8.0 build validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid render path → golden bytes | Grid goldens render with AllowModernLayout=true; the default-path byte-equality theory must stay on AllowModernLayout=false so the existing + flex baselines prove the opt-in didn't perturb them. |
| new baselines → committed bytes | New grid .pdf baselines are committed; they must be deterministic (same render twice = same bytes) — relies on the existing OwnedPdfWriter determinism + structural normalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | grid cases leaking into the 84-case default-path theory | mitigate | Exclude GridLayout from AllCases; GridCases_AreExcludedFromDefaultPath + the unchanged 84 count guard fail loudly on accidental inclusion (a grid case in AllCases throws PdfPolicyException on the canary) |
| T-19-10 | Repudiation | silent re-baseline masking a regression | mitigate | The default-path theory runs with NO MUONROI_UPDATE_SNAPSHOTS; structural compare fails on any byte drift in the existing + flex baselines |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; reuses existing repo-pinned xunit/FluentAssertions test deps (CPM) — Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `dotnet test` Muonroi.Pdf.Tests + Muonroi.Pdf.Governance.Tests both green (per-project).
- GridLayoutTests assert operand-value positions/track sizes; GridLayoutGoldenTests match committed baselines; the default-path all-cases theory + the flex theory match with no re-baseline (existing + flex byte-identical — GRID-08, SC4).
- Regression-guard count stays 84; GridCases_AreExcludedFromDefaultPath green.
- Build validated under net8.0 (not only local net10).
</verification>

<success_criteria>
- GRID-06: unit tests assert PositionedElement.Position / track sizes by value for all representative grid scenarios (fixed, fr, minmax, repeat, gap, explicit placement, span, auto-placement row/column, named areas, nested).
- GRID-07: new GridLayout standalone golden corpus (outside AllCases) + GridLayoutGoldenTests rendered with AllowModernLayout=true; baselines committed.
- GRID-08: existing baselines (default + 9 flex) byte-identical (structural suite green, no re-baseline); flex still renders with the flag on; both suites green per-project; net8.0 build validated.
</success_criteria>

<output>
Create `.planning/phases/19-grid-layout-engine/19-04-SUMMARY.md` when done.
</output>
</content>
