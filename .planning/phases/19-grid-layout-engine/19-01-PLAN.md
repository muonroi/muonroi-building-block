---
phase: 19-grid-layout-engine
plan: 01
type: execute
wave: 1
repo: muonroi-building-block
depends_on: []
files_modified:
  - src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs
  - tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03]

must_haves:
  truths:
    - "With AllowModernLayout=true, LegacyPrintPolicy emits NO forbidden.display.grid / soft-degrade.display.grid violation and NO grid sub-property (soft-degrade.grid-subproperty) warning (GRID-01, GRID-02)"
    - "With AllowModernLayout=true, flex STILL accepts (Phase 18 behaviour unchanged) and grid is now ALSO accepted — the flag unlocks BOTH (GRID-02)"
    - "With AllowModernLayout=false (default), grid behaviour is byte-for-byte unchanged: forbidden.display.grid in strict, soft-degrade.display.grid + grid sub-props dropped in soft-degrade (GRID-03)"
    - "DefaultStrictPolicy is unchanged — it always blocks grid regardless of the flag (GRID-03)"
    - "The Phase-18 tests asserting grid-still-blocked-with-flag-on are flipped to grid-accepted-with-flag-on; LegacyPrintPolicyTests strict/default grid expectations are untouched (GRID-02, GRID-03)"
  artifacts:
    - path: "src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs"
      provides: "grid display + grid sub-prop acceptance gated on allowModernLayout (GRID-01, GRID-02)"
      contains: "allowModernLayout"
    - path: "tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs"
      provides: "grid accept-path tests (flipped) + grid-default-unchanged tests (GRID-02, GRID-03)"
      contains: "Grid_FlagOn"
  key_links:
    - from: "src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs"
      to: "PdfPolicySettings.AllowModernLayout"
      via: "CheckCssFeatures gates the grid display block (:264) and grid sub-prop branch (:294) on allowModernLayout, mirroring the flex gate (:253/:310)"
      pattern: "allowModernLayout"
---

<objective>
Gate `LegacyPrintPolicy` grid acceptance on the EXISTING `PdfPolicySettings.AllowModernLayout` flag — the exact inverse of Phase 18's "grid stays blocked". When the flag is on, the policy stops emitting any grid violation (display + grid sub-properties), mirroring how the flex display block (`:253`) and flex sub-prop branch (`:310`) were gated in Phase 18. Grid behaviour with the flag OFF is byte-for-byte unchanged (strict-block / soft-degrade-to-block). `DefaultStrictPolicy` is untouched. Flip the two Phase-18 "grid still blocked with flag on" tests to "grid accepted with flag on" and add grid accept-path coverage.

Purpose: Establish the GRID acceptance gate (GRID-01/02/03) so the box tree builds grid only when the policy ACCEPTS it (pipeline order: `MPdfService` validates BEFORE layout). No new config key — the flag already exists.
Output: Gated grid policy + flipped/extended policy unit tests proving grid accept-path, grid-default-unchanged, and flex-still-accepted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block (single-repo phase). Commit with `git -C D:/sources/Core/muonroi-building-block`. SUMMARY.md goes in this phase dir.
Run tests PER-PROJECT (memory test_flakiness_nested_build): `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj`. Do NOT run the full solution.
MSTD: no raw `ArgumentNullException` (use `MGuard.NotNull`); no forward `cref` (CS1574).
</execution_context>

<context>
@.planning/phases/19-grid-layout-engine/19-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Exact current shapes (verified 2026-06-21, post-Phase-18). No codebase exploration needed. -->

src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs — current state (flex already gated by Phase 18):
  private readonly bool _softDegrade;            // line 54
  private readonly bool _allowModernLayout;      // line 55 — ALREADY present (Phase 18)
  public LegacyPrintPolicy() : this(softDegrade:false, allowModernLayout:false) {}        // line 61
  public LegacyPrintPolicy(IOptions<PdfConfigs> options)                                   // line 67 — DI ctor reads both flags
      : this(options?.Value?.Policy?.SoftDegradeUnknownDisplay ?? false,
             options?.Value?.Policy?.AllowModernLayout ?? false) {}
  ValidateAsync (line 98) → CheckCssFeatures(doc, violations, _softDegrade, _allowModernLayout)   // line 107 — flag ALREADY threaded
  private static void CheckCssFeatures(IDocument document, List<PolicyViolation> violations, bool softDegrade, bool allowModernLayout)   // line 135

  FLEX gate (the pattern to MIRROR for grid), line 253-262:
    if ((display is "flex" or "inline-flex") && !allowModernLayout) {
        if (softDegrade) { violations.Add(SoftDegradeViolationFor("soft-degrade.display.flex", ...)); softDegradeFlexTriggered = true; }
        else violations.Add(ViolationFor("forbidden.display.flex", ...)); }

  GRID gate (currently NOT gated — the line to CHANGE), line 264-273:
    if (display is "grid" or "inline-grid") {
        if (softDegrade) { violations.Add(SoftDegradeViolationFor("soft-degrade.display.grid", "display", display, selector)); softDegradeGridTriggered = true; }
        else violations.Add(ViolationFor("forbidden.display.grid", "display", display, selector, "display:table")); }

  Grid sub-prop branch (currently NOT gated — the branch to CHANGE), line 292-305 inside `if (softDegrade)` loop:
    bool isGrid = propName.StartsWith("grid", ...);
    if (isGrid) { if (!gridSubPropSeen) { gridSubPropSeen = true; violations.Add(new PolicyViolation("soft-degrade.grid-subproperty", ...)); } }
    else { /* flex branch, ALREADY guarded with `&& !allowModernLayout` at line 310 */ }

  Flex telemetry counter (line 338) fires on softDegradeFlexTriggered; grid telemetry (line 340) on softDegradeGridTriggered.

src/Muonroi.Pdf.Governance/Policies/DefaultStrictPolicy.cs — always hard-blocks grid. LEAVE UNCHANGED.

tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs — Phase 18 test class. Helpers already present:
  ParseAsync(html) (line 21); PolicyWith(bool allowModernLayout, bool softDegrade=false) (line 30) → new LegacyPrintPolicy(Options.Create(new PdfConfigs{ Policy = new PdfPolicySettings{ AllowModernLayout=..., SoftDegradeUnknownDisplay=... }})).
  The TWO tests to FLIP:
    - Grid_FlagOn_StrictBase_StillForbidden (line 62): asserts Accepted==false + Contains "forbidden.display.grid" with flag on. → FLIP to Accepted==true + NotContain "forbidden.display.grid".
    - Grid_FlagOn_SoftDegrade_StillSoftDegradeWarning (line 75): asserts Contains "soft-degrade.display.grid" with flag on. → FLIP to NotContain "soft-degrade.display.grid".

tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyTests.cs — has DisplayGrid_* strict/default expectations (flag-less). DO NOT modify — these prove GRID-03 default-unchanged.
</interfaces>

@src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs
@tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate LegacyPrintPolicy grid display + grid sub-prop acceptance on allowModernLayout (GRID-01, GRID-02)</name>
  <files>src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs</files>
  <read_first>
    - src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs:243-273 (flex gate at :253 — the exact pattern; grid block at :264-273)
    - src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs:282-323 (soft-degrade sub-prop loop; grid branch :292-305; flex branch already guarded at :310)
    - src/Muonroi.Pdf.Governance/Policies/LegacyPrintPolicy.cs:337-341 (soft-degrade telemetry counters)
  </read_first>
  <action>
In `LegacyPrintPolicy.CheckCssFeatures` (the flag `allowModernLayout` is ALREADY a parameter and ALREADY threaded from the DI ctor — Phase 18; no signature or ctor change needed):

- GRID DISPLAY GATE (line 264): change `if (display is "grid" or "inline-grid")` to `if ((display is "grid" or "inline-grid") && !allowModernLayout)`, mirroring the flex gate at line 253 EXACTLY. When the flag is on, NEITHER the `forbidden.display.grid` (strict) NOR the `soft-degrade.display.grid` (soft-degrade) violation is added, and `softDegradeGridTriggered` is NOT set. Leave the inner strict/soft-degrade body identical. Replace the existing `// FLEX-02 / FLEX-04` comment region note so it no longer says "grid stays blocked"; add `// GRID-01 / GRID-02: with allowModernLayout the engine renders real Grid via GridLayoutEngine — accept grid display.`

- GRID SUB-PROP GATE (line 294, inside the `if (softDegrade)` loop, the `if (isGrid)` branch): guard the warning emission with `&& !allowModernLayout`, mirroring the flex branch at line 310. Concretely change `if (!gridSubPropSeen)` to `if (!gridSubPropSeen && !allowModernLayout)`. Add `// GRID-02: when allowModernLayout is on the engine reads grid sub-props (they are NOT dropped) — do not emit the "will be ignored" warning.` The flex branch at line 310 is already correctly guarded — leave it.

- TELEMETRY (line 340): `softDegradeGridTriggered` can no longer be set for grid when the flag is on (the display gate above suppresses it), so the grid counter is naturally suppressed — no extra change. Leave the flex counter (line 338) untouched.

Do NOT touch the flex display gate (line 253), the flex sub-prop branch (line 310), `DefaultStrictPolicy`, `FlexGridSubProperties`, or any transform/gradient/script/href logic. The ONLY edits are the two `&& !allowModernLayout` additions to the grid branches.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet build src/Muonroi.Pdf.Governance/Muonroi.Pdf.Governance.csproj -c Debug</automated>
  </verify>
  <done>LegacyPrintPolicy suppresses ALL grid violations (display + grid sub-props) when AllowModernLayout=true; keeps flex accepted (Phase 18 unchanged); is byte-identical for grid when the flag is false. Governance builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip + extend grid policy tests — accept-path, flex-still-accepted, default-unchanged (GRID-02, GRID-03)</name>
  <files>tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs</files>
  <read_first>
    - tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyAllowModernLayoutTests.cs (full — ParseAsync + PolicyWith helpers; the two grid tests at :62 and :75 to flip)
    - tests/Muonroi.Pdf.Tests/Policy/LegacyPrintPolicyTests.cs (DisplayGrid_* flag-less expectations — DO NOT modify, used as the GRID-03 control)
  </read_first>
  <behavior>
    - Grid_FlagOn_StrictBase_Accepted (FLIPPED from Grid_FlagOn_StrictBase_StillForbidden): html `div{display:grid;}` → PolicyWith(allowModernLayout:true, softDegrade:false) → Accepted==true AND Violations NotContain RuleId "forbidden.display.grid".
    - Grid_FlagOn_SoftDegrade_Accepted_NoGridWarning (FLIPPED from Grid_FlagOn_SoftDegrade_StillSoftDegradeWarning): html `div{display:grid;}` → PolicyWith(allowModernLayout:true, softDegrade:true) → Violations NotContain "soft-degrade.display.grid".
    - GridWithSubProps_FlagOn_Accepted_NoGridSubpropWarning (NEW, GRID-02): html `div{display:grid;grid-template-columns:1fr 1fr;gap:8px;}` → PolicyWith(allowModernLayout:true, softDegrade:true) → Accepted==true AND Violations NotContain "soft-degrade.display.grid" AND NotContain "soft-degrade.grid-subproperty".
    - FlexAndGrid_FlagOn_BothAccepted (NEW, GRID-02): html with one `div{display:flex}` and one `div{display:grid}` → PolicyWith(allowModernLayout:true) → Accepted==true AND Violations NotContain "forbidden.display.flex" AND NotContain "forbidden.display.grid" (the flag unlocks BOTH).
    - Grid_FlagOff_StrictDefault_StillForbidden_BothPolicies (NEW, GRID-03): html `div{display:grid;}` → new LegacyPrintPolicy() (flag off, strict) → Accepted==false AND Contains "forbidden.display.grid"; AND new DefaultStrictPolicy() Violations Contains "forbidden.display.grid" (DefaultStrictPolicy ignores the flag).
    - GridWithSubProp_FlagOff_SoftDegrade_StillWarnsAndDegrades (NEW, GRID-03): html `div{display:grid;grid-template-columns:1fr;}` → PolicyWith(allowModernLayout:false, softDegrade:true) → Accepted==true AND Contains "soft-degrade.display.grid" (Warning) AND Contains "soft-degrade.grid-subproperty" (Warning).
  </behavior>
  <action>
In `LegacyPrintPolicyAllowModernLayoutTests.cs`:
- FLIP the existing `Grid_FlagOn_StrictBase_StillForbidden` (line 62) to `Grid_FlagOn_StrictBase_Accepted` per `<behavior>` — assert `result.Accepted.Should().BeTrue(...)` and `result.Violations.Should().NotContain(v => v.RuleId == "forbidden.display.grid")`.
- FLIP `Grid_FlagOn_SoftDegrade_StillSoftDegradeWarning` (line 75) to `Grid_FlagOn_SoftDegrade_Accepted_NoGridWarning` per `<behavior>` — assert `result.Violations.Should().NotContain(v => v.RuleId == "soft-degrade.display.grid")`.
- ADD the four NEW `[Fact]` tests in `<behavior>` (GridWithSubProps_FlagOn..., FlexAndGrid_FlagOn..., Grid_FlagOff_StrictDefault..., GridWithSubProp_FlagOff_SoftDegrade...). Reuse the existing `ParseAsync` + `PolicyWith` helpers; construct multi-rule HTML inline as in the existing tests. Use FluentAssertions `.Should().Contain(v => v.RuleId == "...")` / `.NotContain(...)` and `.Severity == PolicySeverity.Warning` exactly as the existing tests do.
- Update the class XML-doc bullet list so it states the flag now unlocks BOTH flex and grid.
- Do NOT touch the existing FLEX tests (FlexWithSubProps_FlagOn_Accepted_NoFlexViolation, Flex_FlagOff_StrictDefault..., FlexWithSubProp_FlagOff_SoftDegrade...) — they must stay green (proves flex unaffected — SC4). Do NOT touch LegacyPrintPolicyTests.cs (GRID-03 control).
RED note: the two flipped tests RED against the un-gated policy if run before Task 1; here Task 1 lands first, so confirm GREEN.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~LegacyPrintPolicyAllowModernLayoutTests|FullyQualifiedName~LegacyPrintPolicyTests"</automated>
  </verify>
  <done>The two grid tests are flipped to accept-path; four new grid tests pass (accept incl. sub-props, flex+grid both accepted, default strict + soft-degrade unchanged); all pre-existing flex tests and LegacyPrintPolicyTests stay green (SC4 + GRID-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → policy | PdfConfigs:Policy:AllowModernLayout (caller-supplied) now decides whether BOTH flex AND grid are accepted; it must NOT relax any other blocked feature (transforms, gradients, position:fixed, scripts, href schemes). |
| policy gate → layout | Only the ACCEPT decision reaches layout (MPdfService throws on reject); a mis-scoped flag would let unsupported CSS through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | AllowModernLayout grid scope creep | mitigate | Add `&& !allowModernLayout` ONLY to the grid display block (:264) and grid sub-prop branch (:294); leave position:fixed/sticky, transform, gradient, script, href, @import, @keyframes gates untouched — the FlexAndGrid_FlagOn test asserts only flex+grid unlock, and existing security policy tests stay green |
| T-19-02 | Tampering | DefaultStrictPolicy bypass | mitigate | DefaultStrictPolicy not modified; Grid_FlagOff_StrictDefault test asserts it still blocks grid regardless of flag |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; zero new external packages — Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `dotnet build` of Muonroi.Pdf.Governance green; `dotnet test` of the two affected policy test classes green.
- Grep confirms `(display is "grid" or "inline-grid") && !allowModernLayout` and `!gridSubPropSeen && !allowModernLayout` in LegacyPrintPolicy.cs.
- LegacyPrintPolicyTests.cs is unmodified (git diff shows no change to that file — GRID-03 control).
- Existing flex accept-path tests remain green (SC4 — flex unaffected).
</verification>

<success_criteria>
- GRID-01: LegacyPrintPolicy gates display:grid/inline-grid + grid sub-properties on the EXISTING AllowModernLayout flag (no new config key).
- GRID-02: AllowModernLayout=true → grid accepted (no violation, sub-props not dropped); flip the Phase-18 grid-still-blocked tests to grid-accepted; DefaultStrictPolicy always-strict.
- GRID-03: AllowModernLayout=false → grid behaviour byte-for-byte unchanged; LegacyPrintPolicyTests strict/default grid expectations unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-grid-layout-engine/19-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
