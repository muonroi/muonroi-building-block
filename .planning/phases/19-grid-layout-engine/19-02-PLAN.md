---
phase: 19-grid-layout-engine
plan: 02
type: execute
wave: 2
repo: muonroi-building-block
depends_on: [19-01]
files_modified:
  - src/Muonroi.Pdf/Internal/Layout/Boxes/GridContainerBox.cs
  - src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs
  - src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
  - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
  - tests/Muonroi.Pdf.Tests/Layout/GridBoxTreeTests.cs
autonomous: true
requirements: [GRID-04]

must_haves:
  truths:
    - "GridContainerBox : BoxNode exists carrying resolved container grid props: parsed explicit column/row track lists (after repeat()/minmax() expansion), auto-flow, auto-rows/auto-columns track templates, row/column gap, justify/align items + content, and the parsed grid-template-areas name grid"
    - "BoxNode carries nullable grid-item props (GridColumnRaw, GridRowRaw, GridAreaRaw, JustifySelf; AlignSelf reused from Phase 18) resolved for every child"
    - "A GridTrack model represents a single track size (px/pt/% as resolved-or-raw, fr, auto, minmax(min,max)); repeat(N, list) expands to N copies at parse time"
    - "BoxTreeBuilder maps display:grid/inline-grid → GridContainerBox ONLY when allowModernLayout is true; otherwise the existing BlockBox degrade-path is unchanged (the flag is ALREADY threaded — no new threading)"
    - "grid-template-columns/rows (incl. repeat()/minmax()), grid-template-areas, grid-column/-row/-area placement shorthands, grid-auto-flow, grid-auto-rows/columns, and gap/row-gap/column-gap are parsed and resolved"
  artifacts:
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/GridContainerBox.cs"
      provides: "grid container box type + resolved container props (GRID-04)"
      contains: "class GridContainerBox : BoxNode"
    - path: "src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs"
      provides: "track-size model (fr/auto/minmax/length) + track-list parse helpers (GRID-04)"
      contains: "GridTrack"
    - path: "src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs"
      provides: "gated grid→GridContainerBox mapping + grid prop resolution (GRID-04)"
      contains: "GridContainerBox"
    - path: "tests/Muonroi.Pdf.Tests/Layout/GridBoxTreeTests.cs"
      provides: "box-tree construction asserts (flag on → GridContainerBox; flag off → BlockBox; track-list/repeat/minmax/areas/placement parse)"
      contains: "GridContainerBox"
  key_links:
    - from: "src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs"
      to: "GridContainerBox"
      via: "CreateBox returns GridContainerBox when effectiveDisplay is grid/inline-grid and _allowModernLayout is on (mirrors the flex cases at :223-224)"
      pattern: "new GridContainerBox"
    - from: "src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs"
      to: "src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs"
      via: "ResolveGridProperties parses track lists into List<GridTrack> via GridTrack.ParseTrackList"
      pattern: "ParseTrackList"
---

<objective>
Add the `GridContainerBox` box type + a `GridTrack` track-size model, extend `BoxNode` with grid-item props, and wire `BoxTreeBuilder` to map `display:grid`/`inline-grid` to a `GridContainerBox` ONLY when `AllowModernLayout` is on (else keep the existing `BlockBox` degrade path byte-for-byte). The flag is ALREADY threaded `MPdfService → LayoutAsync → RunLayout → Build` (Phase 18) — grid reuses `_allowModernLayout` verbatim; NO new threading. Resolve grid container + item props: parse `grid-template-columns`/`rows` track lists (expanding `repeat()` and `minmax()`), the `grid-template-areas` string into a name→cell grid, `grid-auto-flow`, `grid-auto-rows`/`columns`, gaps, and the `grid-column`/`grid-row`/`grid-area` placement shorthands. No layout algorithm yet — that is Plan 03; this plan only produces the typed grid box tree.

Purpose: Give the GridLayoutEngine (Plan 03) a correctly-typed, fully-parsed grid box tree to size + place. Keeps the opt-in invariant: with the flag off, grid still falls through to BlockBox (preserving the soft-degrade path and the byte-identical default + flex baselines). This is the parse-heavy plan — grid track lists, `repeat()`/`minmax()`, and named-area parsing are the genuinely-new pieces that flex did not have.
Output: GridContainerBox + GridTrack model + BoxNode grid-item fields + gated mapping + grid prop resolution + box-tree unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Repo: muonroi-building-block. Commit with `git -C D:/sources/Core/muonroi-building-block`.
Run tests PER-PROJECT: `dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj`. Do NOT run the full solution (memory test_flakiness_nested_build).
MSTD: no raw `ArgumentNullException` (use MGuard.NotNull); no forward `cref`.
Parsers must NEVER throw on malformed input — fall back to CSS defaults (T-19-04), exactly like the Phase 18 flex parsers (ParseFlexShorthand, OneOf, Lower).
</execution_context>

<context>
@.planning/phases/19-grid-layout-engine/19-CONTEXT.md
@.planning/phases/19-grid-layout-engine/19-01-SUMMARY.md

<interfaces>
<!-- Exact existing shapes (verified 2026-06-21, post-Phase-18). -->

src/Muonroi.Pdf/Internal/Layout/Boxes/FlexContainerBox.cs — the container-box pattern to MIRROR:
  internal sealed class FlexContainerBox : BoxNode { public string FlexDirection {get;set;}="row"; ... public float RowGap {get;set;} public float ColumnGap {get;set;} public bool IsInlineFlex {get;set;} }
  All props default to CSS initial values; XML-doc each with its CSS source + allowed value set.

src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs — Phase 18 added the flex-item props (lines 88-114): FlexGrow/FlexShrink/FlexBasisRaw/Order/AlignSelf (all nullable; null = CSS initial). AlignSelf (line 114) is REUSED for grid. Add grid-item props the same way.

src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs (Phase 18 wired the flag + flex):
  private bool _allowModernLayout;   // line 12 — ALREADY present
  public BlockBox Build(IStyledNode root, IReadOnlyDictionary<string,DecodedImage>? resolvedImages=null, bool allowModernLayout=false)  // line 15 — sets _allowModernLayout
  CreateBox display switch (line 210-226): block/inline/table cases, then the gated flex cases at line 223-224:
      "flex" when _allowModernLayout => new FlexContainerBox { Source = node },
      "inline-flex" when _allowModernLayout => new FlexContainerBox { Source = node, IsInlineFlex = true },
      _ => new BlockBox { Source = node }
    grid/inline-grid CURRENTLY fall to the `_` default → BlockBox. ADD gated grid cases here.
  BuildNode recursion switch (line 50-104): has `case FlexContainerBox flexBox: ...; BuildChildren(node, flexBox); break;` (line 83-86). ADD a `case GridContainerBox gridBox:` calling a BuildChildren(node, gridBox) overload.
  BuildChildren overloads (line 857-983): includes `private void BuildChildren(IStyledNode node, FlexContainerBox parent)` (line 959). ADD a GridContainerBox overload (delegate to the generic BoxNode-children build, same body shape as the FlexContainerBox one).
  ResolveCssProperties (line 229): fontSize resolved at line 231; calls `ResolveFlexProperties(style, box, fontSize)` at line 402. ADD a `ResolveGridProperties(style, box, fontSize)` call right after line 402.
  ParseLength(string?, float fontSize) — existing length parser (returns points; "" → 0f; "auto"/null handled by caller). Reuse for fixed track lengths + gaps.
  Helpers already present: `private static string? Lower(string? v)` (line 756), `private static string OneOf(string? value, string fallback, params string[] allowed)` (line 761). Reuse for grid-auto-flow / justify-items etc.
  ResolveFlexProperties (line 586) + ParseFlexShorthand (line 678) — the TEMPLATE for ResolveGridProperties + the track-list parser style (TryParse + fall back to default, never throw).

src/Muonroi.Pdf/Internal/Service/MPdfService.cs + LayoutEngine.cs — flag fully threaded in Phase 18 (LayoutEngine ctor 27-30 wires FlexEngine; RunLayout line 235 calls Build(doc.Root, resolvedImages, allowModernLayout)). NO change needed in this plan.

tests/Muonroi.Pdf.Tests/Layout/FlexBoxTreeTests.cs — the box-tree assertion style to MIRROR: FakeStyledNode("div", new(){["display"]="flex",...}) with .ChildList.Add(child); new BoxTreeBuilder().Build(root, null, allowModernLayout:<bool>); assert `.Should().BeOfType<FlexContainerBox>().Which.FlexDirection.Should().Be("column")` etc. Mirror for grid.
</interfaces>

@src/Muonroi.Pdf/Internal/Layout/Boxes/FlexContainerBox.cs
@src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs
@src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GridTrack model + GridContainerBox + BoxNode grid-item props (GRID-04 contracts)</name>
  <files>src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs, src/Muonroi.Pdf/Internal/Layout/Boxes/GridContainerBox.cs, src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs</files>
  <read_first>
    - src/Muonroi.Pdf/Internal/Layout/Boxes/FlexContainerBox.cs (full — container-box shape + XML-doc style)
    - src/Muonroi.Pdf/Internal/Layout/Boxes/BoxNode.cs:88-114 (flex-item props pattern; AlignSelf at :114 reused)
  </read_first>
  <action>
Create `src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs` in namespace `Muonroi.Pdf.Internal.Layout.Boxes`:
- `internal enum GridTrackKind { Length, Percent, Fraction, Auto, MinMax }`.
- `internal sealed class GridTrack` (Claude's discretion: struct vs class — use a class for the MinMax nested-track case):
    * `GridTrackKind Kind`
    * `float Length` (points, for Length kind; resolved at parse time via ParseLength)
    * `float Percent` (0..1 fraction, for Percent kind — resolved against the container axis size at LAYOUT time, Plan 03)
    * `float Fraction` (the `fr` count, for Fraction kind; e.g. `2fr` → 2f)
    * `GridTrack? Min`, `GridTrack? Max` (for MinMax — each a non-MinMax track)
  XML-doc each kind with its CSS form. Auto = content-sized at layout time.
- Static parse API (used by BoxTreeBuilder; NEVER throws — malformed token → an `Auto` track or skip, documented):
    * `internal static List<GridTrack> ParseTrackList(string? value, float fontSize)` — splits a track-list string into tracks, EXPANDING `repeat(N, <track-list>)` to N copies of the inner list (N parsed as a positive int; `auto-fill`/`auto-fit` are OUT of scope per D-01 — treat a non-integer first arg by skipping the repeat and document it) and parsing `minmax(min, max)` into a MinMax track. Tokenization must handle nested parens (repeat may contain minmax). Empty/null → empty list.
    * `internal static GridTrack ParseSingleTrack(string token, float fontSize)` — `<number>fr`→Fraction; `auto`→Auto; `<len>%`→Percent; otherwise ParseLength→Length (fall back to Auto if unparseable).
  Implement repeat() expansion by extracting the integer count and the inner track-list substring (balanced-paren aware), recursively calling ParseTrackList on the inner list, and adding it count times.

Create `src/Muonroi.Pdf/Internal/Layout/Boxes/GridContainerBox.cs` — `internal sealed class GridContainerBox : BoxNode` in the same namespace. Container props (resolved by BoxTreeBuilder), with CSS-default values:
  - `List<GridTrack> TemplateColumns` = new() (from grid-template-columns; empty = no explicit columns)
  - `List<GridTrack> TemplateRows` = new() (from grid-template-rows)
  - `GridTrack? AutoColumns` = null, `GridTrack? AutoRows` = null (from grid-auto-columns/rows; null = auto)
  - `string AutoFlow` = "row" (grid-auto-flow: "row" | "column"; "dense" OUT of scope — strip a trailing "dense" token and document sparse-only per D-01)
  - `float RowGap` = 0f, `float ColumnGap` = 0f (points)
  - `string JustifyItems` = "stretch", `string AlignItems` = "stretch" (item-in-cell defaults)
  - `string JustifyContent` = "start", `string AlignContent` = "start" (track-group-in-container defaults; CSS grid initial is "normal" → treat as start)
  - `string[][] TemplateAreas` = empty (parsed grid-template-areas: a rectangular row-major array of area-name tokens, "." = empty cell). Plus a derived name→cell-rect lookup is computed at layout time (Plan 03) or here as Claude's discretion — store at least the raw `string[][]`.
  - `bool IsInlineGrid` = false (inline-grid; atomic block-level first cut per D-01, mirroring IsInlineFlex)
XML-doc each prop with its CSS source and allowed value set.

In `BoxNode.cs`, add nullable grid-ITEM props (resolved on EVERY child so any box type can be a grid item; null = CSS initial → zero behavioural change when unset), placed next to the Phase-18 flex-item props (after AlignSelf, line 114):
  - `string? GridColumnRaw` (raw `grid-column` token: e.g. "2", "1 / 3", "span 2", null)
  - `string? GridRowRaw` (raw `grid-row` token)
  - `string? GridAreaRaw` (raw `grid-area` token: a named area, or the row/col shorthand "r-start / c-start / r-end / c-end")
  - `string? JustifySelf` (null = "auto" → inherit container JustifyItems)
  - REUSE the existing `AlignSelf` (line 114) for grid align-self.
XML-doc each, noting "consumed by GridLayoutEngine (Plan 03); null = CSS initial value; untouched for non-grid layouts."
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet build src/Muonroi.Pdf/Muonroi.Pdf.csproj -c Debug</automated>
  </verify>
  <done>GridTrack model parses fr/auto/%/length/minmax/repeat without throwing; GridContainerBox exists with resolved container props; BoxNode carries nullable grid-item props (AlignSelf reused); Muonroi.Pdf builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate grid→GridContainerBox in BoxTreeBuilder + ResolveGridProperties (GRID-04)</name>
  <files>src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs, tests/Muonroi.Pdf.Tests/Layout/GridBoxTreeTests.cs</files>
  <read_first>
    - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs:210-226 (CreateBox display switch + the flex cases at :223-224 to mirror), :50-104 (BuildNode recursion switch + FlexContainerBox case at :83), :857-983 (BuildChildren overloads incl. FlexContainerBox at :959)
    - src/Muonroi.Pdf/Internal/Layout/BoxTreeBuilder.cs:229-402 (ResolveCssProperties + the ResolveFlexProperties call at :402), :586-668 (ResolveFlexProperties — the template), :756-770 (Lower / OneOf helpers)
    - src/Muonroi.Pdf/Internal/Layout/Boxes/GridTrack.cs + GridContainerBox.cs (Task 1)
    - tests/Muonroi.Pdf.Tests/Layout/FlexBoxTreeTests.cs (FakeStyledNode + Build assertion style)
  </read_first>
  <behavior>
    - Test 1 (flag on → GridContainerBox): FakeStyledNode "div" ["display"]="grid", built via `new BoxTreeBuilder().Build(root, null, allowModernLayout:true)` → child box `Should().BeOfType<GridContainerBox>()`.
    - Test 2 (flag off → BlockBox, degrade preserved): SAME node built with allowModernLayout:false → child box `Should().BeOfType<BlockBox>()` (NOT GridContainerBox) — proves the degrade path is unchanged.
    - Test 3 (track list + gap): "div" ["display"]="grid" ["grid-template-columns"]="100px 1fr 2fr" ["gap"]="8px" → GridContainerBox.TemplateColumns has 3 tracks: [0]=Length≈75pt (100px), [1]=Fraction 1, [2]=Fraction 2; RowGap≈6f and ColumnGap≈6f (8px→6pt).
    - Test 4 (repeat + minmax): ["grid-template-columns"]="repeat(3, 1fr)" → 3 Fraction(1) tracks. ["grid-template-rows"]="minmax(50px, 1fr)" → 1 MinMax track with Min=Length≈37.5pt and Max=Fraction 1.
    - Test 5 (auto-flow + auto-rows): ["grid-auto-flow"]="column" → AutoFlow=="column". ["grid-auto-rows"]="40px" → AutoRows is a Length≈30pt track. A trailing "dense" (e.g. "row dense") → AutoFlow=="row" (dense stripped, documented).
    - Test 6 (template-areas): ["grid-template-areas"]="\"head head\" \"nav main\"" → TemplateAreas is [["head","head"],["nav","main"]] (2 rows, 2 cols).
    - Test 7 (item placement shorthands): a grid container with a child ["grid-column"]="1 / 3" ["grid-row"]="span 2" ["justify-self"]="center" → child BoxNode GridColumnRaw=="1 / 3", GridRowRaw=="span 2", JustifySelf=="center". A child ["grid-area"]="main" → GridAreaRaw=="main".
  </behavior>
  <action>
BOX TREE (BoxTreeBuilder.cs) — no flag threading needed (`_allowModernLayout` already set in Build):
- In the CreateBox display switch (line 210-226), ADD BEFORE the `_ => new BlockBox` default, immediately after the flex cases (line 224):
    `"grid" when _allowModernLayout => new GridContainerBox { Source = node },`
    `"inline-grid" when _allowModernLayout => new GridContainerBox { Source = node, IsInlineGrid = true },`
  When the flag is off, grid/inline-grid still hit the `_` default → BlockBox (degrade unchanged). Update the existing comment near line 222 (which says grid is "NOT mapped here (Plan-19 scope)") to note grid is now gated like flex.
- In the BuildNode recursion switch (line 50-104), ADD `case GridContainerBox gridBox: BuildChildren(node, gridBox); break;` next to the FlexContainerBox case (line 83), so grid children are built.
- Add a `private void BuildChildren(IStyledNode node, GridContainerBox parent)` overload next to the FlexContainerBox one (line 959), with the same body shape (iterate node children, BuildNode each, add to parent.Children).
- In `ResolveCssProperties`, ADD `ResolveGridProperties(style, box, fontSize);` right after the `ResolveFlexProperties(...)` call (line 402).
- Add `private static void ResolveGridProperties(IComputedStyle style, BoxNode box, float fontSize)` modeled on ResolveFlexProperties:
    * CONTAINER props (only when `box is GridContainerBox gc`):
        - `gc.TemplateColumns = GridTrack.ParseTrackList(style.GetValue("grid-template-columns"), fontSize);` (only assign when non-empty so the default stays an empty list).
        - `gc.TemplateRows = GridTrack.ParseTrackList(style.GetValue("grid-template-rows"), fontSize);`
        - `grid-auto-columns`/`grid-auto-rows` → `GridTrack.ParseSingleTrack` (null when unset).
        - `grid-auto-flow`: strip a "dense" token (document sparse-only), then `OneOf(remaining, "row", "row", "column")`.
        - gap shorthand (one or two lengths → RowGap, ColumnGap) then `row-gap`/`column-gap` overrides via ParseLength — copy the gap block from ResolveFlexProperties (line 615-634) verbatim (gap semantics are identical between flex and grid).
        - `justify-items`/`align-items` via OneOf("start"/"end"/"center"/"stretch", default "stretch"); `justify-content`/`align-content` via OneOf("start"/"end"/"center"/"space-between"/"space-around"/"space-evenly"/"stretch", default "start").
        - `grid-template-areas`: parse a string of quoted row strings (e.g. `"head head" "nav main"`) into `string[][]` — split on quote boundaries to get each row string, then split each row on whitespace into area-name tokens. Validate all rows have equal column count; on mismatch fall back to empty (document). Assign to `gc.TemplateAreas`.
    * ITEM props (every box): read `grid-column`/`grid-row`/`grid-area` raw (trimmed) into GridColumnRaw/GridRowRaw/GridAreaRaw; read `justify-self` via OneOf into JustifySelf (null when "auto"/unset). `align-self` (GridAreaRaw item) reuses the Phase-18 read in ResolveFlexProperties — do NOT re-read it.
  ALL parsing must use TryParse + fall back to defaults; never throw (T-19-04). Reuse ParseLength for any length; reuse GridTrack parse helpers for tracks; do NOT add another length parser.

TESTS: create `tests/Muonroi.Pdf.Tests/Layout/GridBoxTreeTests.cs` (namespace `Muonroi.Pdf.Tests.Layout`) implementing the seven `<behavior>` tests using `FakeStyledNode` + `new BoxTreeBuilder().Build(root, null, allowModernLayout:<bool>)` and FluentAssertions. For track lengths assert `.BeApproximately(75f, 0.1f)` (px→pt) and `.Kind.Should().Be(GridTrackKind.Fraction)` / `.Fraction.Should().Be(1f)`. For TemplateAreas assert array shape + tokens. RED first.
  </action>
  <verify>
    <automated>cd D:/sources/Core/muonroi-building-block && dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -c Debug --filter "FullyQualifiedName~GridBoxTreeTests"</automated>
  </verify>
  <done>display:grid maps to GridContainerBox only with the flag on (BlockBox otherwise); container props (track lists with repeat/minmax, auto-flow, auto-rows/cols, gap, justify/align, template-areas) and item placement shorthands resolve correctly; all seven box-tree tests pass; existing FlexBoxTreeTests + BoxTreeBuilderTests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag → box tree | AllowModernLayout decides grid→GridContainerBox vs BlockBox. When false, the box tree MUST be byte-for-byte identical to today (preserves the default + flex goldens — verified in Plan 04). |
| author CSS → resolved grid props | Untrusted grid-template-*/repeat()/minmax()/grid-template-areas/placement strings parsed; malformed values must fall back to CSS defaults, never throw, and must not allocate unbounded (repeat count). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | BoxTreeBuilder degrade path | mitigate | grid cases use `when _allowModernLayout`; Test 2 asserts BlockBox when flag off — degrade path unchanged (GRID-04) |
| T-19-04 | Denial of Service | track-list / repeat() / minmax() / template-areas parser | mitigate | TryParse + fall back to default; never throw; CLAMP repeat() count to a sane maximum (e.g. 1000) so `repeat(99999999, 1fr)` cannot allocate unbounded tracks — assert the clamp behaviour in a parser unit test |
| T-19-05 | Denial of Service | grid-template-areas parse | mitigate | Reject ragged/empty area grids (fall back to empty) so downstream cell-rect math cannot index out of bounds |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; zero new external packages — Package Legitimacy Gate not triggered |
</threat_model>

<verification>
- `dotnet build` of Muonroi.Pdf green; `dotnet test --filter GridBoxTreeTests` green.
- Grep confirms `new GridContainerBox` in BoxTreeBuilder.cs (gated `when _allowModernLayout`) and `GridTrack.ParseTrackList` invoked in ResolveGridProperties.
- Existing BoxTreeBuilderTests + FlexBoxTreeTests still green (degrade + flex paths unchanged).
- repeat() count is clamped (no unbounded allocation) — parser test asserts the clamp.
</verification>

<success_criteria>
- GRID-04: GridContainerBox + GridTrack model + BoxNode grid-item fields exist; BoxTreeBuilder maps grid→GridContainerBox when the flag is on (else BlockBox); container props (track lists w/ repeat()/minmax(), grid-template-areas, auto-flow, auto-rows/cols, gaps) + item placement shorthands (grid-column/-row/-area, justify-self) resolved. The flag is reused (already threaded — no new threading).
</success_criteria>

<output>
Create `.planning/phases/19-grid-layout-engine/19-02-SUMMARY.md` when done.
</output>
</content>
