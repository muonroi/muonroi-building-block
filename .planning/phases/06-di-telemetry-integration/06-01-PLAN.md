---
phase: 06-di-telemetry-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Muonroi.Pdf.Abstractions/Telemetry/PdfTelemetryDescriptor.cs
  - src/Muonroi.Pdf/Internal/Telemetry/PdfMetrics.cs
autonomous: true
requirements:
  - TEL-01
  - TEL-02
  - TEL-03
  - TEL-04
  - DI-04

must_haves:
  truths:
    - "PdfTelemetryDescriptor implements ITelemetryDescriptor with a public parameterless constructor"
    - "PdfTelemetryDescriptor.ActivitySourceNames and MeterNames both yield 'Muonroi.BuildingBlock.Pdf'"
    - "PdfMetrics.Source is a static ActivitySource named 'Muonroi.BuildingBlock.Pdf' that is never disposed"
    - "PdfMetrics.OperationCounter is Counter<long> named 'pdf.operation'; PdfMetrics.PageCountHistogram is Histogram<int> named 'pdf.page_count'"
  artifacts:
    - path: "src/Muonroi.Pdf.Abstractions/Telemetry/PdfTelemetryDescriptor.cs"
      provides: "ITelemetryDescriptor discovery token for OtelSetup auto-registration"
      contains: "PdfTelemetryDescriptor"
    - path: "src/Muonroi.Pdf/Internal/Telemetry/PdfMetrics.cs"
      provides: "Static ActivitySource + Meter + Counter + Histogram for the render pipeline"
      contains: "PdfMetrics"
  key_links:
    - from: "PdfTelemetryDescriptor"
      to: "PdfTelemetryNames.ActivitySourceName"
      via: "collection expression returning the constant"
      pattern: "PdfTelemetryNames.ActivitySourceName"
    - from: "PdfMetrics"
      to: "PdfTelemetryNames.OperationMetric"
      via: "_meter.CreateCounter"
      pattern: "CreateCounter<long>"
---

<objective>
Create the two telemetry building blocks consumed by the render service in Wave 2: the `PdfTelemetryDescriptor` discovery token (Abstractions) and the static `PdfMetrics` class (impl) holding the `ActivitySource`, `Meter`, counter, and histogram.

Purpose: `OtelSetup.cs` auto-discovers every `ITelemetryDescriptor` via reflection and registers its activity-source and meter names with the OpenTelemetry pipeline. The descriptor must exist and be registered (Plan 06-02) so spans/metrics from `PdfMetrics` are actually collected. Splitting this from `MPdfService` keeps the static telemetry singletons isolated and lets Wave 2 reference them by name without exploration.
Output: 2 files created; both owning projects build green.
</objective>

<execution_context>
@C:/Users/phila/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/phila/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-di-telemetry-integration/06-CONTEXT.md
@.planning/phases/06-di-telemetry-integration/06-RESEARCH.md

## What exists (verified)

`src/Muonroi.Pdf.Abstractions/Telemetry/PdfTelemetryNames.cs` (public static class) supplies all string constants:
- `ActivitySourceName = "Muonroi.BuildingBlock.Pdf"`
- `OperationMetric = "pdf.operation"`
- `PageCountMetric = "pdf.page_count"`
- `TemplateIdTag = "pdf.template_id"`
- `TenantIdTag = "tenant.id"`

`src/Muonroi.Core.Abstractions/Interfaces/ITelemetryDescriptor.cs` defines two members:
`IEnumerable<string> ActivitySourceNames { get; }` and `IEnumerable<string> MeterNames { get; }`.

`Muonroi.Pdf.Abstractions` targets `netstandard2.0`; it already references `Muonroi.Core.Abstractions` (the home of `ITelemetryDescriptor`) — verify the project reference exists before adding the using. If absent, add the ProjectReference (no Version, CPM).

`Muonroi.Pdf` targets `net8.0`; `System.Diagnostics.ActivitySource` and `System.Diagnostics.Metrics.Meter` are BCL — no NuGet package needed. The directory `src/Muonroi.Pdf/Internal/Telemetry/` does not exist yet — create it.
</context>

<interfaces>
<!-- Exact codebase pattern to mirror. Use directly — no exploration needed. -->

From src/Muonroi.Caching.Abstractions/Distributed/DistributedCacheTelemetryDescriptor.cs (the descriptor model):
```csharp
public class DistributedCacheTelemetryDescriptor : ITelemetryDescriptor
{
    public IEnumerable<string> ActivitySourceNames => [DistributedCacheRuntimeTelemetry.ActivitySourceName];
    public IEnumerable<string> MeterNames => [DistributedCacheRuntimeTelemetry.ActivitySourceName];
}
```

From src/Muonroi.Observability/OtelSetup.cs (why the descriptor must be a registered, non-abstract, parameterless type):
```csharp
.Where(t => typeof(ITelemetryDescriptor).IsAssignableFrom(t) && t is { IsInterface: false, IsAbstract: false })
.Cast<ITelemetryDescriptor>()
var activitySources = descriptors.SelectMany(d => d.ActivitySourceNames).Distinct().ToList();
var meters = descriptors.SelectMany(d => d.MeterNames).Distinct().ToList();
```

Static telemetry pattern from src/Muonroi.Caching.Abstractions/Distributed/DistributedCacheRuntimeTelemetry.cs:
static readonly ActivitySource; private static readonly Meter; static readonly Counter/Histogram created from the Meter. Never disposed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PdfTelemetryDescriptor (Abstractions)</name>
  <files>src/Muonroi.Pdf.Abstractions/Telemetry/PdfTelemetryDescriptor.cs</files>
  <action>
Create `PdfTelemetryDescriptor` in namespace `Muonroi.Pdf.Abstractions.Telemetry` (TEL-01, TEL-05/DI-04 discovery):
- `using Muonroi.Core.Abstractions.Interfaces;`
- `public sealed class PdfTelemetryDescriptor : ITelemetryDescriptor`
- Implicit public parameterless constructor (do NOT add an explicit one with parameters — OtelSetup's reflection requires parameterless).
- `public IEnumerable<string> ActivitySourceNames => [PdfTelemetryNames.ActivitySourceName];`
- `public IEnumerable<string> MeterNames => [PdfTelemetryNames.ActivitySourceName];`

Mirror `DistributedCacheTelemetryDescriptor` exactly (per CONTEXT.md Decision 4 — descriptor travels with Abstractions, holds no state). Both members return the single activity-source name; the meter name equals the activity-source name by convention in this repo.

Before building, confirm `Muonroi.Pdf.Abstractions.csproj` has a ProjectReference to `Muonroi.Core.Abstractions`. If not present, add `<ProjectReference Include="..\Muonroi.Core.Abstractions\Muonroi.Core.Abstractions.csproj" />` (no Version attribute — CPM). Note: `Muonroi.Core.Abstractions` must also target netstandard2.0-compatible; if it does not, surface the mismatch in the SUMMARY rather than forcing the reference.
  </action>
  <verify>
    <automated>dotnet build src/Muonroi.Pdf.Abstractions/Muonroi.Pdf.Abstractions.csproj -m:1 -nodereuse:false -q</automated>
  </verify>
  <done>PdfTelemetryDescriptor compiles in Abstractions; implements ITelemetryDescriptor; parameterless ctor; both members return [PdfTelemetryNames.ActivitySourceName]</done>
</task>

<task type="auto">
  <name>Task 2: PdfMetrics static telemetry (impl)</name>
  <files>src/Muonroi.Pdf/Internal/Telemetry/PdfMetrics.cs</files>
  <action>
Create directory `src/Muonroi.Pdf/Internal/Telemetry/` and file `PdfMetrics.cs` in namespace `Muonroi.Pdf.Internal.Telemetry` (TEL-02, TEL-03, TEL-04):
- `using System.Diagnostics;` and `using System.Diagnostics.Metrics;` and `using Muonroi.Pdf.Abstractions.Telemetry;`
- `internal static class PdfMetrics`
- `internal static readonly ActivitySource Source = new(PdfTelemetryNames.ActivitySourceName);`
- `private static readonly Meter _meter = new(PdfTelemetryNames.ActivitySourceName);`
- `internal static readonly Counter<long> OperationCounter = _meter.CreateCounter<long>(PdfTelemetryNames.OperationMetric, unit: "{render}", description: "Counts PDF render operations.");`
- `internal static readonly Histogram<int> PageCountHistogram = _meter.CreateHistogram<int>(PdfTelemetryNames.PageCountMetric, unit: "{page}", description: "Distribution of page count per PDF render.");`

CRITICAL (Pitfall 2): all four are `static readonly` class-level fields. Never wrap `Source` or `_meter` in a `using`; they live for the process lifetime. The `Source` field is consumed by `MPdfService` in Plan 06-03 to start activities.
  </action>
  <verify>
    <automated>dotnet build src/Muonroi.Pdf/Muonroi.Pdf.csproj -m:1 -nodereuse:false -q</automated>
  </verify>
  <done>PdfMetrics compiles; Source is ActivitySource("Muonroi.BuildingBlock.Pdf"); OperationCounter is Counter<long> "pdf.operation"; PageCountHistogram is Histogram<int> "pdf.page_count"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render engine → telemetry exporter | Span tags (template id, tenant id) cross into the observability pipeline; must not carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | PdfMetrics tags | mitigate | Only template id and tenant id are recorded; HTML content and resolved bytes are never written to spans or metric tags |
| T-06-02 | Denial of Service | static ActivitySource/Meter disposal | mitigate | Declared static readonly; never disposed — prevents ObjectDisposedException that would crash all subsequent renders |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; ActivitySource/Meter are BCL types, no new NuGet reference |
</threat_model>

<verification>
dotnet build src/Muonroi.Pdf.Abstractions/Muonroi.Pdf.Abstractions.csproj -m:1 -nodereuse:false -q
dotnet build src/Muonroi.Pdf/Muonroi.Pdf.csproj -m:1 -nodereuse:false -q
</verification>

<success_criteria>
- `PdfTelemetryDescriptor` exists in Abstractions, implements `ITelemetryDescriptor`, has a public parameterless constructor (OtelSetup-discoverable)
- Both `ActivitySourceNames` and `MeterNames` return `["Muonroi.BuildingBlock.Pdf"]`
- `PdfMetrics.Source` is a static `ActivitySource` named `Muonroi.BuildingBlock.Pdf`
- `PdfMetrics.OperationCounter` is `Counter<long>` named `pdf.operation`; `PdfMetrics.PageCountHistogram` is `Histogram<int>` named `pdf.page_count`
- Both projects build with 0 errors
</success_criteria>

<output>
Create `.planning/phases/06-di-telemetry-integration/06-01-SUMMARY.md` when done
</output>
