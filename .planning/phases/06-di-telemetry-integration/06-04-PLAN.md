---
phase: 06-di-telemetry-integration
plan: 04
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - tests/Muonroi.Pdf.Tests/Service/DependencyInjectionTests.cs
  - tests/Muonroi.Pdf.Tests/Service/ConfigValidationTests.cs
  - tests/Muonroi.Pdf.Tests/Service/MPdfServiceIntegrationTests.cs
  - tests/Muonroi.Pdf.Abstractions.Tests/Telemetry/PdfTelemetryDescriptorTests.cs
autonomous: true
requirements:
  - DI-01
  - DI-02
  - DI-03
  - DI-04
  - PIPE-08
  - TEL-01
  - TEL-02
  - TEL-03
  - TEL-04

must_haves:
  truths:
    - "A test proves AddPdf registers all pipeline services and is idempotent when called twice (SC1)"
    - "A test proves RenderAsync converts valid HTML to a non-empty stream whose first 8 bytes are %PDF-1.7 (SC2)"
    - "A test proves a render emits a completed activity on Muonroi.BuildingBlock.Pdf carrying pdf.template_id and tenant.id, and that pdf.page_count is recorded (SC3)"
    - "A test proves a render exceeding MaxRenderDurationMs is cancelled with OperationCanceledException (SC4)"
    - "A test proves PdfConfigs with MaxPages:0 throws at startup via ValidateOnStart (SC5)"
  artifacts:
    - path: "tests/Muonroi.Pdf.Tests/Service/DependencyInjectionTests.cs"
      provides: "SC1 + DI-02/DI-04 coverage"
      contains: "AddPdf"
    - path: "tests/Muonroi.Pdf.Tests/Service/ConfigValidationTests.cs"
      provides: "SC5 startup validation coverage"
      contains: "ValidateOnStart"
    - path: "tests/Muonroi.Pdf.Tests/Service/MPdfServiceIntegrationTests.cs"
      provides: "SC2 + SC3 + SC4 end-to-end coverage"
      contains: "%PDF-1.7"
    - path: "tests/Muonroi.Pdf.Abstractions.Tests/Telemetry/PdfTelemetryDescriptorTests.cs"
      provides: "TEL-01 descriptor coverage"
      contains: "PdfTelemetryDescriptor"
  key_links:
    - from: "MPdfServiceIntegrationTests"
      to: "ActivityListener"
      via: "subscribe to Muonroi.BuildingBlock.Pdf source"
      pattern: "ActivityListener"
    - from: "MPdfServiceIntegrationTests"
      to: "MeterListener"
      via: "observe pdf.page_count instrument"
      pattern: "MeterListener"
    - from: "ConfigValidationTests"
      to: "OptionsValidationException"
      via: "Host.StartAsync throws"
      pattern: "OptionsValidationException"
---

<objective>
Add the integration test suite that proves all five Phase 6 success criteria. This is the verification gate for the phase — it exercises the wiring from Plans 06-01 through 06-03 against a real DI container and a real render.

Purpose: Without these tests, SC1–SC5 are only asserted by inspection. This plan converts each success criterion into an executable assertion. It depends on all three implementation plans completing.
Output: 4 test files; `dotnet test` passes (existing 47 + new tests).
</objective>

<execution_context>
@C:/Users/phila/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/phila/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-di-telemetry-integration/06-CONTEXT.md
@.planning/phases/06-di-telemetry-integration/06-RESEARCH.md

## Test infrastructure (verified)

- `tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj` references `Muonroi.Pdf` and `Muonroi.Pdf.Abstractions` and has `InternalsVisibleTo` from the engine. xunit 2.9.2 + FluentAssertions 7.2.0 + NSubstitute 5.3.0 inherited via Directory.Build.props.
- `tests/Muonroi.Pdf.Abstractions.Tests/` exists for the descriptor unit test.
- Existing tests under `Layout/`, `Font/`, `Image/`, `Writer/` total 47 and must keep passing.

The DI extension test needs `Microsoft.Extensions.DependencyInjection`, `Microsoft.Extensions.Configuration`, `Microsoft.Extensions.Hosting` (for `ValidateOnStart` → `Host.StartAsync`). These are in CPM; add the PackageReferences to `Muonroi.Pdf.Tests.csproj` if not already present (no inline Version).

## Activity/Meter capture patterns (BCL)

Activity capture: create an `ActivityListener` with `ShouldListenTo = src => src.Name == PdfTelemetryNames.ActivitySourceName`, `Sample = (ref _) => ActivitySamplingResult.AllDataAndRecorded`, and an `ActivityStopped` callback that collects stopped activities into a list. Register via `ActivitySource.AddActivityListener(listener)` BEFORE the render. Assert the collected activity has tags `pdf.template_id` and `tenant.id`.

Metric capture: use `MeterListener` with `InstrumentPublished = (inst, l) => { if (inst.Meter.Name == PdfTelemetryNames.ActivitySourceName) l.EnableMeasurementEvents(inst); }` and `SetMeasurementEventCallback<int>` to capture `pdf.page_count` recordings. Call `listener.Start()` before the render.
</context>

<interfaces>
<!-- Build a real container for the integration + DI tests -->
var config = new ConfigurationBuilder().AddInMemoryCollection(new Dictionary<string,string?> {
    ["PdfConfigs:Limits:MaxHtmlBytes"] = "8388608",
    ["PdfConfigs:Limits:MaxDomDepth"] = "256",
    ["PdfConfigs:Limits:MaxElementCount"] = "100000",
    ["PdfConfigs:Limits:MaxImagePixels"] = "25000000",
    ["PdfConfigs:Limits:MaxPages"] = "1000",
    ["PdfConfigs:Limits:MaxRenderDurationMs"] = "15000",
    ["PdfConfigs:Limits:MaxFontFiles"] = "32",
}).Build();
var services = new ServiceCollection();
services.AddPdf(config); // Muonroi.Pdf.Extensions
// IMLog<MPdfService> must be registered for MPdfService to resolve — register a test double
// (NSubstitute Substitute.For<IMLog<MPdfService>>() or a no-op IMLogFactory) if AddPdf does not bring logging.
var provider = services.BuildServiceProvider();
var svc = provider.GetRequiredService<IMPdfService>();
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DI registration + telemetry descriptor tests (SC1, TEL-01)</name>
  <files>
    tests/Muonroi.Pdf.Tests/Service/DependencyInjectionTests.cs
    tests/Muonroi.Pdf.Abstractions.Tests/Telemetry/PdfTelemetryDescriptorTests.cs
  </files>
  <action>
`DependencyInjectionTests.cs` (namespace matching the test project convention, e.g. `Muonroi.Pdf.Tests.Service`):
- Test `AddPdf_RegistersAllPipelineServices`: build a ServiceCollection, register a no-op `IMLog<MPdfService>` double (and `IMLogFactory` if needed), call `AddPdf(config)`, `BuildServiceProvider()`, assert `GetService<IMPdfService>()`, `IHtmlParser`, `ICssCascadeEngine`, `IPdfCssPolicy`, `IImageDecoder`, `IResourceResolver`, `IPdfWriter` are all non-null.
- Test `AddPdf_DoesNotRegisterFontResolver`: assert `GetService<IFontResolver>()` is null (DI-02/DI-04 — no default).
- Test `AddPdf_CalledTwice_DoesNotDuplicate` (SC1): build a ServiceCollection, call `AddPdf(config)` twice, assert the count of `ServiceDescriptor`s with `ServiceType == typeof(IMPdfService)` is exactly 1 (use `services.Count(d => d.ServiceType == typeof(IMPdfService))`). Repeat the assertion for `IPdfWriter`. Also assert `BuildServiceProvider().GetRequiredService<IMPdfService>()` resolves without throwing.

`PdfTelemetryDescriptorTests.cs` (TEL-01):
- Test `Descriptor_HasParameterlessCtor_AndCorrectNames`: `var d = new PdfTelemetryDescriptor();` assert `d.ActivitySourceNames` is `["Muonroi.BuildingBlock.Pdf"]` and `d.MeterNames` is `["Muonroi.BuildingBlock.Pdf"]` (use FluentAssertions `.Should().ContainSingle().Which.Should().Be(...)`). Assert it is assignable to `ITelemetryDescriptor`.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false -q --filter "FullyQualifiedName~DependencyInjectionTests" && dotnet test tests/Muonroi.Pdf.Abstractions.Tests/Muonroi.Pdf.Abstractions.Tests.csproj -m:1 -nodereuse:false -q --filter "FullyQualifiedName~PdfTelemetryDescriptorTests"</automated>
  </verify>
  <done>DI registration tests pass: all services resolve, no IFontResolver default, AddPdf idempotent (single IMPdfService descriptor); descriptor test passes with correct source/meter names</done>
</task>

<task type="auto">
  <name>Task 2: Startup validation test (SC5)</name>
  <files>tests/Muonroi.Pdf.Tests/Service/ConfigValidationTests.cs</files>
  <action>
`ConfigValidationTests.cs`:
- Test `AddPdf_MaxPagesZero_ThrowsAtStartup` (SC5): build an in-memory config identical to the valid one but with `["PdfConfigs:Limits:MaxPages"] = "0"`. Build a host using `Host.CreateDefaultBuilder().ConfigureServices(s => { s.AddSingleton<IConfiguration>(config); RegisterLoggingDouble(s); s.AddPdf(config); })` OR a `ServiceCollection` + `services.AddPdf(config)` followed by forcing validation. Because `ValidateOnStart()` runs during host start, the cleanest assertion is to build a minimal `IHost` (`new HostBuilder().ConfigureServices(...).Build()`) and assert `await host.StartAsync()` throws `OptionsValidationException`. Use FluentAssertions `await act.Should().ThrowAsync<OptionsValidationException>()`.
- IMPORTANT (RESEARCH Pitfall 3): if `PdfConfigs.PdfLimits` exposes only `const` members and the `MaxPages:0` config value cannot bind to a settable instance property, this test will FAIL because validation never sees 0. If that is the case, STOP and record a BLOCKING finding in the SUMMARY: "SC5 cannot be satisfied — PdfConfigs.PdfLimits needs settable instance properties; requires an Abstractions gap-closure plan." Do not weaken the test to pass.
- Add a positive test `AddPdf_ValidLimits_StartsSuccessfully`: same host with valid config; assert `await host.StartAsync()` does not throw, then `await host.StopAsync()`.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false -q --filter "FullyQualifiedName~ConfigValidationTests"</automated>
  </verify>
  <done>ConfigValidationTests pass: MaxPages:0 throws OptionsValidationException at host start; valid config starts cleanly — OR a BLOCKING finding is recorded in the SUMMARY if PdfLimits is const-only</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end render + telemetry + timeout tests (SC2, SC3, SC4)</name>
  <files>tests/Muonroi.Pdf.Tests/Service/MPdfServiceIntegrationTests.cs</files>
  <action>
`MPdfServiceIntegrationTests.cs` — resolve `IMPdfService` from a real `AddPdf` container (register a no-op `IMLog<MPdfService>` double). Use a small valid HTML document, e.g. `"<html><body><p>Hello Muonroi PDF</p></body></html>"`, and default `PdfRenderOptions` (A4 portrait). Set `options.TemplateId` if that member exists; otherwise omit.

- Test `RenderAsync_ValidHtml_ProducesPdf17Stream` (SC2): `using var ms = new MemoryStream(); var result = await svc.RenderAsync(html, ms, options, default);` Assert `ms.Length > 0`, `result.ByteCount > 0`, `result.PageCount >= 1`. Read the first 8 bytes (`ms.Position = 0; var head = new byte[8]; ms.Read(head, 0, 8);`) and assert `Encoding.ASCII.GetString(head) == "%PDF-1.7"`.

- Test `RenderAsync_EmitsActivityWithSnakeCaseTags` (SC3): register an `ActivityListener` on `PdfTelemetryNames.ActivitySourceName` (see context pattern) BEFORE the render, collecting stopped activities. After `RenderAsync`, assert exactly one activity was stopped, its `OperationName`/`DisplayName` is `pdf.render`, `activity.Status == ActivityStatusCode.Ok`, and it has tags `pdf.template_id` and `tenant.id` (assert tag keys are present and snake_case; `tenant.id` value is `"unknown"` in the no-HTTP test context). Dispose the listener at test end.

- Test `RenderAsync_RecordsPageCountHistogram` (SC3 metric): register a `MeterListener` capturing `pdf.page_count` int measurements before the render; after `RenderAsync`, assert at least one measurement was recorded and its value equals `result.PageCount`. Also assert `pdf.operation` counter recorded a measurement of 1 with a `pdf.status` tag of `"ok"` (capture `long` measurements similarly).

- Test `RenderAsync_ExceedsTimeout_ThrowsOperationCanceled` (SC4): build a SECOND container whose config sets `["PdfConfigs:Limits:MaxRenderDurationMs"] = "1"` (1 ms). To force the render past 1 ms deterministically, register a stub `IHtmlParser` (via NSubstitute or a hand-written test double registered BEFORE `AddPdf` so `TryAddSingleton` keeps the stub) whose `ParseAsync` awaits `Task.Delay(500, ct)` and then throws/returns — i.e. it observes the linked token. Assert `await svc.RenderAsync(...)` throws `OperationCanceledException` (or `TaskCanceledException`, a subclass). Use `await act.Should().ThrowAsync<OperationCanceledException>()`. This proves the linked CTS + CancelAfter wiring (PIPE-08).
  - If overriding `IHtmlParser` proves brittle, alternatively register a stub `IPdfWriter` whose `WriteAsync` awaits `Task.Delay(500, ct)`; either seam observing the token satisfies SC4.
  </action>
  <verify>
    <automated>dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false -q --filter "FullyQualifiedName~MPdfServiceIntegrationTests"</automated>
  </verify>
  <done>Integration tests pass: %PDF-1.7 stream produced (SC2); pdf.render activity with pdf.template_id + tenant.id tags and Ok status emitted, pdf.page_count + pdf.operation recorded (SC3); 1 ms timeout render throws OperationCanceledException (SC4)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test container → AddPdf | Tests assert the real registration surface; no production code paths bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Repudiation | unverified success criteria | mitigate | Each of SC1–SC5 has a dedicated executable test; verification is no longer by inspection |
| T-06-10 | Denial of Service | timeout regression undetected | mitigate | SC4 test forces a render past a 1 ms budget and asserts OperationCanceledException — locks the PIPE-08 wiring against regression |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; test packages (xunit/FluentAssertions/NSubstitute) resolved via CPM, already pinned |
</threat_model>

<verification>
dotnet test tests/Muonroi.Pdf.Tests/Muonroi.Pdf.Tests.csproj -m:1 -nodereuse:false -q
dotnet test tests/Muonroi.Pdf.Abstractions.Tests/Muonroi.Pdf.Abstractions.Tests.csproj -m:1 -nodereuse:false -q
</verification>

<success_criteria>
- SC1: a test proves `AddPdf` registers all services and is idempotent (single `IMPdfService` descriptor after two calls)
- SC2: a test proves `RenderAsync` produces a non-empty stream beginning with `%PDF-1.7`
- SC3: a test proves a completed `pdf.render` activity on `Muonroi.BuildingBlock.Pdf` carries `pdf.template_id` + `tenant.id` tags, and `pdf.page_count` is recorded
- SC4: a test proves a render exceeding `MaxRenderDurationMs` throws `OperationCanceledException`
- SC5: a test proves `PdfConfigs` with `MaxPages: 0` throws `OptionsValidationException` at host startup
- All existing 47 tests plus the new tests pass
</success_criteria>

<output>
Create `.planning/phases/06-di-telemetry-integration/06-04-SUMMARY.md` when done
</output>
